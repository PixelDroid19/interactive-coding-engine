# Learning Safety Implementation Plan

> **For agentic workers:** Use superpowers:executing-plans in this session. User requested autonomous implementation; preserve the checkout and do not spawn agents.

**Goal:** Corregir permisos de IA y falsas evaluaciones antes de integrar la orientación por curso.

**Architecture:** Conservar el tutor y los repositorios existentes. Separar permiso de escritura de la decisión del modelo; separar reflexión de evidencia evaluada. Validar los efectos reales en estado, no solo el texto del prompt.

**Tech Stack:** React, TypeScript, Vitest, Vite; Fastify/PostgreSQL en el backend hermano.

**Spec:** `docs/superpowers/specs/2026-09-06-course-learning-polish.md`

## Global Constraints

- Español; temas normal y cyber; sin modelos descargados automáticamente.
- Sin commits, despliegues, migraciones de datos reales ni eliminación de cambios existentes.
- Trabajar directamente; cada corrección tiene regresión roja y verde.

### 1. Permisos y cancelación del tutor

**Files:** `src/learning/tutor/tutorTools.ts`, `tutorAgent.ts`, `tutorAgent.integration.test.ts`, `src/components/tutor/SocraticTutor.tsx`, `SocraticTutor.test.tsx`.

**Interfaces:** Conservar `allowsTutorWrite(mode, question): boolean` y `runTutorTurn(input, service, workspace)`. El ejecutor comprueba el permiso antes de generar contenido y la señal de cancelación antes de cada efecto.

- [x] Reproducir con una prueba de agente el caso `Explícame cómo corregir esto sin escribir la solución`: archivo original intacto, herramienta denegada y ninguna generación de archivo.
- [x] Ejecutar `vitest run src/learning/tutor/tutorAgent.integration.test.ts` y observar el fallo.
- [x] Rechazar negaciones y preguntas explicativas; aceptar órdenes directas claras como `Corrige app.js`. Modo pista/revisión/explicación nunca escribe.
- [x] Añadir cancelación durante generación y cambio de actividad/curso al panel; una respuesta tardía no escribe, no reemplaza la conversación actual ni declara éxito.
- [x] Verificar suite de tutor y panel; revisar el flujo completo, incluida reparación del plan.

### 2. Reflexiones sin calificación inventada

**Files:** `src/learning/exam.ts`, `exam.test.ts`, `curriculumEvidence.ts`, `types.ts`, `src/components/learning/ExamMode.tsx`, `LeaderMode.tsx`, pruebas asociadas y lectores de resultados.

**Interfaces:** La evaluación escrita produce estado sin calificar y conserva respuestas. `saveExamEvaluation` y `saveLeaderInterview` guardan intentos/reflexiones sin crear evidencia de dominio; formatos anteriores siguen legibles.

- [x] Probar que repetir palabras clave no obtiene clasificación verde ni incrementa habilidades.
- [x] Ejecutar la prueba y confirmar el fallo contra el cálculo actual.
- [x] Sustituir puntuación por registro sin calificar; enseñar preguntas de contraste y próximos pasos, no una etiqueta de fracaso.
- [x] Permitir respuestas breves y registrar dificultades sin mínimos arbitrarios; bloquear doble envío y mostrar errores de guardado sin falso éxito.
- [x] Verificar repositorio, UI y payload al backend con alumno/curso explícitos: recorrido de cuentas ficticias locales documentado el 7 de septiembre; OAuth externo no forma parte de esa prueba.

### 3. Próxima entrega y cierre parcial

- [x] Mantener abiertas las entregas de orientación por curso e integración de la especificación; documentar su implementación tras inspeccionar el catálogo y no sustituirlas por estos dos arreglos.
- [x] Ejecutar TypeScript, suite relevante y navegador antes de reportar resultados. Comparar `git diff --check` y estado de ambos repositorios.

## Orientación y evidencia verificadas el 6 de septiembre

- `nextLearningAction.ts` selecciona una actividad publicada del curso abierto: reanudar con tiempo guardado, práctica pendiente, siguiente actividad o repaso al terminar. No modifica el progreso. Las cinco rutas reales tienen pruebas; cursos vacíos y bloqueados no reciben destinos inventados.
- Roadmap ofrece una acción principal y mantiene el mapa libre. El apoyo de bases de JavaScript solo aparece en Fundamentos/JavaScript. Lit, Cells y AI Engineer abren su contenido real.
- Lectura, reproducción, migración de completados y autoevaluación de repaso no crean nuevas notas de dominio. Se preserva el historial anterior. La falta de notas no bloquea el aprendizaje; disponibilidad publicada sí.
- El tutor verifica el archivo vigente antes de escribir y se desmonta al cambiar de identidad. Regresión roja: una edición del alumno durante generación era reemplazada; verde: se conserva y se informa del conflicto.
- Frontend completo: 1091 aprobadas, 2 omitidas antes de la última protección de archivo; suite posterior de agente/panel/App: 46 aprobadas. TypeScript y build correctos. Continúan advertencias de chunks grandes y módulos Node externalizados en Pyodide.
- Backend completo: 210 aprobadas, 0 omitidas, incluidas 27 integraciones contra PostgreSQL desechable en loopback. TypeScript correcto. Cluster detenido al terminar; no se tocaron bases reales.
- Navegador: desktop y 390×844, normal/cyber; acción sugerida, gesto de inicio, reloj, pausa, editar, ejecutar (`prueba de navegación 5`), guardar al salir y abrir Depura (código inicial 0/2 con error de sintaxis). Destinos reales comprobados de JavaScript, Lit, Cells y AI Engineer. Viewport restaurado.

## Pendiente para el objetivo global

- Auditar intentos reales frente a saltar/ver solución y proyectos sin comprobaciones. Actualmente el registro curricular histórico de fallos y el de intentos remotos no tienen el mismo origen. No atribuir aprendizaje comprobado a estos registros hasta separar sus contratos.
- Comprobar la ayuda con un modelo local real y auditar la exclusión actual del tutor en AI Engineer sin provocar competencia entre modelos/GPU. Las protecciones de concurrencia están probadas con proveedores controlados; eso no acredita calidad pedagógica de un modelo real.
- Examen/entrevista escritos son compatibilidad local sin entrada desde la navegación vigente; no presentarlos como integración nueva visible ni como sincronización de respuestas al backend.
- Completar cobertura de todos los tipos de actividad y cuenta autenticada real en la orientación, no solo las entradas iniciales. La verificación de software no demuestra resultados de aprendizaje humano.

## Actualización de evidencia del 7 de septiembre

- Tutor real en Chrome/WebGPU: Qwen 3 devuelve una pista breve sobre las dos llamadas del ejercicio JavaScript, sin escribir código. Qwen 2.5 Coder produjo respuestas poco útiles en ese mismo caso; no se acredita calidad general de los modelos ni de todos los modos del tutor.
- El servicio reconoce solo el prefijo de razonamiento vacío de Qwen 3 en JSON estructurado; mantiene el rechazo de razonamiento no vacío y respuestas fuera del contrato. Se retiraron los registros temporales del contenido generado.
- Evidencias anteriores sin procedencia de una comprobación no se vuelven a enviar como dominio. El perfil original permanece intacto y las entradas antiguas ya encoladas quedan conservadas en `unverifiedEvidence`. Recarga y fallo de almacenamiento tienen pruebas. No se reescribió el historial ya publicado en el servidor.
- La exclusión del tutor en AI Engineer se mantiene porque sus laboratorios administran sus propios modelos. Al entrar en el curso se libera la sesión residente del tutor; la caché descargada permanece. Las preparaciones tardías no restauran motores liberados ni descartan una preparación posterior.
- Chrome: JavaScript con Qwen 3 preparado → AI Engineer, lectura «Elegir modelo según tu equipo», inspección del modelo sin descargar → vuelta al intento guardado de JavaScript → Qwen 3 preparado de nuevo desde caché. Esta prueba verifica continuidad visible; la propiedad del motor y sus cancelaciones se verifican con las regresiones del servicio.
- Backend: 210 pruebas aprobadas, incluidas 27 integraciones PostgreSQL; TypeScript correcto. Base desechable detenida conservando sus archivos. Sin despliegues, migraciones de bases reales ni commits.

Continúan pendientes la matriz integral de tipos de actividad, el recorrido autenticado real y la calidad pedagógica del tutor fuera del caso observado. Los puntos anteriores no sustituyen esos criterios.

## Recorrido autenticado y matriz del catálogo — 7 de septiembre

- `nextLearningAction.test.ts` recorre las 782 actividades disponibles de los cinco cursos, siguiendo el orden de módulos y usando los distintos registros de completado. Comprueba destino, tipo, módulo y ausencia de mutaciones. Distribución: Fundamentos 91, JavaScript 96, Lit 180, Cells 252 y AI Engineer 163. El catálogo vigente contiene clase, lectura, razonamiento, depuración y proyecto; los retos integrados en clase se auditan por separado.
- Se publicaron los cinco catálogos exportados actuales en un backend de pruebas enlazado con PostgreSQL desechable. El publicador individual y el de lote fallaban al pasar `--api`/`--dir`: el validador ejecutaba `parseArgs` al importarse. Se movió esa lectura a su entrada CLI y se añadieron dos pruebas que ejecutan los publicadores completos contra HTTP local.
- Chrome con frontend y API locales, sesiones ficticias persistidas por el repositorio real: alumna A → predicción 4/resultado 6 → función que supera tres casos → reflexión «Todavía me cuesta explicar return.» → autoevaluación «Con ayuda». PostgreSQL conserva los tres intentos con curso `fundamentos`, actividad `guided:variables:0`; la reflexión es `ungraded` y no tiene puntuación.
- El docente, con permiso real de tutor en la base de prueba (sin simulador de roles), abrió el intento y vio código, casos, predicción, explicación y autoevaluación. La alumna B recibió el formulario vacío y mantuvo cero intentos; A mantuvo sus tres registros. Una recarga de desarrollo también conservó el paso y el borrador, exigiendo volver a comprobar el código.
- Límite de evidencia: las sesiones se crearon como fixtures en loopback; no se probó el consentimiento OAuth ni el correo externo. Tampoco se ejecutó cada una de las 782 actividades manualmente: la matriz prueba la política de navegación, no el aprendizaje de una persona ni cada runtime.

Pendiente: comprobar orientación de retos internos pendientes y tipos de compatibilidad que no aparecen en el catálogo, y ampliar las pruebas reales de calidad del tutor. Mantener explícitas las limitaciones de OAuth externo y cobertura manual.

## Recuperación de retos dentro de clase — 7 de septiembre

- La sugerencia del mapa resuelve los retos pendientes mediante las clases reales del curso abierto y lleva a su marca de tiempo. Una reproducción completada no oculta un reto fallido; una comprobación posterior aprobada o un reto ya completado sí lo retiran de pendientes. Se conserva la prioridad de reanudar una actividad abierta.
- Regresión reproducida antes del arreglo con el reto real de Fundamentos 01 (96 173 ms). Pruebas del selector y del mapa verifican destino, módulo y tiempo; una prueba del reproductor confirma que el reto aparece tras el gesto de inicio al abrir en ese tiempo. También se comprueba aislamiento entre cursos.
- Verificación actual: frontend 1145 pruebas aprobadas, 0 fallidas y 2 omitidas; TypeScript, build y `git diff --check` correctos. Persisten las advertencias de tamaño de chunks y módulos Node externalizados de Pyodide.
- Chrome: conservar intento de depuración → mapa de JavaScript → actividad sugerida → clase correcta → regresar al mapa. Este recorrido fue como visitante; no sustituye el recorrido autenticado positivo de la nueva sugerencia de reto interno, que sigue pendiente junto con ampliar la calidad real del tutor.

## Verificación autenticada y contexto del tutor — 7 de septiembre

- Se cerró el recorrido autenticado pendiente con una alumna ficticia en el backend y PostgreSQL locales: Fundamentos 01 → marca del reto → comprobación real 0/3 → saltar el reto → completar reproducción → mapa. La sugerencia siguió señalando el reto pendiente y, después del gesto de inicio, lo abrió de nuevo. El borrador se guardó al salir. No se usó OAuth externo ni una cuenta real.
- La matriz de orientación ahora incluye los retos integrados publicados de los cinco cursos, además de sus 782 actividades principales. Las marcas del catálogo vigente pasan la política de recuperación; no se modificaron duraciones ni lecciones para conseguirlo.
- Una prueba real de Lit reveló que el modo pista solo consultaba el HTML activo, aunque este enlazaba el JavaScript del componente. Se reprodujo con dos pruebas rojas y se añadió lectura acotada de referencias locales directas: archivo activo y hasta dos relacionados; presupuesto de código compartido de 3000 caracteres; sin URLs externas, archivos inexistentes ni recorrido recursivo. Los diagnósticos conservan su sección, sin repetir el HTML.
- Un segundo fallo real se debía al filtro: rechazaba la pregunta del modelo por mencionar una etiqueta ya presente (`<alerta-app>`). La regresión reprodujo el borrador exacto. Ahora permite esa referencia textual conocida, pero sigue rechazando etiquetas nuevas, atributos, bloques HTML e instrucciones ejecutables. Se retiró toda instrumentación temporal.
- Chrome/WebGPU, mismo ejercicio Lit con 0/2 comprobaciones y Qwen 3: el panel informó que consultó tres archivos y devolvió «¿Quién registra la etiqueta <alerta-app> en el entorno de desarrollo?». No escribió archivos ni proporcionó una línea de solución. La comparación acredita estos arreglos de contexto y validación, no calidad universal del modelo.
- Verificación actual: frontend 1155 aprobadas, 0 fallidas, 2 omitidas; backend 212 aprobadas, 0 omitidas, incluidas integraciones PostgreSQL. TypeScript y build de ambos repositorios correctos. Se mantienen las advertencias de chunks grandes y Pyodide. API, Vite y PostgreSQL de prueba se detuvieron conservando sus datos; no hubo commits, despliegues ni cambios a cuentas reales.

Siguiente revisión concreta: las pistas y comprobaciones originales del catálogo Lit todavía incluyen casos que dictan una respuesta literal (por ejemplo, el último consejo y la segunda comprobación de su primer ejercicio de depuración). Auditar su aceptación por comportamiento sin sustituir los ejercicios por contenido genérico. La garantía de aprendizaje humano y la calidad de todos los modelos no se deducen de la suite de software.

## Depuración nativa por comportamiento — 7 de septiembre

- Revisadas las primeras siete prácticas de depuración de Web Components y Lit. Sus segundas comprobaciones ya no exigen una expresión regular de la solución: verifican nuevas instancias, construcción, conexión/desconexión/reconexión, ubicación real de la implementación, entradas numéricas distintas, ausencia booleana y propagación desde un componente anidado. Las pistas de registro, constructor, limpieza, encapsulación y booleanos orientan el diagnóstico sin dictar la línea final.
- Se corrigió además la lectura del contenido del host: si existe Shadow DOM, se consulta su contenido. Antes, una tarjeta correctamente construida seguía fallando porque el evaluador miraba solo el árbol exterior.
- Regresiones rojas antes del cambio: registro con variable/nombre calculado, comentario antes de `super()`, constructor heredado y cinco soluciones nativas de sintaxis alternativa. Nueva suite de 25 casos: siete programas iniciales incorrectos, variantes válidas y falsas correcciones que conservan listeners, dejan la implementación fuera de la raíz, fijan un número, mantienen `active="false"` o no atraviesan Shadow DOM. Junto con la auditoría curricular: 45 pruebas aprobadas.
- Chrome, interfaz real de depuración: editar `app.js` → Comprobar → 2/2 en cada una de las siete prácticas. En la quinta se usó asignación numérica directa, incluido el resultado cero; Happy DOM omite ese cero en su setter, por lo que la fixture automatizada serializa explícitamente el resultado. El evaluador publicado no se flexibilizó para aceptar un cero ausente.
- En la séptima, `bubbles: true` con `composed: false` da 1/2; opciones completas mediante una variable dan 2/2. El evaluador escucha en `document`, comprueba que la ruta nace en el componente y crea una frontera adicional real; no exige una forma concreta del código ni del `target` retargeteado.
- Verificación final: `npm run lint`, `npm run build` y `git diff --check` correctos. Suite completa con `--maxWorkers=2`: 1180 aprobadas, 0 fallidas, 2 omitidas. Dos ejecuciones con concurrencia predeterminada fallaron cerca de cinco segundos en casos distintos; la auditoría de cintas pasó por separado y la suite completa pasó al acotar procesos. No se aumentaron tiempos ni se omitieron pruebas para obtener verde. Persisten avisos de chunks grandes, Pyodide y avisos del entorno de tests.
- Alcance de esta revisión: un archivo de especificaciones, sus pruebas curriculares y una nueva suite de comportamiento. Sin cambios nuevos al backend, guiones, audio, IDs o títulos; sin commits ni despliegues. La prueba manual modifica intentos locales de visitante y no acredita aprendizaje humano.

Pendiente concreto: continuar la auditoría conductual de los retos de clase 01–07 y de las prácticas posteriores de Lit, conservando el contrato y la pedagogía de cada actividad. Esta revisión no declara auditado manualmente todo el catálogo ni todos los modelos del tutor.

## Retos nativos de clase y contraste — 7 de septiembre

- Los retos de clase 01–07 también se comprueban por comportamiento: registro y accesibilidad, construcción y Shadow DOM, reloj conectado/desconectado, encapsulación y estilos, conversión numérica, propiedades booleanas y eventos públicos desde una raíz anidada. La suite nativa reúne ahora 49 casos positivos y negativos; junto con flujo curricular son 69 pruebas aprobadas.
- En Chrome se publicaron estas siete clases en la API y PostgreSQL de pruebas y se ejecutaron sus comprobaciones con una alumna ficticia autenticada. Las siete aceptaron programas válidos; la primera rechazó una declaración de accesibilidad sin efecto real. No se publicó contenido en producción.
- La prueba del reloj usa una instancia oculta independiente: no desconecta ni altera el componente visible del alumno. Comprueba actualizaciones y su suspensión, no acredita por sí sola la ausencia de toda fuga de recursos.
- Antes del arreglo de colores, la suite frontend completa terminó con 1204 aprobadas, 0 fallidas y 2 omitidas; backend con 212 aprobadas. El panel de reflexión recibió después tokens de tema coherentes y cuatro regresiones de contraste sobre la hoja de estilos completa. Se reprodujeron tres fallos de contraste antes de corregirlos. Normal/cyber, escritura, habilitación y registro comprobados en Chrome; 23 pruebas específicas, TypeScript y build aprobados.
- API, Vite y PostgreSQL temporales quedaron detenidos; datos de pruebas preservados. El servidor original del usuario sigue intacto. No se alteraron audios, guiones, IDs ni títulos.

## Continuidad al terminar un reto — 7 de septiembre

- La comprobación visual detectó que registrar un reto al final de la clase volvía al segundo cero. Causa: el reproductor ejecutaba `seek` más allá de la duración y a continuación `play`, cuya semántica al final es repetir.
- Los dos flujos, saltar y continuar después de aprobar, ahora conservan el estado final si no queda contenido. Los retos intermedios siguen reanudando la narración. Al recuperar un intento, el gesto de continuar/saltar también retira la pantalla de inicio para no cubrir el cierre.
- Seis regresiones ejercitan el reproductor y el evaluador reales, incluidos intentos recuperados. Se observaron dos fallos por reinicio y luego dos por la pantalla de inicio antes de cada corrección; las 27 pruebas del reproductor y TypeScript pasan.
- Chrome en localhost: recuperar el intento tras recarga → saltar el reto final → cierre visible sin reproducción; volver al reto → editar una solución válida → comprobar 2/2 → continuar sin escribir → reloj en 05:08/05:08 y acciones de cierre accesibles. La prueba es local de visitante y usa el contenido publicado disponible; no constituye una nueva validación autenticada del backend.
- Validación final de esta corrección: 1214 pruebas frontend aprobadas, 0 fallidas y 2 omitidas con `--maxWorkers=2`; TypeScript, build y `git diff --check` correctos. Permanecen avisos de Pyodide y tamaño de chunks. En el navegador hubo una reconexión de Vite durante los comandos de validación y errores de canal de extensión; el cierre se volvió a comprobar después. No se acredita una consola completamente limpia ni cobertura móvil adicional.

Sigue pendiente la auditoría conductual posterior a la clase 07 de Lit. La cobertura automatizada de navegación por todo el catálogo y las pruebas concretas de tutor no equivalen a ejecutar manualmente todas las actividades ni a demostrar resultados de aprendizaje humano.

## Composición, estado y teclado — unidades 08–11

- Ocho comprobaciones de texto fuente, en clase y depuración, se sustituyeron por comportamiento real. Se prueban proyección e identidad de los nodos del consumidor, actualización de listas con estados distintos y vacíos, conservación del array anterior donde el enunciado pide inmutabilidad, paso de objetos, eliminación del id solicitado y coherencia entre datos y representación.
- El menú debe abrir, mover el foco, cerrar con Escape, devolver el foco y sincronizar ARIA. El control de guardar debe ejecutar su acción; una función sin usar que mencione el atributo ya no aprueba. Las instancias auxiliares se eliminan y la comprobación de teclado restaura el foco anterior.
- Se añadieron 23 regresiones a la suite nativa: ocho programas iniciales fallan todas sus comprobaciones, soluciones equivalentes pasan y falsas correcciones fallan. Se observaron 14 regresiones rojas en las dos tandas previas a los cambios. La suite nativa reúne 72 casos.
- Chrome con API y PostgreSQL locales y alumna ficticia autenticada: clase y depuración de las cuatro unidades aceptaron las ocho soluciones alternativas (2/2). Se usaron nodos DOM, `concat`, nombre calculado de `render`, acceso calculado a `item`, `splice`, comparación de Escape mediante una colección y `toggleAttribute`. PostgreSQL conserva ocho intentos `success` con curso y actividad correctos. Catálogo publicado solo en loopback, release `6dadcee2-986b-4f13-a104-08631484670c`.
- Validación final: 1237 pruebas frontend aprobadas, 0 fallidas, 2 omitidas; TypeScript, build y `git diff --check` correctos. Persisten advertencias de Pyodide, chunks y limpieza de recursos en Happy DOM. El editor mostró tres diagnósticos estáticos en la solución compacta del menú, aunque ambas comprobaciones de navegador pasaron; no se acredita un editor ni una consola sin diagnósticos.
- Archivos de esta revisión: `specs08to14.ts`, `nativeDebugBehavior.integration.test.ts` y este plan. Sin cambios nuevos al backend, guiones, audio, IDs o títulos. No se repitió la matriz móvil para esta modificación de evaluadores. Los ocho flujos manuales no equivalen a auditar todo el catálogo.

La auditoría conductual específica queda cubierta hasta la unidad 11. Las comprobaciones posteriores de Lit siguen pendientes de revisión; no se declara calidad universal del tutor ni aprendizaje humano demostrado.

## Asincronía y contrato publicable — unidades 13–14

- Se reprodujeron ocho errores mediante once casos reales de componentes nativos: falsos aprobados sin carga/cancelación, sin foco/acción de Confirmar o sin evento de notificación; falsos rechazados por nombres de variable o métodos calculados. Antes de corregir, ocho casos fallaron y tres pasaron.
- El reto de búsqueda ahora observa carga antes de resolver, desconecta una instancia independiente, comprueba que no publique después y verifica su uso tras reconectar. La depuración ejecuta carreras adicionales con etiquetas distintas y una respuesta vacía, sin exigir `requestId` ni otro nombre de variable. No se simula una API de red inexistente en el enunciado ni se acredita cobertura de todos los fallos remotos.
- El diálogo verifica mensajes diferentes, foco en Confirmar, `close`, el evento y la acción de Confirmar. La práctica de notificación verifica mostrar/cerrar en otra instancia y emitir el evento documentado. Su resultado esperado explicita `close`, y las pistas orientan sin entregar código. La prueba de foco conserva y restaura el foco anterior.
- Chrome con alumna ficticia autenticada y publicación local `b08d76bb-f6d4-4d47-9f94-6491ff29421e`: búsqueda sin protección 2/3, misma implementación con invalidación 3/3; carrera 2/2, diálogo 3/3 y notificación con método calculado 2/2. Los cinco intentos se comprobaron en PostgreSQL: un parcial 67 y cuatro éxitos 100 en sus actividades correctas.
- Validación: 103 pruebas específicas aprobadas; suite frontend completa 1248 aprobadas, 0 fallidas y 2 omitidas; TypeScript, build y `git diff --check` correctos. Persisten avisos de Pyodide, chunks grandes y limpieza de recursos del entorno de tests. Sin nueva matriz móvil ni afirmación de consola limpia.
- Archivos modificados en esta revisión: `specs08to14.ts`, `nativeDebugBehavior.integration.test.ts` y este plan. Sin cambios nuevos al backend, audios, guiones, duraciones o títulos; sin commits ni despliegues.

Pendiente concreto: unidad 12 (form-associated/ElementInternals, no implementado por Happy DOM) y comprobaciones posteriores desde la unidad 15. Para formularios hace falta una regresión de navegador nativo; no reemplazar ese contrato por un simulador permisivo.

## Formularios nativos — unidad 12

- Se añadió una regresión ejecutable en Chrome en `e2e/native/index.html`, con once programas conocidos y el generador/evaluador reales (`buildPreviewDocument` y `runChallengeValidation`). Usa un iframe aislado nuevo por caso; un error de evaluación nunca cuenta como rechazo correcto. No necesita backend, audio ni identidad, no escribe progreso y no entra en el build principal.
- Antes del cambio, 6/11 regresiones correctas: dos soluciones válidas con acceso calculado a las APIs se rechazaban; dos campos sin valor inicial y uno sin validez del host se aprobaban. Después, 11/11 correctas con el mismo conjunto de casos.
- La cantidad debe publicar el uno inicial, participar como host en el formulario, invalidarse con cero y negativos, recuperar validez con uno/cuatro y enviar el valor actual. La depuración comprueba el cupón inicial, cambios, vacío y el nombre elegido/modificado por el consumidor. Las comprobaciones crean y retiran sus propios formularios, sin mover el campo visible del alumno. El contrato nativo se contrastó con [WHATWG HTML, form-associated custom elements](https://html.spec.whatwg.org/multipage/custom-elements.html#custom-elements-face-example).
- Chrome, frontend/API/PostgreSQL locales, alumna ficticia autenticada y release local `dbf297f7-6b97-43ad-8277-deeab98bcf12`: solución calculada del reto 2/2 y depuración 3/3. PostgreSQL conserva ambos intentos `success`, puntuación 100 y actividad correcta. No se publicó en producción.
- Validación: suite frontend completa 1248 aprobadas, 0 fallidas y 2 omitidas; once casos adicionales en navegador nativo; TypeScript (incluida la nueva prueba), build y `git diff --check` correctos. Persisten avisos de Pyodide/chunks y del entorno de tests. La consola de Chrome registró errores de canal de extensión durante las recargas; no se acredita consola totalmente limpia. No se repitió la matriz móvil por esta modificación de evaluadores.
- Archivos: `specs08to14.ts`, los tres archivos de `e2e/native/` y este plan. Sin cambios nuevos a backend, guiones, MP3, duraciones, títulos o progreso real. La prueba de formularios se ejecuta por separado de Vitest, según su README.

La revisión específica de comprobaciones nativas 01–14 queda realizada. Sigue pendiente revisar las comprobaciones desde Lit 15; las pruebas de software no demuestran resultados de aprendizaje humano ni calidad universal de los modelos.

## Lit: lectura del componente y estados completos — unidad 17

- La lectura compartida trataba `:host` como un selector descendiente: rechazaba el estado inicial correcto del panel. Además, si existía Shadow DOM pero faltaba el nodo solicitado, buscaba en el host y entregaba un aprobado a la depuración 15 sin corregir. Se corrigieron ambas rutas sin cambiar el reproductor, el audio ni los guiones.
- El reto 17 comprueba sin sesión, sesión vacía, nombre y textos de cada aviso, cambio de persona, nueva colección, vuelta al vacío y cierre de sesión. La depuración comprueba cantidades diferentes y vuelta a cero. Acepta `if` y ternarios, sin exigir sintaxis fuente. Las comprobaciones de transición crean y retiran instancias independientes.
- Se añadieron quince regresiones de navegador con Lit real, el generador de preview y el evaluador de producción. Antes del primer cambio: cinco errores entre trece casos; tras reparar la lectura inicial quedaron expuestos cuatro falsos aprobados antes enmascarados. La revisión adicional reprodujo un encabezado válido rechazado y cantidades incorrectas aceptadas por coincidencia parcial. Resultado final en Chrome: 15/15. Las once regresiones de formularios siguen 11/11 tras compartir su ejecutor de pruebas.
- Suite frontend final: 1248 aprobadas, 0 fallidas y 2 omitidas; TypeScript, build final y `git diff --check` correctos. La primera suite, simultánea con build, tuvo un fallo en la auditoría de cintas a los 5,46 s; la auditoría aislada pasó en 2,51 s y las dos suites completas posteriores sin build concurrente pasaron. No se modificaron sus límites ni sus aserciones. Persisten avisos del entorno de tests, Pyodide e imports/chunks grandes.
- Archivos de esta tanda: `specs15to20.ts`, `e2e/native/form-contracts.ts`, `regressionRunner.ts`, `lit-contracts.ts`, `lit.html`, el README de pruebas y este plan. Las entradas e2e no se importan desde la aplicación. Se usó el Vite existente sin modificar su proceso ni la pestaña del usuario; no se abrió API/PostgreSQL ni se escribió progreso de alumnado. Sin commits, publicación de catálogo o despliegues.

Pendiente: completar la auditoría de templates/bindings de las unidades 15–16 y de las unidades 18 en adelante; esta tanda solo corrige una comprobación compartida de la 15. No se ha repetido aquí el flujo autenticado de guardado de la unidad 17 ni la matriz móvil. La revisión de software no demuestra por sí sola aprendizaje humano ni calidad universal de IA.

## Fidelidad de la vista previa y bindings — unidad 16

- Las regresiones de Lit revelaron un fallo compartido del motor: al insertar JS/CSS mediante reemplazos de texto, `$$` se convertía en `$` y otras secuencias de reemplazo introducían partes del documento. Un template de moneda válido mostraba `42` en vez de `$42`. Se reprodujo con cinco pruebas que ejecutan el JS resultante y examinan CSS parseado; las cinco fallaron antes del cambio. Las inserciones ahora usan callbacks que conservan el contenido literal. La suite de `previewDocument` pasa 14/14.
- Esta corrección pertenece al generador común, no a un arreglo especial para Lit 16. No se añaden dependencias, motores o capas de interfaz. La regresión de Lit real también confirma que el template con moneda vuelve a evaluarse correctamente.
- Las doce nuevas regresiones de la unidad 16 separan el fallo del motor de ocho errores del evaluador: dos soluciones equivalentes rechazadas y seis programas incompletos aceptados. Se sustituyen las comprobaciones de sintaxis por cambios de `locked`, cliente y total, valor cero, importe decimal exacto y texto con etiquetas inofensivas que no deben interpretarse como HTML. Se comprueban propiedad y presencia del atributo disabled. Se mantienen los ejemplos/pistas de `?disabled`, pero una propiedad booleana equivalente también pasa, coherente con las [expresiones de Lit](https://lit.dev/docs/templates/expressions/).
- El enunciado explicita que los datos cambian y se representan como texto; el resultado esperado de depuración incluye ambos estados. Las pruebas crean y retiran instancias propias. No se alteran MP3, guiones, duraciones, IDs o títulos.
- Chrome final: 27/27 regresiones Lit y 11/11 formularios. Suite frontend completa: 1253 aprobadas, 0 fallidas y 2 omitidas; TypeScript, build y `git diff --check` correctos. Persisten avisos de Pyodide, imports/chunks grandes y del entorno de tests. No se abrió un backend temporal, no se publicó catálogo ni se escribió progreso. Los casos de navegador ejecutan el generador y evaluador reales, pero no sustituyen la prueba autenticada de guardado.
- Archivos de esta tanda: `src/engine/previewDocument.ts`, su test, `specs15to20.ts`, `e2e/native/lit-contracts.ts`, el README de regresiones y este plan. Se conservan los cambios previos; sin commits ni despliegues.

Pendiente concreto: templates de Lit 15, unidades 18 en adelante y comprobación autenticada de guardado de 16–17. Los resultados anteriores no acreditan por sí solos todos los cursos, backend/producción ni aprendizaje humano.

## Primera tarjeta Lit — unidad 15

- Dieciocho regresiones nuevas reproducen rechazos de aliases, variables intermedias y arrays de templates; aprobados sin precio, con 800 en vez de 80 o con el precio fuera del artículo; y falsas correcciones de `innerHTML` respaldadas por comentarios. Doce casos fallaron antes de cambiar la evaluación; la revisión adicional detectó dos aprobados al devolver un template vacío y un error técnico al comprobar un elemento nativo sin Lit.
- Se conserva la comprobación del nombre y se añade el precio exacto dentro del mismo artículo. La depuración exige el párrafo en Shadow DOM, sin aceptar el host como sustituto.
- La comprobación de template usa APIs públicas de Lit: observa el valor que devuelve `render` durante el ciclo normal en una instancia de prueba, comprueba su tipo y renderiza ese valor en un fragmento separado para verificar que produce el contenido solicitado. No lee código fuente, campos privados de Lit ni llama al método del componente fuera de su ciclo. Acepta aliases, variables y arrays; un template vacío no acredita DOM escrito imperativamente. Restaura el método observado, desconecta la parte temporal y retira su instancia.
- Se contrastaron [isTemplateResult y TemplateResultType](https://lit.dev/docs/api/custom-directives/#isTemplateResult), [render](https://lit.dev/docs/api/templates/#render) y [RootPart.setConnected](https://lit.dev/docs/api/misc/#RootPart) con documentación oficial. Los módulos auxiliares pertenecen al Lit ya utilizado por la vista previa y se importan durante la comprobación; no se añadió una dependencia al frontend ni una capa de navegación. Esta prueba necesita el CDN del runtime, como las demás vistas Lit.
- Resultado final en Chrome: 45/45 regresiones del conjunto 15–17. Incluye una solución que detectaría llamadas extra del evaluador a `render`. Un elemento nativo que no cumple esta práctica se rechaza como respuesta incorrecta, no como fallo de infraestructura. Suite frontend final: 1253 aprobadas, 0 fallidas y 2 omitidas; TypeScript, build y `git diff --check` correctos. Persisten las advertencias de Pyodide, imports/chunks grandes y del entorno de tests.
- Archivos de esta tanda: `specs15to20.ts`, `e2e/native/lit-contracts.ts`, el README de regresiones y este plan. Sin cambios nuevos al backend, motor de preview, audios, guiones, duraciones o títulos; sin catálogo publicado, commits ni despliegues. La pestaña del usuario y su servidor se conservaron.

Siguiente verificación de integración: clase y depuración de 15–17 con una cuenta ficticia, API y PostgreSQL locales, comprobando sus intentos guardados. También sigue pendiente revisar evaluadores de 18 en adelante. La revisión actual no acredita toda la plataforma ni producción.

## Integración autenticada de Lit 15–17 y diagnóstico del editor

- Se exportó el catálogo actual y se publicó únicamente en la API de loopback: versión 7, release `3b2e55b8-50c5-4b0b-812c-a29412f5303c`, hash `abf2a2edb92d3d355feefd35494539b3bdd97258c41896adcce190f398e570a4`. No se publicó en producción ni se modificó su catálogo.
- Chrome, Vite 3007, API 8087 y PostgreSQL 55438 con `alumna-a@browser.example.test`: se entró por la puerta de inicio en cada clase, se avanzó al reto, se editó y comprobó en el editor real y se volvió al mapa para abrir las tres depuraciones. La clase 15 mostró `$800` y 1/3; al corregir a `$80`, el alias y variable intermedia pasaron 3/3. Depuración 15 aceptó un array de templates, clase/depuración 16 aceptaron la propiedad booleana `.disabled`, y clase/depuración 17 aceptaron ramas `if`.
- Consulta directa a `exercise_attempts`, enlazada con `actors` y `users`: siete registros nuevos con la identidad ficticia, curso y actividad correctos. Uno es `partial` con 33 en `componentes-lit-15-app`; los otros seis son `success` con 100, uno por clase/depuración de 15–17. No se usaron respuestas escritas ficticias para acreditar reflexión: se eligió continuar sin escribir.
- Tras completar la depuración 17, continuar llevó a la clase 18 con su puerta de inicio intacta. Después de recargar, volver al mapa y reabrir la depuración 17, se ofreció recuperar el borrador y reapareció la solución guardada al elegir continuar. Esto prueba continuidad de esa actividad, no una garantía universal de sincronización.
- Se observaron dos diagnósticos estáticos sobre programas Lit que ejecutaron y aprobaron. La reproducción con `TypeScriptLanguageService`, las mismas bibliotecas TypeScript del worker y `buildWorkspaceSemanticFiles` confirmó TS2307 (faltan tipos del módulo `lit`) y el TS2345 derivado (la clase ya no se reconoce como `HTMLElement`). El analizador solo recibió `app.js`: no hay declaraciones Lit para este workspace. La auditoría existente de las 93 clases solo revisa sus estados iniciales; aquí la cinta inicial contiene un comentario, no el programa Lit posterior. No se ocultaron diagnósticos ni se añadieron tipos permisivos.
- Se cerraron la pestaña propia y los tres procesos temporales, conservando su base de datos y archivos para reproducir. El Vite original 3000 y la pestaña del usuario permanecieron intactos. Único archivo del repositorio modificado en esta tanda: este plan; sin commits ni despliegues. No se repitieron suites de código o matriz móvil porque no hubo implementación nueva en esta tanda.

Pendiente prioritario: proporcionar al editor tipos fieles al runtime Lit publicado y comprobar soluciones válidas y errores reales, sin silenciar el análisis. Después continúa la revisión de evaluadores desde 18. La integración autenticada de 15–17 queda verificada en el entorno local descrito, no en producción.

## Tipos oficiales de Lit en el editor

- Cinco pruebas reprodujeron los falsos diagnósticos antes de la corrección. La nueva integración usa el mismo módulo virtual del worker y `buildWorkspaceSemanticFiles` con soluciones reales posteriores al estado inicial: alias de `html`, variable de template, propiedad booleana y ramas con arrays. Conserva errores por miembros y exportaciones inexistentes. Tres comprobaciones adicionales cubren subrutas de directivas, Task/Context y ausencia de globals o módulos inventados.
- `vite.config.ts` incorpora únicamente declaraciones `.d.ts` y metadatos de resolución de los paquetes publicados. `lit@3.3.3`, `@lit/task@1.0.3` y `@lit/context@1.1.6` están fijados como dependencias de desarrollo y registrados en el lockfile canónico de pnpm, las mismas versiones raíz del import map. El servicio resuelve dependencias cuando se importan, sin registrarlas como raíces globales. No se cambiaron el runtime CDN, los mensajes del analizador ni las declaraciones propias de Cells. Referencia: [tipos oficiales de Lit](https://lit.dev/docs/tools/development/).
- Resultado: suite completa 1261 aprobadas, 0 fallidas y 2 omitidas; comprobación enfocada del editor y Cells 37/37 antes de las tres pruebas adicionales; TypeScript y build correctos. El JS principal permanece en 2845.79 kB (864.01 kB gzip); las declaraciones se incluyen en el worker de TypeScript, ahora de 6803.34 kB. Persisten las advertencias existentes de Pyodide, imports/chunks y del entorno de tests.
- Chrome local, cuenta ficticia y API/PostgreSQL de la integración anterior: la depuración 17 recuperó su solución con «Sin errores»; añadir `metodoInexistente()` cambió a «1 error · 0 advertencias»; retirarlo recuperó «Sin errores» y pasó 2/2. Mapa → clase 15 → puerta de inicio → inicio explícito → pausa → reto → edición con alias/variable → tarjeta Teclado/$80 y 3/3, sin falsos errores. Captura inspeccionada en viewport de escritorio. No se certificó móvil ni todas las soluciones de todas las unidades.
- El navegador integrado falló al conectar con su puente nativo; se usó Chrome. La primera carga de Vite mostró un conflicto de caché compartida entre servidores, resuelto usando una configuración temporal con caché aislada para 3007, sin modificar la configuración de producción para ocultarlo. La consola conservó mensajes de canal asíncrono de extensión; no se presenta como una consola completamente limpia.
- Archivos de implementación: `vite.config.ts`, `src/editor/typeScriptLanguageService.ts`, `package.json`, `pnpm-lock.yaml`; pruebas en `src/editor/litWorkspace.integration.test.ts`. Sin cambios al backend, audios, guiones o evaluadores en esta tanda. Sin commit, publicación de catálogo ni despliegue. Los lockfiles heredados de npm/bun no son la fuente del CI y no se regeneraron.

El defecto de tipos de Lit reproducido queda corregido y verificado localmente. Continúa pendiente la revisión de evaluadores desde la unidad 18 y la aceptación integral de la plataforma; estos resultados no acreditan producción ni aprendizaje humano.

## Reactividad observable — unidad 18

- Diecinueve casos nuevos de navegador cubren clase y depuración. Diez reprodujeron inicialmente rechazos de getters/herencia o aprobados de vistas fijas, defaults ausentes, accessors ocultos e incrementos incorrectos. La revisión posterior reprodujo dos aprobados parciales indebidos al cambiar ambas propiedades a la vez y otro aprobado con `online=true` sin atributo.
- Se reemplazaron las comprobaciones de texto fuente por cuatro evaluaciones de comportamiento, conservando los IDs y el número de comprobaciones de cada actividad. Se usan instancias separadas con limpieza y `updateComplete`; el evaluador no llama `requestUpdate` ni `render` para suplir la reactividad del programa.
- La ficha convierte atributos, interpreta su presencia booleana aunque el valor sea `false`, responde al cambio de nombre y a retirar/restaurar `online`. La prueba de propiedades alterna cambios independientes para que una propiedad reactiva no oculte otra que no lo sea. El enunciado escrito explicita `online=false` por defecto; no se modificaron guiones, beats de voz, duración o títulos. El contador debe mostrar exactamente 1, 2, 3 y 4, y responder también a asignaciones directas de count, incluidos cero y 12; 21 ya no pasa como 2.
- Se contrastaron herencia, presencia booleana y actualización con [propiedades de Lit](https://lit.dev/docs/components/properties/) y [updateComplete](https://lit.dev/docs/components/lifecycle/). Los getters y la herencia funcionan en los programas ejecutados; no se exige una forma concreta de escribir la declaración ni una frase específica para el estado desconectado.
- Chrome local en `/e2e/native/lit.html`: 64/64 regresiones correctas, incluidas las 45 anteriores, sin fallos de infraestructura contados como rechazos. Captura y resultado visible inspeccionados. Suite final: 1261 aprobadas, 0 fallidas y 2 omitidas; TypeScript, build y diff check correctos. Persisten las advertencias de build conocidas y mensajes de canal asíncrono del navegador; no se afirma consola completamente limpia.
- Archivos de esta tanda: `src/curriculum/web-components-lit/specs15to20.ts`, `e2e/native/lit-contracts.ts`, su README y este plan. No se publicaron catálogos, no se modificó el backend y no hubo commit/despliegue. Esta prueba no escribe progreso y no sustituye la integración autenticada de la unidad 18. Se usó un Vite temporal con caché aislada; la pestaña y servidor originales del usuario se conservaron.

Siguiente alcance pendiente: evaluadores de estado interno e inmutabilidad (19–20), integración autenticada de las actividades nuevas y aceptación integral de la plataforma.

## Estado interno, inmutabilidad e integración de 18–20

- Se añadieron 39 casos de navegador: 15 de estado interno, 18 de inmutabilidad y seis alternativas de renderizado en el host para 18–20. Se reprodujeron once fallos de evaluación de 19 y once de 20 antes de corregirlos. La revisión adicional reprodujo seis rechazos de soluciones válidas por exigir Shadow DOM en unidades que no enseñan encapsulación.
- La unidad 19 comprueba reservas desde capacidad 0, 2 y 5, intentos que exceden el límite, conservación de la capacidad del consumidor y cambios públicos por propiedad/atributo. El estado se verifica con la API pública `getPropertyOptions`, sus atributos observados y transiciones reales, no con una expresión regular del código. La depuración cambia `_loading` entre carga y listo, comprueba que no se refleja y rechaza conservar una entrada `loading` pública sin uso. Se aceptan opciones reordenadas o reutilizadas en una variable. Referencia: [ReactiveElement.getPropertyOptions](https://lit.dev/docs/v2/api/ReactiveElement/#getPropertyOptions).
- La unidad 20 conserva snapshots de valores y referencias previos. Añadir debe mantener lo anterior, respetar el texto y asignar IDs distintos; completar se prueba con una tarea que no es la primera, otra tarea y un ID inexistente. Se comprueban tanto el array nuevo como el objeto reemplazado y las referencias que no deben cambiar. La depuración añade dos veces, verifica filas y conserva el array anterior. Se admiten concat y push sobre una copia nueva; el enunciado escrito se centra en no mutar los datos anteriores, en lugar de prohibir una palabra. No se cambiaron guiones, voz, duración o títulos.
- Las comprobaciones de 18–20 leen el Shadow DOM abierto o el DOM del propio componente. Esto no relaja las pruebas de encapsulación explícita de la unidad 15. La matriz final pasó 103/103 con el evaluador real y el import map publicado. Suite final 1261 aprobadas, 0 fallidas, 2 omitidas; TypeScript, build y diff check correctos. Persisten las advertencias conocidas de build. Archivos: `specs15to20.ts`, `e2e/native/lit-contracts.ts`, README de regresiones y este plan. No se añadieron dependencias ni cambios al backend en esta tanda.
- Se exportó el catálogo actual y se publicó solo en la API local: release `c00a05aa-00c4-4966-8df2-285e3b489b6a`, versión 8, hash `33b9957ce8da59c5c234a22d597ab8ceafb1e54fe7fdb42f35400125945a0b58`. Chrome, Vite 3007 con caché aislada, API 8087 y PostgreSQL 55438: clase 18 con getter/orden alternativo 2/2; depuración con herencia 2/2; inventario sin límite 1/2 y corregido 2/2; carga con opciones reutilizadas/getter 3/3; tablero con concat 2/2; notas con push sobre una copia 2/2. Todos mostraron «Sin errores» en el editor. Se utilizaron puerta de inicio, edición real, Comprobar, mapa y depuraciones distintas; no se inventaron respuestas de reflexión.
- Consulta directa a `exercise_attempts` con joins a `actors` y `users`: siete registros de `alumna-a@browser.example.test` en `web-components-lit`, uno parcial con 50 en `componentes-lit-19-app` y seis `success` con 100 para clase/depuración de 18–20. Continuar desde depuración 20 llevó a clase 21 con puerta de inicio; recargar la conservó. Reabrir depuración 20 ofreció el borrador y recuperó la solución de la copia del array. Captura inspeccionada. Estos resultados prueban el entorno local y esas actividades, no producción ni todas las condiciones de sincronización.

La integración autenticada de 18–20 queda verificada. Continúa pendiente la revisión desde la unidad 21 y la aceptación integral de la plataforma. No hubo commit, despliegue ni publicación externa.

## Formularios Lit 21 y enunciados visibles sin sobrecarga

- Se añadieron 21 programas de navegador; 15 reprodujeron fallos reales antes de cambiar el evaluador. El perfil aprobaba navegación sin prevenir, nombres fijos/vacíos, errores silenciosos y emisiones duplicadas. La depuración rechazaba funciones flecha, bind y objetos handleEvent, pero aprobaba enviar durante render, omitir el envío o dejar de prevenir el segundo submit. También había rechazos por exigir Shadow DOM en esta unidad. Se contrastaron las alternativas con [eventos de Lit](https://lit.dev/docs/components/events/).
- Las cuatro comprobaciones de la unidad 21 conservan sus IDs y ahora actúan sobre formularios de instancias independientes. El perfil prueba dos nombres distintos, recorte de espacios, una emisión por submit, prevención de navegación, vacío/espacios sin emisión, mensaje y corrección posterior. La depuración comprueba que el primer render no envía y que dos submits sucesivos actúan sin navegar. No se llama al método privado para suplir el binding. Las soluciones de regresión quedan fuera del catálogo del alumno. La matriz final es 124/124 y no cuenta errores del evaluador como rechazos correctos. Estas pruebas comprueban comportamiento, no certifican por sí solas la técnica de binding usada en el código.
- El recorrido autenticado reveló que el panel compartido escondía requisitos del enunciado dentro de «Necesito ayuda». Siete pruebas nuevas reprodujeron ocultación en retos, depuración y proyectos, pérdida del contexto en razonamiento sin pistas y corrupción de nombres como `detail.name`/`app.js` al separar frases. Ahora `PracticeBrief` admite contexto visible y las cuatro vistas lo conservan fuera de las pistas. El separador solo corta tras puntuación seguida de espacio; mantiene propiedades, rutas, versiones y decimales. Una prueba adicional mantiene plegado el recordatorio teórico etiquetado «Antes de empezar», sin plegar el contrato, «Punto de partida» ni «Cómo comprobarlo».
- La inspección móvil detectó que el encabezado tapaba el cierre del reto: a 390×844, encabezado hasta y=153.6 y botón de cierre en y=78.2, no alcanzable mediante hit test. `ChallengeDrawer` ahora vive dentro del workspace y se limita a su altura, sin medir el encabezado con listeners ni añadir estado. Tras el cambio, el botón quedó en y=177.2, alcanzable. Se ejercitaron minimizar, ampliar y Comprobar; capturas de móvil y escritorio inspeccionadas en ambos temas. Los recordatorios siguen disponibles, pero no desplazan todo el contrato hacia una sección de pistas.
- La revisión real de razonamiento descubrió que las consignas Lit reutilizaban el diagrama resuelto de la lectura. Una prueba reprodujo esa filtración. `buildReasoning` ahora usa el contexto de la aplicación y pide ordenar las tarjetas sin exponer su secuencia; mantiene el walkthrough como explicación de revisión. Esto se aplica a las 45 actividades construidas por esa función. Se verificó en la interfaz de la unidad 21 que el contexto está visible, las pistas cerradas y la secuencia correcta ausente. No se alteraron los pasos, el validador de orden, títulos, guiones, audio ni duración.
- Integración local: Chrome, Vite 3007, API 8087, PostgreSQL 55438 y `alumna-a@browser.example.test`. Puerta de inicio → gesto de reproducción → reto → programa inicial 0/2 → solución con función flecha 2/2 → continuar sin inventar reflexión → mapa → depuración con función flecha 2/2 → clase 22 con puerta de inicio. El editor mostró «Sin errores» en ambas soluciones. Consulta a `exercise_attempts` unida a `actors`/`users`: clase 21 failure/0 a 01:15:09, success/100 a 01:15:52 y depuración 21 success/100 a 01:17:13 del 7 de septiembre, hora de Bogotá, todos del curso correcto. Se verificó además el contrato completo del proyecto «el Eco» de AI Engineer en su panel móvil, sin abrir ayuda ni acreditar su resolución.
- El catálogo se publicó únicamente en loopback: versión 9 para formularios y versión 10 tras separar la consigna de razonamiento, release final `b8870ee0-fa9d-4a48-84d3-93e0199e4362`, hash `c9760652de8af05b713116aeb439ae6e88448cde9540a3e5accc4d2582243659`. No hubo cambios de código al backend, publicación externa, commit o despliegue.
- Verificación final: TypeScript correcto; 1270 pruebas aprobadas, 0 fallidas, 2 omitidas con `npx vitest run --maxWorkers=2`; build correcto y diff check correcto. Una pasada anterior compartiendo recursos con build falló en la auditoría temporal de 177 cintas a los 5079 ms; la auditoría sola pasó 4/4 y la suite completa posterior pasó sin aumentar tiempos ni retirar comprobaciones. Se conservan advertencias de Pyodide, imports/chunks, localStorage experimental y abortos del entorno de pruebas. En Chrome se observaron errores de canal de extensión y abortos durante recarga/HMR; no se certifica consola completamente limpia. El navegador integrado no pudo conectar con su puente, por lo que se utilizó Chrome.
- Archivos de implementación: `specs21to26.ts`, `web-components-lit/reasoning.ts`, `ChallengeDrawer.tsx`, `ScrimPlayer.tsx`, `PracticeBrief.tsx`, `practiceCopy.ts`, `DebuggingView.tsx`, `SoloProjectView.tsx`, `ReasoningPracticeView.tsx` y `_practice-brief.scss`; regresiones junto a estas vistas, `reasoning.test.ts`, `e2e/native/lit-contracts.ts` y su README. Se conservaron los cambios anteriores del checkout. No se añadieron dependencias, descargas de modelos ni tareas en segundo plano al producto.

Se cerraron la pestaña propia y los servicios temporales 3007/8087/55438, conservando la base de datos y sus evidencias. El servidor original 3000 y las pestañas del usuario quedaron intactos; se retiró la emulación de pantalla.

Pendiente: continuar la revisión de evaluadores desde 22 y la aceptación integral de aprendizaje, IA y sincronización entre cursos. Esta tanda acredita las rutas y el entorno descritos, no producción, todos los navegadores o aprendizaje humano.

## Ciclo nativo de Lit — unidad 22

- Catorce programas nuevos de navegador. Once iniciales reprodujeron siete discrepancias antes de corregir: aprobación sin intervalo, con cambios tras retirar el elemento o sin reiniciar; rechazo por orden de callbacks o DOM del host; aprobación del reloj con `super.connectedCallback` solo en un comentario. Se agregaron el programa inicial, una vista fija y el intervalo real de un segundo. Las soluciones se mantienen fuera del catálogo del alumno.
- La clase conserva los IDs `lit22-render` y `lit22-super` y añade `lit22-reconnect`: observa 0, 1 y 2 tanto en ticks como en la vista, retirada sin cambios, notificaciones del ciclo base y reanudación tras reconectar. La consigna escrita ahora explicita un segundo por incremento. La depuración comprueba render y dos conexiones mediante el ciclo real, aceptando herencia y renderizar en el host. No se cambiaron voz, guiones, duración, títulos ni el runtime del alumno.
- Se emplea un observador registrado con la API pública `addController`/`removeController`; no modifica el prototipo, no llama manualmente `render`/`requestUpdate`/callbacks y no sustituye timers. Fuentes contrastadas: [ciclo de vida](https://lit.dev/docs/components/lifecycle/) y [controladores reactivos](https://lit.dev/docs/composition/controllers/). La observación de ticks detenidos no certifica la liberación de todos los handles o recursos internos posibles: no es una auditoría de memoria.
- La primera implementación temporal juntó limpieza y espera de reinicio. En Chrome, una espera solicitada de 1100 ms duró 1998,7 ms en un iframe oculto; dos programas defectuosos terminaron en error de evaluación, no en rechazo válido. Se separaron esas esperas en comprobaciones breves y se evitó intentar reconectar un programa que aún no había renderizado. No se amplió el límite global de tres segundos. Se retiraron todos los registros y listeners de diagnóstico temporales. `regressionRunner.ts` conserva únicamente la mejora que muestra la causa concreta de los errores.
- Chrome en `http://127.0.0.1:3007/e2e/native/lit.html`: resultado final 138/138, sin fallos de evaluación contados como rechazos. Se inspeccionaron el resultado visible y la captura de escritorio. El navegador integrado falló al conectar con su puente y se usó Chrome; sus mensajes de canal de extensión siguen presentes, por lo que no se certifica consola completamente limpia. No se probó móvil ni la integración autenticada de esta unidad en esta tanda.
- TypeScript y build correctos. Suite de frontend: 1270 aprobadas, 0 fallidas y 2 omitidas con dos workers. La última guarda de render antes de reconexión se verificó además con la matriz nativa completa. Persisten advertencias de Pyodide, imports/chunks y del entorno de tests; JS principal de build: 2864,90 kB, 867,69 kB gzip. No se añadieron dependencias ni cambios al backend.
- Archivos de esta tanda: `src/curriculum/web-components-lit/specs21to26.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/regressionRunner.ts`, su README y este plan. Se preservaron todas las modificaciones previas. Sin commit, despliegue, publicación de catálogo ni escritura de progreso.

Se cerraron la pestaña y el Vite 3007 propios. Se conservaron la pestaña y el servidor 3000 del usuario; no se arrancaron API ni PostgreSQL en esta tanda.

Pendiente: integración autenticada de la unidad 22, evaluadores desde 23 y la aceptación integral de aprendizaje, IA y sincronización entre cursos. La prueba de esta tanda no acredita producción ni el objetivo global.

## Integración de Lit 22 y reflexión compartida

- Recorrido autenticado local con la cuenta sintética `alumna-a@browser.example.test`: puerta de inicio, gesto de reproducción, pausa, reto inicial 0/3, solución con callbacks en distinto orden y render en el host 3/3, continuar sin escribir, mapa, Depura 2/2 con `super['connectedCallback']()`, continuar y puerta de la clase 23. Los intentos quedaron en PostgreSQL con curso e ítem correctos. Catálogo publicado solo en loopback, versión 11, release `a36d1077-4035-49f0-a69e-d3d9c76d2d10`. No se inventó una reflexión ni se acreditó aprendizaje humano.
- La reflexión compartida ya no sortea requisitos genéricos por hash ni exige hablar de entradas/salidas cuando la actividad consiste en organizar un modelo. Las preguntas y ejemplos se ajustan a reto, depuración, razonamiento o proyecto. El texto explica que se imagina otro caso, no que ya se modificó el programa. Se mantiene el carácter opcional y sin calificación automática, establecido anteriormente. La revisión de redacción favoreció preguntas breves y diferenciadas por actividad; no añadió pasos al recorrido.
- Reproducción móvil de Depura a 390×844: los botones de IA y ayuda en vivo tapaban el campo de reflexión. Ahora tienen una fila reservada fuera del contenido desplazable. Se verificó ausencia de solapamiento en ambos temas, con contenido hasta y=776 y botones desde y=788. La adaptación se limita a Depura de hasta 640 px; el carril de escritorio permanece.
- El panel de ayuda tenía el cierre debajo del encabezado en móvil. Se ajustó su capa y posición; el cierre tiene un objetivo mínimo de 44 px. Al abrir recibe foco; Escape dentro del panel lo cierra y devuelve el foco al lanzador, sin robarlo si el alumno ya pasó al editor. Pruebas de regresión y comprobación real del cierre en ambos temas móviles y Escape en escritorio. No se solicitó ayuda a una persona ni se descargó un modelo.
- Recarga final y «Continuar donde lo dejé» conservaron la solución y el editor volvió a «Sin errores». Las preguntas de Depura se inspeccionaron en navegador; las variantes de razonamiento y proyecto tienen cobertura de componentes, pero queda pendiente su aceptación visual completa con estas nuevas preguntas.
- Validación final: frontend 1276 aprobadas, 0 fallidas, 2 omitidas (`/tmp/platform-reflection-verified.json`); backend 212 aprobadas, 0 fallidas, 0 omitidas en una base de integración separada (`/tmp/platform-reflection-backend-final.json`). TypeScript de ambos repositorios, build del frontend y diff check correctos. Persisten advertencias de Pyodide, tamaño/imports de chunks y del entorno de pruebas. El navegador integrado volvió a fallar al conectar; se usó Chrome.
- Archivos de esta tanda: `src/learning/variation.ts` y sus pruebas, `PostSolveStudio.tsx` y sus pruebas, `SoloProjectView.integration.test.tsx`, `LearnerLiveHelpPanel.tsx` y sus pruebas, `_live-help.scss` y este plan. Sin cambios de código al backend en esta tanda. Se preservaron los cambios anteriores; sin commit, despliegue ni nuevas dependencias.

Se retiró la emulación de pantalla y se cerraron la pestaña propia y los servicios temporales 3007/8087/55438, conservando las bases y evidencias. El servidor original 3000 quedó intacto.

Pendiente: aceptación visual de las otras variantes de reflexión, evaluadores desde Lit 23 y comprobación integral de IA, aprendizaje y aislamiento/sincronización entre cursos. Los resultados anteriores son evidencia local, no una certificación de toda la plataforma.

## Recuperación del catálogo de ayuda de IA

- Se reprodujo un bloqueo al abrir la ayuda, cerrarla con la consulta de modelos pendiente y volver a abrirla. La marca `loadingModels` impedía la nueva consulta mientras la respuesta anterior ya no podía actualizar el panel. El efecto ahora depende de la apertura, disponibilidad y catálogo, no de esa marca de presentación; una respuesta de una apertura anterior sigue descartándose.
- Una segunda regresión mostró que el catálogo podía llegar después de cambiar de curso y dejar «Preparar modelo» deshabilitado: actualizar la lista invalidaba el efecto antes de su `finally`. La finalización visible se hace ahora en la propia respuesta vigente, tanto de éxito como de error.
- La comprobación visual detectó otro desajuste: al cambiar de curso se cargaba una preferencia antigua de modelo, aunque el selector mostraba la alternativa válida. La prueba con una preferencia ya ausente del catálogo reprodujo la desaparición de la información de memoria. Cargar una conversación ya no reemplaza una selección establecida desde el catálogo; el gesto de preparar usa el modelo mostrado. Un último caso reprodujo un error antiguo que seguía visible tras recuperar la consulta; se limpia al iniciar la nueva consulta.
- Cinco regresiones añadidas en `SocraticTutor.test.tsx`: reapertura con éxito o error tardíos, cambio de curso durante una consulta sin duplicarla, preferencia obsoleta tras cargar el catálogo y recuperación tras error. Prueban el componente real; el servicio de modelos es controlado para no descargar ni ejecutar WebGPU. Se mantuvieron las pruebas anteriores de cancelación de preparación, generación, edición y aislamiento por curso.
- Chrome con fixture temporal fuera del repo: consulta pendiente → cerrar → abrir → catálogo actual → resolver respuesta anterior → cambiar de JavaScript a Lit. Se conservaron contexto, selección e información de memoria; el contador registró dos consultas y cero preparaciones. Capturas de escritorio y 390×844 inspeccionadas, Escape cierra. La fixture no acredita calidad de generación.
- Aplicación autenticada local con Vite 3007, API 8087 y PostgreSQL 55438: Depura Lit 22 → catálogo real → mapa → clase 22 → puerta de inicio → reproducir por gesto → pausar → editar un comentario sintético → recargar vista previa. Se comprobó el diálogo de regreso y se conservó el borrador mediante «Guardar cambios y salir». Después: Cursos → JavaScript → Depura 1 → ayuda con nombre de actividad, curso y catálogo correctos. Recarga final verificada. No se preparó ningún modelo ni se envió una pregunta a la IA. No se acreditaron soluciones ni reflexiones en este recorrido.
- El navegador integrado no conectó con su puente y se utilizó Chrome. No hubo pantalla en blanco ni overlay del framework. Se observaron errores de canal de extensión en la consola; no se certifica consola completamente limpia. El cambio productivo está limitado a `src/components/tutor/SocraticTutor.tsx`, sus pruebas y este plan, preservando modificaciones anteriores. Sin cambios al backend, dependencias, publicación de catálogo, commit ni despliegue.

- Verificación final: TypeScript correcto, 1281 pruebas aprobadas, 0 fallidas y 2 omitidas (`/tmp/tutor-catalog-verified.json`, dos workers), build y diff check correctos. Persisten las advertencias de Pyodide, imports/chunks y localStorage/abortos del entorno de pruebas. JS principal: 2865,83 kB, 867,74 kB gzip. No se repitió toda la suite del backend porque su código no cambió; se comprobó su integración local autenticada en el recorrido descrito.

Se retiró la emulación de pantalla y se cerraron la pestaña propia y los servicios temporales 3007/8087/55438. Se conservaron la base, las pruebas temporales y el servidor original 3000.

Pendiente: validar generación real y calidad pedagógica, las demás variantes de reflexión y evaluadores, y la aceptación integral de todos los cursos. El catálogo operativo no prueba por sí solo esos requisitos.

## Generación real de Qwen3: pistas y respuestas directas

- Prueba autenticada local con Qwen3-1.7B-q4f16_1-MLC, preparado por gesto en Chrome/WebGPU, en Depura Lit 23 («updated crea un bucle de actualizaciones»). El programa inicial siguió sin editarse y obtuvo 0/3. Se usaron preguntas sintéticas, sin publicar catálogo ni desplegar cambios.
- El modo pista devolvió literalmente la pregunta del alumno. Se añadió rechazo de ese eco, normalizando mayúsculas, acentos y puntuación, con una única reparación y error controlado si vuelve a repetirse. Pruebas unitarias y de integración reprodujeron el defecto antes del cambio. El prompt ahora permite nombrar variables y métodos existentes y pide indicar dónde observar. No se inyectaron respuestas prefabricadas. La repetición real posterior mencionó `updated`, pero siguió siendo débil: no constituye aceptación pedagógica.
- En «Explícame», el mismo modelo consumió los 240 tokens en un borrador inglés iniciado por `<think>`. La causa era que solo las peticiones JSON desactivaban el razonamiento de Qwen3. Se añadió `enableThinking` opcional al protocolo y todas las fases del tutor piden respuesta directa. El servicio transmite esa opción únicamente a Qwen3. Las peticiones de laboratorio que la omiten conservan sus valores predeterminados; la opción explícita `true` sigue disponible. Referencia primaria consultada: https://github.com/mlc-ai/web-llm/pull/686.
- En respuestas directas Qwen3 se retienen los fragmentos hasta validar el resultado: se retira solo el prefijo de razonamiento vacío; un borrador no vacío se rechaza, sin publicarlo por `onChunk`. Los borradores antiguos del asistente no se reenvían como contexto. No se borraron preguntas ni se reescribió el historial guardado. Hay pruebas de prefijo dividido entre fragmentos, rechazo sin filtración, opciones omitidas/activadas, modelos distintos y las cuatro fases del tutor.
- Repetición real de la misma petición sobre `total`, `subtotal` y `updated`: se obtuvo una nueva explicación en español, sin un nuevo borrador interno y sin editar el archivo. **Aceptación de contenido fallida:** llamó infinito al ciclo pese al límite `total < 30`, atribuyó el inicio a un cambio de `subtotal` no observado y terminó cortada. La documentación primaria de Lit confirma que cambiar una propiedad reactiva en `updated` solicita otra actualización, pero no justifica ignorar el límite del programa: https://lit.dev/docs/components/lifecycle/#reactive-update-cycle. El protocolo mejoró; la explicación no se considera fiable todavía.
- Validación final: TypeScript correcto, 1292 pruebas aprobadas, 0 fallidas, 2 omitidas (`/tmp/tutor-direct-final.json`), build y diff check correctos. Persisten advertencias de Pyodide, chunks/imports y del entorno de pruebas. JS principal 2866,77 kB, 868,14 kB gzip. No se repitió la suite del backend porque no cambió su código; el recorrido usó API y PostgreSQL locales reales. No se acreditó ninguna solución ni reflexión.
- Archivos de esta tanda: `src/learning/tutor/tutorHint.ts` y sus pruebas, `tutorAgent.ts` y pruebas de integración, `tutorPrompt.ts` y sus pruebas, `src/engine/ai/localGenerationProtocol.ts`, `localGenerationService.ts` y sus pruebas, y este plan. Se conservaron las modificaciones anteriores. Sin commit, dependencias nuevas, despliegue ni cambios productivos al backend.

Pendiente: una respuesta breve, completa y ajustada al programa real; pistas que orienten de verdad; evaluación de calidad por modelos y cursos; evaluadores desde Lit 23 y aceptación integral de aprendizaje/sincronización. No se cierra el objetivo global por estos resultados parciales.

Se cerraron la pestaña propia y los servicios temporales 3007/8087/55438, liberando el modelo y conservando la base y las evidencias. El servidor original 3000 quedó intacto.

## Respuestas cortadas: conservar el motivo de finalización

- El adaptador descartaba `finish_reason`, incluido el fragmento final sin texto que emite WebLLM. Se comprobó el contrato en la dependencia instalada y una regresión roja reprodujo la pérdida. `LocalGenerationResult.finishReason` conserva ahora ese dato opcional sin cambiar la configuración ni la salida textual de los laboratorios existentes.
- En respuestas del tutor fuera del modo pista, `length` provoca una única reformulación breve con las mismas observaciones, sin volver a ejecutar herramientas. Un segundo corte da error controlado. La instrucción pide volver a redactar, no continuar el fragmento. La guía de redacción se aplicó únicamente a esa instrucción corta. Los fragmentos no se publican por el callback del tutor antes de validar la respuesta; el consumidor recibe solo el resultado final. Cancelar impide la reformulación. El presupuesto por llamada permanece acotado y no se incrementa automáticamente.
- Pruebas rojas: pérdida de la señal del proveedor, aceptación de explicación incompleta, aceptación de dos cortes y omisión del requisito de reparación en el prompt explicativo. Las regresiones verdes cubren la señal en un fragmento vacío, respuesta final, máximo de una reparación, lecturas no repetidas, ausencia de escrituras, no filtración del texto parcial y cancelación. No se considera que estas pruebas controladas acrediten precisión del modelo.
- Chrome/WebGPU, cuenta sintética ya autenticada, Qwen3 guardado, Depura Lit 23 con el mismo código sin editar y comprobación 0/3: la nueva respuesta terminó en una frase completa, pero todavía inventó cambios de `subtotal` dentro de `updated` e ignoró el tope del programa. **Precisión no aceptada.** La UI por sí sola no permite atribuir esa respuesta al camino de reparación; ese contrato se prueba en la integración controlada.
- Se intentó contrastar el mismo caso sin historial mediante la segunda sesión sintética. Chrome rechazó la navegación a la ruta local de fixture con `ERR_BLOCKED_BY_CLIENT`; no se eludió la restricción ni se borró el historial. La hipótesis de contaminación por respuestas anteriores queda sin verificar. El ejercicio también conserva evaluadores basados en fuente; su revisión sigue pendiente.
- Archivos de esta tanda: `src/engine/ai/localGenerationProtocol.ts`, `localGenerationService.ts` y sus pruebas; `src/learning/tutor/tutorAgent.ts`, sus pruebas de integración, `tutorPrompt.ts`, sus pruebas y este plan. Sin cambios al backend, dependencias, publicación, commit ni despliegue. Se preservaron los cambios anteriores.
- Se cerraron la pestaña propia y los servicios temporales, conservando PostgreSQL y las evidencias en disco. El contraste fallido con otra sesión no bloquea el trabajo restante sobre los evaluadores y la plataforma.

Pendiente: precisión y contexto del tutor, señales de truncación en las otras fases que generan planes/archivos, evaluación Lit 23 en adelante y aceptación integral de cursos, aprendizaje y sincronización. La terminación formal de una respuesta no demuestra que enseñe correctamente.

Validación final de esta tanda: TypeScript y diff check correctos, 1297 pruebas aprobadas, 0 fallidas y 2 omitidas (`/tmp/tutor-completion-final.json`). Build correcto con el código productivo final; después solo se reforzaron pruebas y documentación. JS principal 2867,34 kB, 868,36 kB gzip. Persisten advertencias de Pyodide, chunks/imports y del entorno de pruebas. No se repitió la suite del backend, cuyo código no cambió.

## Lit 23: evaluar el ciclo, no una forma de escribirlo

- Reproducción roja en Chrome con el evaluador productivo: el filtro correcto de «Pan», «Café» y «Arroz» para `a` fallaba porque se esperaban dos coincidencias en lugar de tres. También se rechazaban otro nombre del Map, métodos calculados, getters y render en el host. En cambio, un total fijo con comentarios que contenían el patrón esperado, una vista fija y derivar después del render obtenían aprobación indebida. Una regresión posterior reprodujo el rechazo de otra etiqueta junto al número de resultados.
- El reto ahora comprueba cambios independientes de `items` y `query`, búsqueda sin distinguir mayúsculas, vacío, ausencia de coincidencias, orden y conservación del array derivado cuando las entradas no cambian. Se comprueban tanto `visible` como el número mostrado, sin exigir el texto literal «Coincidencias».
- Depura ahora comprueba subtotal más 10 % con valores distintos, cero y decimales; la vista correcta desde el primer render y una única actualización para cambios agrupados. Un controlador observa las notificaciones reales de Lit: no se invocan manualmente hooks ni se reemplazan timers. Se acepta un getter puro o multiplicar por 1.1 sin exigir `willUpdate` ni una asignación concreta. La descripción distingue el comportamiento esperado del bucle inicial acotado, cuyo tope de seguridad permanece. Referencia primaria contrastada: https://lit.dev/docs/components/lifecycle/#reactive-update-cycle.
- Se añadieron 21 programas a `e2e/native/lit-contracts.ts`, incluidos comienzos incompletos, alternativas válidas y mutaciones falsas. La ejecución completa terminó en **159/159 regresiones nativas correctas**, sin errores del evaluador. Los programas iniciales de clase y Depura quedaron en 0/3. Los casos de las unidades anteriores también pasaron. Las soluciones de prueba permanecen fuera del catálogo del alumno.
- El primer recorrido de la aplicación arrancó Vite sin fijar `VITE_LEARNING_API_URL`, por lo que su proxy usó `api.devt.lat`: se observó la cinta publicada antigua con dos pruebas, no la nueva. No era un defecto del evaluador local. Se detuvo ese Vite y se repitió con la variable apuntando explícitamente a la API de pruebas 8087. No se desplegó ni publicó contenido en producción. Para próximas pruebas, fijar siempre el destino local antes de abrir la aplicación; la página nativa no consulta esa API.
- Se exportaron las 45 clases de Lit a `/tmp/lit23-catalog.HiYLUf` y se publicó únicamente en loopback: versión 12, release `41602cc1-66ee-4b4b-b553-bf982f597ed6`. Después de recargar, el reto cargado por el backend mostró las instrucciones nuevas y tres comprobaciones.
- Recorrido: puerta de inicio → gesto de reproducción → pausa → reto; recarga contra API local → conservar posición → starter 0/3 → editar con método calculado, otro nombre del Map y etiqueta «Resultados disponibles» → ejecutar vista previa (3) → comprobar 3/3 → guardar cambios y salir → Depura → starter 0/3 → getter puro → ejecutar → comprobar 3/3. Se inspeccionaron resultados en escritorio. No se rellenaron reflexiones ni se atribuyó aprendizaje humano a la ejecución sintética.
- La sesión local mostró «Prueba alumna-b». Consulta de PostgreSQL confirmó cuatro intentos de esa cuenta: fallo y éxito en `componentes-lit-23-app`, y fallo y éxito en `componentes-lit-23-debug`, todos bajo `web-components-lit`, con puntuaciones 0 y 100 correspondientes a las comprobaciones. No se probó OAuth externo ni se modificó código del backend.
- Validación: TypeScript correcto; 1297 pruebas aprobadas, 0 fallidas y 2 omitidas (`/tmp/lit23-evaluation-final.json`); build y diff check correctos. JS principal 2871,93 kB, 869,13 kB gzip. Persisten las advertencias conocidas de Pyodide, chunks/imports y del entorno de pruebas. La suite nativa y el recorrido autenticado son evidencia adicional a Vitest, no equivalentes a cobertura de todo el catálogo.
- Archivos de esta tanda: `src/curriculum/web-components-lit/specs21to26.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin nuevas dependencias, commit, despliegue ni regeneración de audio. Se preservaron las modificaciones anteriores.

Pendiente: evaluadores desde Lit 24, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de todos los cursos y de la sincronización. No se cierra el objetivo global con esta unidad.

Se cerraron las dos pestañas propias y los servicios temporales 3007/8087/55438. Se conservaron las bases y los catálogos de prueba; el servidor original 3000 quedó intacto.

## Lit 24: foco real y aviso después del render

- `DebuggingView` abre la vista antes de recargar y comprobar ejercicios de navegador: un iframe oculto no podía enfocar. Regresión roja y verde en integración; recorrido real en Chrome desde Reto hasta resultado, sin abrir previamente la vista.
- La depuración reemplaza patrones de fuente por foco real y conservación del foco externo durante dos actualizaciones. El código inicial queda en 0/1; una solución con referencia local obtiene 1/1 en el laboratorio. Los comentarios ya no suman puntos. Se exige un input existente, evitando comparar dos valores ausentes.
- El reto de clase ahora incluye un evento `results-ready` ya escrito en el starter. El alumno debe esperar al render antes de emitirlo. El evaluador observa el status dentro del listener; esperar desde el evaluador después de `search()` ocultaba el anuncio prematuro. Se comprueban anuncio único, segunda búsqueda mediante el botón y estados previos distintos. Referencia: https://lit.dev/docs/components/lifecycle/#updatecomplete, sección de eventos tras render. No se exige una forma literal de escribir `await`.
- Quince programas nativos de Lit 24 pasan, incluidos programas iniciales en cero, soluciones alternativas y mutaciones. La suite activa cada iframe de foco antes de cargar su código, sin enfocar el input ni simular eventos del alumno. Esto corrigió rechazos intermitentes de programas correctos; se conservan mensajes que identifican la etapa de foco que falla.
- Suite frontend: 1298 pruebas aprobadas y 2 omitidas; TypeScript correcto. Build correcto con advertencias de Pyodide y tamaño de chunks. La suite nativa completa terminó dos veces en 173/174: el caso de Lit 22 con intervalo de un segundo falla también sin pruebas paralelas. No se considera verde ni se atribuye la causa a carga sin evidencia.
- Pendiente inmediato: diagnosticar esa temporización, repetir la integración del reto de clase 24 desde el catálogo servido por la API local y verificar intentos persistidos. El recorrido real de esta tanda cubrió la depuración, no la publicación del nuevo reto. Después siguen los evaluadores desde Lit 25, precisión del tutor, sus fases de planes/archivos y aceptación integral. Sin despliegue, commit ni regeneración de audio.

### Contraste de temporización e integración de Lit 24

- El diagnóstico de Lit 22 observó estado y vista en cero a los 1241 ms (y después a los 1270 ms): no se había perdido un tick entre lecturas. El mismo programa y evaluador obtuvo 3/3 con el iframe visible, mientras que oculto obtuvo 2/3. Se corrigió el entorno de la suite: los casos del monitor se ejecutan visibles y activos, como en el laboratorio; no se aumentaron límites, cambiaron timers ni relajaron los resultados. La suite completa posterior terminó en **174/174 regresiones correctas**. Ya no se afirma cobertura de temporizadores en documentos ocultos.
- Se exportaron las 45 lecciones actuales a `/tmp/lit24-catalog.2s2myh` y se publicó únicamente en loopback: versión 13, release `1a19e5c3-d758-45a1-8a29-e2ec267790a2`. Se verificó antes de arrancar que Vite 3007 apunta a `http://127.0.0.1:8087`, sin recurrir a producción.
- Cuenta sintética `alumna-b@browser.example.test`, Chrome: conservar borrador anterior → mapa → clase 24 → puerta de inicio → reproducir por gesto → pausar → reto actualizado → inicio 0/2 → solución con referencia local y `then` → ejecutar → input enfocado y botón Buscar muestra dos resultados → comprobar 2/2 → continuar sin reflexión → mapa → Depura → inicio 0/1 → solución con render en el host → comprobar 1/1. Se inspeccionó la presentación del resultado en escritorio. No se atribuye este recorrido a una auditoría completa de móvil ni de sincronización de audio.
- PostgreSQL confirmó cuatro intentos de esa cuenta bajo `web-components-lit`: `componentes-lit-24-app` failure/0 a las 04:04:52 y success/100 a las 04:05:29; `componentes-lit-24-debug` failure/0 a las 04:06:32 y success/100 a las 04:06:53 del 7 de septiembre, hora de Bogotá. No se inventaron reflexiones ni se modificaron cuentas o datos de producción.
- Validación final: 1298 pruebas aprobadas, ninguna fallida y 2 omitidas (`/tmp/lit24-final.json`); TypeScript, build y diff check correctos. Persisten advertencias conocidas de Pyodide y chunks grandes. JS principal: 2874,39 kB, 869,74 kB gzip. Sin nuevas dependencias, regeneración de audio, commit ni despliegue.
- Quedan los evaluadores desde Lit 25, precisión/contexto del tutor y las fases que producen planes/archivos, además de la aceptación integral por curso. Lit 24 no cierra el objetivo global.

Se cerraron las dos pestañas propias y los servicios de pruebas 3007/8087/55438, conservando la base y el catálogo. El servidor original 3000 quedó intacto.

## Lit 25: depuración del tema CSS por comportamiento

- La reproducción nativa mostró crédito indebido para el inicio fijo en azul, un token escrito solo en comentarios, una regla sin elementos y contenido vacío. Depura ahora evalúa el color calculado del texto Marca en Shadow DOM: dos temas independientes, cambio externo sin render manual y herencia desde el contenedor sin alterar la segunda instancia. No exige una sintaxis concreta de declaración de estilos.
- Nueve programas nuevos cubren esas mutaciones, el token aplicado al span, la herencia desde `:host`, un getter de estilos, una lectura del token solo al inicio y un valor fijo que bloquea la herencia. La comprobación devuelve los colores realmente observados cuando falla, no una descripción inventada del resultado.
- Recorrido real en Chrome, mapa → Depura 25 → comprobar inicio 0/1, con ambos colores observados en azul → editar con getter y color heredado desde host → comprobar 1/1. No se abrió el tutor ni se rellenaron reflexiones. No se publicó catálogo ni se verificaron nuevos intentos en la API o PostgreSQL en esta tanda.
- Suite general repetida sin build concurrente: 1298 pruebas aprobadas, 0 fallidas y 2 omitidas (`/tmp/lit25-debug-verified.json`). La primera ejecución tuvo un fallo en la auditoría temporal del catálogo; la repetición pasó, sin atribuir su causa a la concurrencia como hecho probado. TypeScript, build y diff check correctos. JS principal: 2875,92 kB, 870,16 kB gzip; persisten advertencias de Pyodide y chunks grandes.
- Archivos de esta tanda: `src/curriculum/web-components-lit/specs21to26.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin dependencias nuevas, cambios al backend, commit, despliegue ni regeneración de audio. Se preservaron los cambios anteriores.

Pendiente: el reto de clase 25 aún conserva su evaluación anterior; después siguen Lit 26 en adelante, precisión/contexto del tutor y fases de planes/archivos, y aceptación integral de la plataforma. Las comprobaciones de color no acreditan contraste accesible ni cierran el objetivo global.

La ejecución nativa final, con los diagnósticos definitivos, terminó en **183/183 regresiones correctas** en Chrome. Se cerró solo la pestaña temporal de la suite; la aplicación original se conservó abierta.

## Lit 25: reto de clase e integración local

- Reproducción roja: el inicio recibía 1/2; getter, array y alias de css válidos fallaban por un patrón literal. Un comentario, un selector sin elementos y un borde de ancho cero obtenían 2/2. La prueba inspeccionaba fuente, no el tema aplicado.
- Se concretó el enunciado antes ambiguo: borde izquierdo del host, contenido original en Shadow DOM, color público mediante `--card-accent` y reserva azul `#2563eb`. El grosor y el diseño restante son libres. El ejemplo ya enseñaba un borde; no se exige copiar su declaración. La evaluación comprueba dos tarjetas, cambio externo sin render manual, herencia y ausencia de tema. Devuelve la etapa y el resultado observado cuando falla. No acredita contraste accesible ni exige una forma literal de `static styles`.
- Once programas nuevos: las alternativas anteriores, el inicio, borde funcional, comentario, selector sin coincidencias, ancho cero, ausencia del fallback, bloqueo de herencia y contenido ausente. Suite completa en Chrome: **194/194 regresiones correctas**, incluidos todos los casos anteriores. Referencia de estilos y arrays: https://lit.dev/docs/components/styles/.
- Catálogo exportado a `/tmp/lit25-catalog.Rj7HFg`, publicado solo en la API loopback 8087: versión 14, release `07de1933-73ee-4a71-94b1-d18bac431f66`. Vite 3007 se inició apuntando explícitamente a esa API; ninguna publicación en producción.
- Chrome con cuenta sintética: conservar borrador de Depura 24 → mapa → clase 25 → inicio por gesto → pausa → reto con instrucciones nuevas → starter 0/1 y diagnóstico de borde ausente → editar con getter, array, alias y borde de 6 px → ejecutar → comprobar 1/1. No se escribió una reflexión ni se abrió IA. Después de una recarga de desarrollo, «Continuar donde lo dejé» recuperó el borrador correcto; se inspeccionó el enunciado renderizado en escritorio, sin atribuir cobertura móvil o de sincronización completa del audio.
- PostgreSQL verificó los dos intentos de `alumna-b@browser.example.test` bajo `web-components-lit` y `componentes-lit-25-app`: failure/0 a las 04:35:00 y success/100 a las 04:35:26 del 7 de septiembre (Bogotá). Se reutilizaron servicios reales con identidades de prueba; no se probó OAuth externo.
- TypeScript y diff check correctos; 103 pruebas dirigidas y suite general de 1298 aprobadas, 0 fallidas, 2 omitidas (`/tmp/lit25-class-final.json`). Build correcto: JS principal 2877,99 kB, 870,80 kB gzip. Persisten advertencias de Pyodide y chunks grandes. El backend no cambió; no se volvió a ejecutar su suite completa.
- Archivos: `src/curriculum/web-components-lit/specs21to26.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin nuevas dependencias, commit, despliegue ni audio regenerado. Se conservaron las modificaciones anteriores y se cerraron las dos pestañas propias.

Pendiente desde Lit 26, precisión y contexto del tutor, fases de planes/archivos y aceptación integral de todos los cursos, frontend y backend. Esta tanda no cierra el objetivo global.

## Lit 26: composición comprobada en el navegador

- La reproducción roja encontró falsos positivos con comentarios, slots ausentes, piezas vacías y estilos que bloqueaban el consumidor; listas de nombres de part y sintaxis alternativa válida fallaban. El inicio de clase recibía crédito parcial. Se sustituyó la inspección de fuente por dos cambios CSS externos sobre la superficie real y comprobación de los nodos originales asignados a cada slot. La clase comprueba también cabeceras dinámicas en negrita, sin aplicar esa negrita al cuerpo o las acciones. Se aclaró el enunciado y se retiró del inicio el atributo part ya resuelto; el ejemplo se conserva.
- Veinte regresiones nuevas cubren inicio, soluciones alternativas y mutaciones, incluyendo clonación del contenido y negrita global. Chrome terminó con **214/214**. No se simula CSS ni se llama manualmente a render. Referencia para varios nombres de part: https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Selectors/::part.
- Catálogo local de 45 lecciones exportado a `/tmp/lit26-catalog.biUyhf`, importado solo en API loopback 8087: versión 15, release `03c18393-aa4b-4d13-9122-aee05b01acd9`. Recorrido Chrome: preservar borrador anterior → mapa → clase 26 → empezar por gesto → pausar → reto inicial 0/2 → editar con part dinámico de varios nombres y selector sin comillas → ejecutar → 2/2 → continuar sin reflexión → mapa → Depura 26 → inicio 0/1 → editar part dinámico y slot → 1/1, Resuelto. No se abrió IA ni se inventaron reflexiones del alumno.
- PostgreSQL confirmó cuatro intentos de la cuenta sintética `alumna-b@browser.example.test`, curso `web-components-lit`: clase `componentes-lit-26-app`, failure/0 a las 04:46:52 y success/100 a las 04:47:20; depuración, failure/0 a las 04:47:50 y success/100 a las 04:48:12 del 7 de septiembre (Bogotá). Se probaron API y persistencia reales con identidades de prueba, no OAuth externo ni producción.
- TypeScript correcto; suite general **1298 aprobadas, 0 fallidas, 2 omitidas** (`/tmp/lit26-final.json`); build correcto, JS principal 2880,86 kB, 871,64 kB gzip. Persisten advertencias de Pyodide y chunks grandes. Sin cambios al backend ni repetición de toda su suite. Sin aceptación móvil, contraste accesible o sincronización completa del audio en esta tanda.
- Archivos: `src/curriculum/web-components-lit/specs21to26.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Se preservaron modificaciones previas. Sin dependencias nuevas, commit, despliegue ni regeneración de audio; se cerraron las dos pestañas temporales.

Pendiente: Lit 27 en adelante, contexto y precisión del tutor, fases de planes/archivos y aceptación integral de los demás cursos. Esta verificación acotada no cierra el objetivo global.

## Lit 27: identidad observable en listas editables

- La reproducción roja mostró que el inicio recibía 1/3 solo por importar `repeat`; alias, destructuring y funciones de clave separadas válidas fallaban por patrones de fuente. Una implementación que sobrescribía el texto escrito en cada actualización obtenía 3/3. La evaluación anterior solo marcaba un nodo con `dataset` durante un cambio de orden.
- Depura ahora crea una instancia aislada, usa tres ids con nombres repetidos, escribe un valor distinto en cada input y sigue tanto el nodo como su valor al reordenar, insertar y quitar. Sustituye cada item por una copia para descartar claves por referencia, comprueba el estado vacío y una inserción posterior. El resultado esperado y las pistas explican el contrato sin entregar la expresión exacta.
- Once regresiones nuevas cubren inicio, solución directa, alias, función de clave, destructuring, render en el host, comentario engañoso, clave por objeto, `map`, datos fijos y sobrescritura de ediciones. Suite nativa Chrome: **225/225**. Referencia: https://lit.dev/docs/templates/lists/.
- TypeScript correcto; suite general **1298 aprobadas, 0 fallidas, 2 omitidas** (`/tmp/lit27-debug-final.json`); build correcto, JS principal 2883,56 kB y 872,46 kB gzip. Persisten las advertencias conocidas de Pyodide y chunks grandes.
- Catálogo de 45 lecciones importado solo en la API loopback como versión 16, release `14654afc-9b7e-4843-a065-fe71bef7dc67`. En Chrome se abrió Depura 27 y el inicio produjo 0/1; PostgreSQL confirmó ese intento failure/0 para `alumna-b@browser.example.test` a las 04:59:14 del 7 de septiembre (Bogotá). Chrome perdió la conexión antes de completar la edición; el navegador integrado no pudo abrir la ruta local de identidad. Por tanto, esta tanda no afirma un intento success guardado ni el estado Resuelto visible, aunque la misma solución alternativa pasa en la suite nativa real.
- Archivos: `src/curriculum/web-components-lit/specs27to32.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin dependencias nuevas, cambios de backend, commit, despliegue ni regeneración de audio.

La interrupción anterior quedó resuelta en una tanda posterior junto con el reto de clase; la evidencia definitiva está en la sección siguiente.

## Lit 27: reto de clase y cierre de la integración local

- El reto de clase dejó de buscar una forma concreta de escribir `repeat`. La nueva evaluación crea una bandeja aislada, reordena copias nuevas, inserta y quita pedidos con nombres repetidos, sigue la identidad exacta de cada fila y pulsa la acción de la segunda fila para comprobar que elimina su propio id. El estado «Sin pedidos» solo se acepta después de vaciar la lista. El enunciado admite `when` o un condicional equivalente y no entrega la expresión de clave.
- Diez mutaciones nuevas reproducen el inicio, alternativas con alias y función separada, clave por índice, eliminación equivocada, estado vacío fijo, ausencia de acciones y recreación de nodos. Sumadas a las once de Depura, la ejecución completa en navegador terminó en **235/235 regresiones correctas**. La suite usa nodos, inputs y eventos reales; no prescribe sintaxis ni mide rendimiento. Referencia contrastada: https://lit.dev/docs/templates/lists/.
- Se reexportaron las 45 lecciones actuales y se importaron solo en la API loopback como versión 17, release `e921b31d-3b3d-490b-af11-df53091df335`. Vite 3007 apuntó explícitamente a esa API; no hubo publicación en producción.
- Recorrido autenticado con la cuenta sintética `alumna-b@browser.example.test`: mapa → clase 27 → puerta de inicio y reproducción por gesto → pausa → reto actualizado → inicio 0/1 → solución con alias de `repeat` y clave estable → vista previa con Teclado y Cable, acción de la segunda fila correcta → editor sin errores → comprobar 1/1. Sin escribir reflexión, se volvió al mapa y se abrió Depura 27: solución alternativa con alias y clave por id → 1/1, «Resuelto». No se abrió IA ni se atribuye aprendizaje humano a la cuenta de prueba.
- PostgreSQL confirmó cuatro intentos del curso `web-components-lit`: `componentes-lit-27-app` failure/0 a las 05:10:26 y success/100 a las 05:14:27; `componentes-lit-27-debug` failure/0 a las 04:59:14 y success/100 a las 05:14:58 del 7 de septiembre, hora de Bogotá. Se usaron identidades y servicios locales de prueba; no se verificó OAuth externo.
- Validación final: TypeScript correcto; suite general **1298 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit27-complete-final.json`); build correcto, JS principal 2886,20 kB y 873,09 kB gzip. Persisten advertencias conocidas de Pyodide, imports y chunks grandes. El backend no cambió y no se repitió toda su suite.
- Archivos: `src/curriculum/web-components-lit/specs27to32.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Se preservaron las modificaciones previas. Sin dependencias nuevas, commit, despliegue ni audio regenerado.

Pendiente: Lit 28 en adelante, precisión y contexto del tutor, fases que producen planes/archivos, y aceptación integral de los demás cursos, móvil y sincronización. Esta unidad no cierra el objetivo global.

Se cerraron las pestañas propias y los servicios temporales 3007/8087/55438; se conservaron la base y los catálogos de prueba. El servidor original 3000 quedó intacto.

## Lit 28: estado visual y foco bajo petición

- La evaluación anterior mezclaba un regex de `classMap`, `styleMap` y `ref` con una única llamada a `focusSearch`. La reproducción roja rechazó soluciones correctas con aliases y bindings equivalentes, mientras una implementación que enfocaba desde `updated` obtenía 3/3. El inicio de Depura generaba además un error de evaluación al no tener todavía `focusSearch`, en lugar de un fallo pedagógico normal.
- La clase ahora crea una instancia aislada y observa el resultado: `selected=true/false/true`, anchos 65/35/80 %, estabilidad de los nodos, foco explícito sobre el input y conservación de un control externo durante updates. Depura comprueba que el input no tome foco al montar ni actualizar, que `focusSearch` lo enfoque y que dos updates posteriores respeten el foco movido por la persona. Los fallos devuelven la etapa y el valor observable. No se inspecciona fuente ni se llama manualmente a hooks de Lit.
- Diecisiete programas nuevos cubren inicio, solución directa, aliases, bindings equivalentes, render en el host, comentarios, valores fijos, ancho desactualizado, acción vacía, control equivocado y robo de foco durante render o `updated`. La suite completa visible en navegador terminó en **252/252 regresiones correctas**. Los casos aceptan una consulta DOM encapsulada si cumple el mismo contrato, aunque el material sigue proponiendo `ref` como la herramienta didáctica principal.
- El catálogo final de 45 lecciones se publicó solo en loopback como versión 19, release `a220e596-4a93-4366-98d4-e14ce707b373`, hash `f75e974d927a2b3a7162f1edc1a1d635080db5c10e58cfc50545e405fd6a1c06`. La versión 18 intermedia quedó superada al mejorar los diagnósticos; nada se publicó en producción.
- Recorrido autenticado con `alumna-b@browser.example.test`: mapa → clase 28 → puerta de inicio y gesto de reproducción → pausa → reto actualizado → inicio 0/1 → solución con aliases de `classMap`/`styleMap` → vista previa → 1/1. Se sustituyó una primera variante válida que dejaba un diagnóstico del editor por una variante equivalente sin errores y se volvió a comprobar 1/1. Después: mapa → Depura 28 → inicio 0/1 → mover el foco fuera de render y exponer `focusSearch` → 1/1, «Resuelto». No se abrió IA ni se escribieron reflexiones.
- PostgreSQL confirmó intentos failure/0 y success/100 de `componentes-lit-28-app`, y failure/0 y success/100 de `componentes-lit-28-debug`, bajo `web-components-lit`. El éxito final de clase ocurrió a las 05:29:03 y la comprobación final de Depura, ya sin diagnósticos del editor, a las 05:35:58 del 7 de septiembre, hora de Bogotá. Los intentos adicionales corresponden a recomprobaciones de variantes válidas y a la recarga del catálogo definitivo. Se usaron únicamente identidades y servicios locales de prueba; no OAuth externo.
- Validación definitiva: TypeScript correcto; suite general **1298 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit28-definitive.json`); build correcto, JS principal 2890,04 kB y 873,79 kB gzip. Persisten advertencias conocidas de Pyodide, imports y chunks grandes. El backend no cambió y no se repitió su suite completa.
- Archivos: `src/curriculum/web-components-lit/specs27to32.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin nuevas dependencias, commit, despliegue ni regeneración de audio; se preservaron los cambios previos.

Pendiente: Lit 29 en adelante, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 28 no cierra el objetivo global.

Se cerraron las dos pestañas propias y los servicios temporales 3007/8087/55438; se conservó el directorio de datos local y el servidor original 3000 quedó intacto.

## Lit 29: tareas estables, estados y carreras

- La evaluación anterior combinaba comprobaciones textuales de `Task` con una ejecución que no distinguía una carrera real ni exigía los cuatro estados de la clase. En Depura, una temperatura fija o una tarea recreada desde `render` podía recibir crédito parcial o completo, y una solución observable equivalente podía ser rechazada por su sintaxis.
- La clase ahora ejecuta una instancia aislada y observa Cargando → Teclado, Cargando → Sin resultados, Cargando → Error y una carrera lento-rápida cuyo resultado final debe seguir siendo Cable. Depura observa Cargando → 20 y tres actualizaciones posteriores sin reinicio. Ambos evaluadores informan la etapa y el valor recibido; no inspeccionan fuente ni invocan manualmente hooks del alumno.
- Diecisiete programas nuevos cubren starters, solución canónica, aliases, render en el host, estados fijos, consulta ignorada, vacío/error ausentes, carrera obsoleta, tarea recreada durante render y ausencia del estado pendiente. La ejecución completa visible en Chrome terminó en **269/269 regresiones correctas** con iframes, señales y temporizadores reales.
- El catálogo vigente de 45 lecciones se importó únicamente en la API loopback como versión 20, release `964f795e-585d-4fd0-a202-25eb189e662f`. Vite 3007 apuntó explícitamente a esa API; no hubo publicación en producción.
- Recorrido autenticado con `alumna-b@browser.example.test`: mapa → clase 29 → puerta de inicio y reproducción por gesto → pausa → reto nuevo → starter 0/1 → solución con una `Task` estable y estados completos → vista previa Teclado → 1/1. Sin escribir reflexión, se volvió al mapa y se abrió Depura 29: starter 0/1 con diagnóstico sobre Cargando → mover la `Task` a la instancia → editor sin errores → 1/1, «Resuelto». No se abrió IA ni se atribuye aprendizaje humano a esta cuenta sintética.
- PostgreSQL confirmó `componentes-lit-29-app` failure/0 a las 05:52:22 y success/100 a las 05:53:03; `componentes-lit-29-debug` failure/0 a las 05:54:57 y success/100 a las 05:55:16 del 7 de septiembre, hora de Bogotá. Se usaron servicios e identidades locales de prueba; no se verificó OAuth externo.
- Validación definitiva: TypeScript correcto; pruebas dirigidas **103/103**; suite general **1298 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit29-definitive.json`); build correcto, JS principal 2893,31 kB y 874,42 kB gzip. Persisten advertencias conocidas de Pyodide, imports y chunks grandes. El backend no cambió y no se repitió su suite completa.
- Archivos: `src/curriculum/web-components-lit/specs27to32.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin dependencias nuevas, commit, despliegue ni audio regenerado; se preservaron los cambios previos.

Pendiente: Lit 30 en adelante, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 29 no cierra el objetivo global.

Se cerraron las pestañas propias y los servicios temporales 3007/8087/55438; se conservó el directorio de datos local y el servidor original 3000 quedó intacto.

## Lit 30: controladores, ciclo de vida e independencia

- La evaluación anterior combinaba regex con comprobaciones parciales: rechazaba soluciones equivalentes por el orden del código, daba crédito al starter y no podía demostrar limpieza, duplicación de listeners ni aislamiento entre instancias. La reproducción roja confirmó esos falsos positivos y negativos antes de modificar el contrato.
- La clase ahora ejecuta un componente aislado, controla `navigator.onLine`, cuenta `requestUpdate` y observa En línea/Sin conexión. Exige exactamente una notificación por evento, ninguna tras desconectar el host y una sola al reconectarlo. Depura crea dos paneles: dos clicks y dos llamadas directas al segundo controlador deben producir 2/0 y luego 2/2, conservando estado independiente. No se inspecciona fuente ni se invocan manualmente hooks de Lit.
- Dieciocho programas nuevos cubren starters, solución canónica, aliases, objeto `handleEvent`, comentarios, vista fija, referencia inestable, limpieza ausente, listeners acumulados, notificación desde el botón, valor fijo y estado compartido. La ejecución completa visible en Chrome terminó en **287/287 regresiones correctas** con iframes, eventos y ciclos de vida reales.
- El catálogo vigente de 45 lecciones se importó únicamente en la API loopback como versión 21, release `82483afe-2f19-4696-9348-4baa86518611`, hash `7b724c28...`. Vite 3007 apuntó explícitamente a esa API; no hubo publicación en producción.
- Recorrido autenticado con `alumna-b@browser.example.test`: mapa → clase 30 → puerta de inicio y reproducción por gesto → pausa → reto nuevo → starter 0/1 → controlador de red con registro, limpieza y `requestUpdate` → editor sin errores, vista previa En línea → 1/1. Sin escribir reflexión, se volvió al mapa y se abrió Depura 30: starter 0/1 tras dos clicks → notificación trasladada al controlador → editor sin errores → 1/1, «Resuelto». La inspección visual a 1024×768 no mostró overlays ni recortes evidentes. No se abrió IA ni se atribuye aprendizaje humano a la cuenta sintética.
- PostgreSQL confirmó `componentes-lit-30-app` failure/0 a las 06:10:06 y success/100 a las 06:10:34; `componentes-lit-30-debug` failure/0 a las 06:11:01 y success/100 a las 06:11:21 del 7 de septiembre, hora de Bogotá. Se usaron servicios e identidades locales de prueba; no se verificó OAuth externo.
- Validación definitiva: TypeScript correcto; pruebas dirigidas **103/103**; suite general **1298 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit30-definitive.json`); build correcto, JS principal 2896,71 kB y 875,33 kB gzip. Persisten advertencias conocidas de Pyodide, imports y chunks grandes. El backend no cambió y no se repitió su suite completa.
- Archivos: `src/curriculum/web-components-lit/specs27to32.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin dependencias nuevas, commit, despliegue ni audio regenerado; se preservaron los cambios previos.

Pendiente: Lit 31 en adelante, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 30 no cierra el objetivo global.

Se cerraron las pestañas propias y los servicios temporales 3007/8087/55438; se conservó el directorio de datos local y el servidor original 3000 quedó intacto.

## Lit 31: contrato publicable, accesibilidad y export nombrado

- La evaluación anterior solo observaba el texto inicial y una emisión, por lo que aprobaba texto fijado, acciones durante el estado deshabilitado, eventos atrapados y emisiones duplicadas. Además, los regex rechazaban exports nombrados equivalentes. La matriz roja reprodujo cada falso positivo y falso negativo antes de cambiar el contrato.
- La clase ahora crea una instancia aislada, comprueba Guardar → Publicar, alterna `disabled`, observa la reflexión en el host y el botón nativo, exige cero eventos mientras está bloqueado y exactamente un `library-action` habilitado con `bubbles` y `composed`. Depura verifica que `status-chip` registre una clase con identidad pública `StatusChip`, que su instancia muestre “Listo” y que el módulo la ofrezca como export nombrado. La inspección textual queda limitada al export, que no puede sobrevivir a la transformación ejecutable del preview.
- Quince programas nuevos cubren starters, declaración exportada, export separado, clase asignada, render en Shadow DOM o en el host, texto fijo, bloqueo ausente, atributo booleano textual, evento atrapado/duplicado, clase registrada distinta y default-only. La ejecución completa visible en navegador terminó en **302/302 regresiones correctas**.
- El catálogo vigente de 45 lecciones se importó únicamente en la API loopback como versión 22, release `772ad70a-93f6-4122-90f7-a8470f6a061a`, hash `56cff550bd5ddc451c06dc1172ecb74c99f6a749ba3342f314901096ba7ed500`. Vite 3007 apuntó explícitamente a esa API; no hubo publicación en producción.
- Recorrido autenticado con `alumna-b@browser.example.test`: mapa → clase 31 → puerta de inicio y reproducción por gesto → pausa → reto nuevo → starter 0/1 → botón reactivo, booleano y evento público → editor sin errores, preview Guardar → 1/1. Sin escribir reflexión, se volvió al mapa y se abrió Depura 31: starter 0/2 → clase asignada a `StatusChip`, export nombrado separado y registro de esa identidad → editor sin errores → 2/2, «Resuelto». La captura a 1024×768 mostró editor, resultados y reflexión opcional sin overlays ni recortes evidentes; la consola no registró errores ni advertencias. No se abrió IA ni se atribuye aprendizaje humano a la cuenta sintética.
- PostgreSQL confirmó `componentes-lit-31-app` failure/0 a las 06:27:40 y success/100 a las 06:28:41; `componentes-lit-31-debug` failure/0 a las 06:29:33 y success/100 a las 06:30:00 del 7 de septiembre, hora de Bogotá. Se usaron servicios e identidades locales de prueba; no se verificó OAuth externo.
- Validación definitiva: TypeScript correcto; pruebas dirigidas **103/103**; suite general **1298 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit31-definitive.json`); build correcto, JS principal 2899,09 kB y 875,88 kB gzip. Persisten advertencias conocidas de Pyodide, imports y chunks grandes. El backend no cambió y no se repitió su suite completa.
- Archivos: `src/curriculum/web-components-lit/specs27to32.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin dependencias nuevas, commit, despliegue ni audio regenerado; se preservaron los cambios previos.

Pendiente: Lit 32 en adelante, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 31 no cierra el objetivo global.

Se cerraron las pestañas propias y los servicios temporales 3007/8087/55438; se conservó el directorio de datos local y el servidor original 3000 quedó intacto.

## Lit 32: primer corte vertical completo

- La evaluación anterior repartía tres comprobaciones débiles entre crear, cerrar y rechazar una sola entrada: el starter recibía crédito, muchas mutaciones visibles aprobaban y alternativas inmutables válidas podían fallar por su forma. La reproducción roja incluyó títulos sin recortar, prioridades fuera de rango, ids repetidos, mutación del array, formulario o etiquetas ausentes, navegación no prevenida, filtro y vacío ignorados, cierre global, filas sin acción y variantes de Depura por identidad.
- La clase ahora recorre una instancia aislada de `issue-app`: exige formulario y controles etiquetados, estado vacío, entradas inválidas sin cambios, envío cancelado, título recortado, prioridad entera 1–3, ids únicos, arrays nuevos, preservación de elementos no elegidos, resumen, acción Cerrar de la fila correcta y filtros all/open/closed. Depura prepara dos tickets, cierra el segundo y comprueba array nuevo, objeto nuevo solo para el elegido, primer objeto conservado, vista actualizada e id desconocido sin cambios. Los fallos informan la etapa y el valor observado; no prescriben `map` ni otra sintaxis.
- Veintidós programas nuevos cubren starters, solución canónica, render en host, alternativa `findIndex`/`slice` y las mutaciones anteriores. La ejecución completa visible en navegador terminó en **324/324 regresiones correctas**. El contrato usa componentes, formularios, eventos, filas y actualizaciones reales en iframe; no acredita rendimiento ni accesibilidad completa.
- El catálogo vigente de 45 lecciones se importó únicamente en la API loopback como versión 23, release `b485b255-6589-49b5-b7d4-246bbbe9d284`, hash `d74cf4a0abfa512bb58d0a95601b96fd9224fe42748b29c495fbb3113889f215`. Vite 3007 apuntó explícitamente a esa API; no hubo publicación en producción.
- Recorrido autenticado con `alumna-b@browser.example.test`: mapa → clase 32 → puerta de inicio y reproducción por gesto → pausa → reto actualizado → starter 0/1 → solución completa con formulario, reglas, copias, lista, cierre y filtro → editor sin errores → 1/1. Sin escribir reflexión, se volvió al mapa y se abrió Depura 32: starter 0/1 → alternativa inmutable con `findIndex` y `slice` → editor sin errores → 1/1, «Resuelto». La inspección visual a 1024×768 mostró código, diagnóstico y reflexión opcional sin overlays o recortes evidentes. No se abrió IA ni se atribuye aprendizaje humano a la cuenta sintética.
- PostgreSQL confirmó `componentes-lit-32-app` failure/0 a las 06:49:39 y success/100 a las 06:50:30; `componentes-lit-32-debug` failure/0 a las 06:51:00 y success/100 a las 06:51:37 del 7 de septiembre, hora de Bogotá. Se usaron servicios e identidades locales de prueba; no se verificó OAuth externo.
- Validación definitiva: TypeScript correcto; pruebas dirigidas **103/103**; suite general **1298 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit32-definitive-2.json`); build correcto, JS principal 2903,78 kB y 877,04 kB gzip. Persisten advertencias conocidas de Pyodide, imports y chunks grandes. El backend no cambió y no se repitió su suite completa.
- Archivos de esta tanda: `src/curriculum/web-components-lit/specs27to32.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin nuevas dependencias, commit, despliegue ni audio regenerado; se preservaron los cambios previos.

Pendiente: Lit 33 en adelante, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 32 no cierra el objetivo global.

Se cerraron las pestañas propias y los servicios temporales 3007/8087/55438; se conservó el directorio de datos local y el servidor original 3000 quedó intacto.

## Lit 33: directivas por ciclo observable y alcance local

- La auditoría encontró dos causas relacionadas: el evaluador premiaba las palabras `Directive` y `directive`, por lo que el starter obtenía 1/2, y el ejemplo/pista enseñaban `part.element` para una expresión hija. La API vigente de Lit documenta `parentNode` para `ChildPart`; `element` pertenece a otros tipos de Part. Se corrigieron ejemplo y pistas para enseñar el contrato real, incluida la validación de `PartType.CHILD` en el ejemplo.
- La clase ahora crea dos `validation-form` aislados y un input externo. Observa estado inicial limpio, entrada inválida, corrección válida, error independiente en la segunda instancia y una última transición; exige mensaje exacto, `aria-invalid`, nodos estables y alcance local. Depura crea dos `live-label`, comprueba Uno → Dos → vacío, independencia con B y dos asignaciones agrupadas que terminan en Final. Los fallos devuelven estados recibidos; no se inspecciona la sintaxis ni se llama manualmente a hooks de Lit.
- Dieciocho programas nuevos cubren starters, solución con `ChildPart`, nombres alternativos, consulta local, `update` o `render`, Shadow DOM y host, además de mutaciones con aria fija/ausente, mensaje fijo/ausente/persistente, consulta global, regla incorrecta, valor inicial guardado, vacío ignorado y memoria compartida. La matriz completa visible terminó en **342/342 regresiones correctas**.
- El catálogo final de 45 lecciones se importó únicamente en la API loopback como versión 25, release `326822c6-c29f-4b65-8740-4264232db0ec`, hash `e7aeb3263eb30ad47810ee4f9af65be3a3d374099a53bf1962f98e14e31c9e98`. La versión 24 intermedia quedó superada al corregir el ejemplo. La API confirmó versión 25 y el artefacto servido de la lección contiene `part.parentNode`, no `part.element`; no hubo publicación en producción.
- Recorrido autenticado con `alumna-b@browser.example.test`: mapa → clase 33 → puerta de inicio y reproducción por gesto → pausa → reto actualizado → starter 0/1 → directiva con `PartType.CHILD`, `parentNode`, mensaje y aria sincronizados → editor sin errores y preview con formulario → 1/1. Sin escribir reflexión, se volvió al mapa y se abrió Depura 33: starter 0/1 → devolver el valor vigente → editor sin errores → 1/1, «Resuelto». La inspección visual a 1024×768 mostró editor, resultado y reflexión opcional sin overlays o recortes evidentes; la consola quedó sin errores ni advertencias. No se abrió IA ni se atribuye aprendizaje humano a la cuenta sintética.
- PostgreSQL confirmó `componentes-lit-33-app` failure/0 a las 07:06:47 y success/100 a las 07:08:07; `componentes-lit-33-debug` failure/0 a las 07:08:29 y success/100 a las 07:08:51 del 7 de septiembre, hora de Bogotá. Se usaron servicios e identidades locales de prueba; no se verificó OAuth externo.
- Validación definitiva tras corregir también el ejemplo: TypeScript correcto; pruebas dirigidas **103/103**; suite general **1298 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit33-definitive-2.json`); build correcto, JS principal 2908,33 kB y 878,02 kB gzip. Persisten advertencias conocidas de Pyodide, imports y chunks grandes. El backend no cambió y no se repitió su suite completa.
- Archivos de esta tanda: `src/curriculum/web-components-lit/specs33to40.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin nuevas dependencias, commit, despliegue ni audio regenerado; se preservaron los cambios previos.

Pendiente: Lit 34 en adelante, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 33 no cierra el objetivo global.

Se cerraron las pestañas propias y los servicios temporales 3007/8087/55438; se conservó el directorio de datos local y el servidor original 3000 quedó intacto.

## Lit 34: animación posterior al render y movimiento reducido

- La evaluación anterior comprobaba llamadas aisladas a `add` y `show`, pero no demostraba que el nodo animado ya estuviera conectado, que cada aviso conservara su contenido e identidad, que Cerrar actuara sobre la fila elegida ni que la preferencia de movimiento reducido cambiara solo el efecto. La reproducción roja confirmó falsos positivos para vistas, ids y acciones fijas, y falsos negativos para variantes observables equivalentes.
- La clase ahora crea dos avisos iguales en una instancia aislada. En la rama de movimiento reducido exige contenido, ids únicos, región `aria-live="polite"` y cero animaciones; en la rama normal exige que solo la fila recién añadida y ya conectada reciba una animación con al menos dos estados de `opacity` o `transform`. También pulsa Cerrar en la segunda fila y conserva la primera. Depura crea dos paneles: el método público y el botón visible deben mostrar y después animar el `article` de su propia instancia sin alterar la otra.
- Veinte programas nuevos cubren starters, soluciones canónica y alternativa, render en Shadow DOM o en el host, animación siempre activa/ausente/prematura, nodo equivocado, anuncio ausente, mensaje fijo, ids duplicados, cierre incorrecto, acción ausente, detalle fijo y animación diferida fuera de la promesa. La matriz completa visible en navegador terminó en **362/362 regresiones correctas**.
- El catálogo vigente de 45 lecciones se importó únicamente en la API loopback como versión 26, release `3d384aea-0b6e-4e2b-9b17-a51ab952bc16`, hash `ae07ad16c858bdce1c36eaad4b8520e3e8363bf1c005c1536ebfcd80cd550d7f`. Vite 3007 apuntó explícitamente a esa API; no hubo publicación en producción.
- Recorrido autenticado con `alumna-b@browser.example.test`: mapa → clase 34 → puerta de inicio → gesto explícito de reproducción → reto actualizado → starter 0/1 → solución con inserción inmutable, `updateComplete`, preferencia reducida y animación sobre la fila nueva → editor sin errores → 1/1. Sin escribir reflexión, se volvió al mapa y se abrió Depura 34: starter 0/1 con el fallo nulo reproducido → esperar `updateComplete` y consultar el `article` local → editor sin errores → 1/1, «Resuelto». La captura a 1024×768 mostró editor, resultado y reflexión opcional sin overlays ni recortes evidentes; la consola no registró errores ni advertencias. No se abrió IA ni se atribuye aprendizaje humano a la cuenta sintética.
- PostgreSQL confirmó `componentes-lit-34-app` failure/0 a las 07:31:53 y success/100 a las 07:32:48; `componentes-lit-34-debug` failure/0 a las 07:33:38 y success/100 a las 07:34:10 del 7 de septiembre, hora de Bogotá. Se usaron servicios e identidades locales de prueba; no se verificó OAuth externo.
- Validación definitiva: TypeScript correcto; pruebas dirigidas **103/103**; suite general **1298 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit34-definitive.json`); build correcto, JS principal 2913,87 kB y 879,30 kB gzip. Persisten advertencias conocidas de Pyodide, imports y chunks grandes. El backend no cambió y no se repitió su suite completa.
- Archivos de esta tanda: `src/curriculum/web-components-lit/specs33to40.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin nuevas dependencias, commit, despliegue ni audio regenerado; se preservaron los cambios previos.

Pendiente: Lit 35 en adelante, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 34 no cierra el objetivo global.

Se cerraron las pestañas propias y los servicios temporales 3007/8087/55438; se conservó el directorio de datos local y el servidor original 3000 quedó intacto.

## Lit 35: suscripciones con propietario y limpieza simétrica

- La evaluación anterior combinaba comprobaciones de fuente con una interacción aislada: rechazaba implementaciones observables equivalentes y aprobaba programas que devolvían una limpieza ineficaz, compartían la misma referencia mutable, notificaban solo al primer observador o recreaban listeners imposibles de retirar. La reproducción roja confirmó esos falsos negativos y positivos antes de sustituir el contrato.
- La clase ahora crea dos tableros aislados, publica métricas distintas y exige una instantánea propia por observador. Desconecta uno, verifica silencio, lo reconecta varias veces y exige una sola actualización posterior en cada tablero. Depura usa dos instancias, publica dos eventos, desconecta una y repite la reconexión para comprobar que la referencia añadida y retirada es estable y que no se acumulan notificaciones. Los diagnósticos describen la transición observada sin exigir nombres internos ni una sintaxis concreta.
- Dieciocho programas nuevos cubren starters, soluciones canónicas, aliases, `handleEvent`, render en host, cleanup ineficaz o ausente, snapshot compartido, notificación parcial, vista fija, arrow nueva por conexión, referencia distinta al retirar y tipo de evento equivocado. La matriz completa visible en navegador terminó en **380/380 regresiones correctas**.
- El catálogo vigente de 45 lecciones se importó únicamente en la API loopback como versión 27, release `6f712d57-91cf-41c2-9ac7-b24427d6bab5`, hash `7b76231616104e85aa0f05717bf5b15213e9a4c5b170788bdb6b4dee912ced11`. Vite 3007 apuntó explícitamente a esa API; no hubo publicación en producción.
- Recorrido autenticado con `alumna-b@browser.example.test`: mapa → clase 35 → puerta de inicio → reproducción por gesto explícito → pausa → reto actualizado → starter 0/1 → fuente con publicación por snapshot, suscripción, cleanup y tablero con listener estable → editor sin errores → 1/1. Sin escribir reflexión, se completó la clase, se volvió al mapa y se abrió Depura 35: starter 0/1 → listener estable y limpieza simétrica → editor sin errores → 1/1, «Resuelto». La captura a 1024×768 mostró editor, resultado y reflexión opcional sin overlays ni recortes evidentes. No se abrió IA ni se atribuye aprendizaje humano a la cuenta sintética.
- PostgreSQL confirmó `componentes-lit-35-app` failure/0 a las 07:53:54 y success/100 a las 07:54:48; `componentes-lit-35-debug` failure/0 a las 07:55:15 y success/100 a las 07:56:05 del 7 de septiembre, hora de Bogotá. Se usaron servicios e identidades locales de prueba; no se verificó OAuth externo.
- Validación definitiva: TypeScript correcto; pruebas dirigidas **103/103**; suite general **1298 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit35-definitive.json`); build correcto, JS principal 2918,96 kB y 880,19 kB gzip. Persisten advertencias conocidas de Pyodide, imports y chunks grandes. El backend no cambió y no se repitió su suite completa.
- Archivos de esta tanda: `src/curriculum/web-components-lit/specs33to40.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin nuevas dependencias, commit, despliegue ni audio regenerado; se preservaron los cambios previos.

Pendiente: Lit 36 en adelante, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 35 no cierra el objetivo global.

Se cerraron las pestañas propias y los servicios temporales 3007/8087/55438; se conservó el directorio de datos local y el servidor original 3000 quedó intacto.

## Lit 36: frontera normalizada y proveedores intercambiables

- La evaluación anterior combinaba regex de objetos y clases con un único pago feliz. Aprobaba montos inválidos enviados al proveedor, ausencia del estado pendiente, montos conservados como texto, resultados con campos crudos, vistas acopladas a `note` y excepciones sin normalizar. También producía errores en DOM ligero y rechazaba adapters válidos por función. La matriz roja reprodujo cada falso positivo y negativo antes del cambio productivo.
- La clase ahora recorre una instancia aislada desde “Sin pago”: prueba cero sin llamada externa, contrato fallido normalizado, pago diferido con `pending`, botón bloqueado y “Procesando”, conversión de `"1250"` a 1250, respuesta A sin campos crudos, excepción externa sin filtrar su detalle y sustitución por un proveedor B que solo ofrece `charge`. Depura exige el puerto `quote`, normaliza 950 centavos a 9.5 USD, cambia a 17 EUR y conserva 0 COP como costo válido. Se observa el componente real en Shadow DOM o host; no se exige clase, nombres privados ni sintaxis concreta.
- Diecinueve programas nuevos cubren starters, soluciones de clase y función, aliases, render en host, validación ausente, estado pendiente omitido, monto textual, campos crudos filtrados, vista acoplada, error propagado, divisor o moneda incorrectos y cotizaciones fijas. La matriz completa visible en navegador terminó en **399/399 regresiones correctas**.
- El catálogo vigente de 45 lecciones se importó únicamente en la API loopback como versión 28, release `f3db3dc6-06ed-4090-9cd2-0303bab1b7b1`, hash `51d09989909604b56476bef6fa0b6f5aec913f0bb7031334eeb5cbd2230e540b`. Vite 3007 apuntó explícitamente a esa API; no hubo publicación en producción.
- Recorrido autenticado con `alumna-b@browser.example.test`: mapa → clase 36 → puerta de inicio → reproducción por gesto explícito → pausa → reto actualizado → starter 0/1 → adapter con validación, normalización, error, pending y `finally` → editor sin errores → 1/1. Sin escribir reflexión, se completó la clase, se volvió al mapa y se abrió Depura 36: starter 0/1 → adapter funcional e intercambiable, normalización y render del contrato → editor sin errores → 1/1, «Resuelto». La captura a 1024×768 mostró editor, resultado y reflexión opcional sin overlays ni recortes evidentes; la consola no registró errores ni advertencias. No se abrió IA ni se atribuye aprendizaje humano a la cuenta sintética.
- PostgreSQL confirmó `componentes-lit-36-app` failure/0 a las 08:13:40 y success/100 a las 08:14:14; `componentes-lit-36-debug` failure/0 a las 08:14:33 y success/100 a las 08:15:01 del 7 de septiembre, hora de Bogotá. Se usaron servicios e identidades locales de prueba; no se verificó OAuth externo.
- Validación definitiva: TypeScript correcto; pruebas dirigidas **103/103**; suite general **1298 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit36-definitive.json`); build correcto, JS principal 2924,59 kB y 881,50 kB gzip. Persisten advertencias conocidas de Pyodide, imports y chunks grandes. El backend no cambió y no se repitió su suite completa.
- Archivos de esta tanda: `src/curriculum/web-components-lit/specs33to40.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin nuevas dependencias, commit, despliegue ni audio regenerado; se preservaron los cambios previos.

Pendiente: Lit 37 en adelante, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 36 no cierra el objetivo global.

Se cerraron las pestañas propias y los servicios temporales 3007/8087/55438; se conservó el directorio de datos local y el servidor original 3000 quedó intacto.

## Lit 37: museo remoto, modelo normalizado y fallback resiliente

- La evaluación anterior mezclaba regex de nombres con una interacción limitada a `toggleFavorite(7)`: aprobaba servicios sin señal, modelos crudos, formularios o etiquetas ausentes, estados remotos omitidos, favoritos y tarjetas fijados, y rechazaba variantes válidas por su forma. La matriz roja reprodujo esos falsos positivos y negativos antes de sustituir el contrato.
- La clase ahora recorre una instancia aislada con formulario real y envío cancelado, búsqueda recortada, `AbortSignal`, servicio y cliente reemplazables, estados Cargando/Sin obras/error normalizado, transformación exclusiva a `{id,title,artist,year,imageUrl}`, fallback sin imagen rota y favoritos que añaden y retiran exactamente el id elegido con arrays nuevos y `aria-pressed`. Depura cambia `art` entre vacío, datos completos y datos sin imagen; exige título limpio, `alt`, URL normalizada y fallback reactivos aunque exista `image.url` crudo.
- Veintiséis programas nuevos cubren starters, soluciones canónicas, aliases, render en host, señal omitida, datos crudos filtrados, formulario/etiqueta/prevención/estado ausentes, error externo filtrado, favoritos y tarjetas fijos, fallback roto y las variantes equivalentes de depuración. La matriz completa visible terminó en **425/425 regresiones correctas**.
- El catálogo vigente de 45 lecciones se importó únicamente en la API loopback como versión 29, release `bfb9c8ac-671e-4239-8dda-74ffa14d89c6`, hash `b699f1765cb7b516b1bd38e937372e212762ad457d9fb0a71d4a1916023502d2`. Vite 3007 apuntó explícitamente a esa API; no hubo publicación en producción.
- Recorrido autenticado con `alumna-b@browser.example.test`: se conservó el borrador previo de Depura 36, mapa → clase 37 → puerta de inicio → reproducción por gesto explícito → pausa → reto actualizado → starter 0/1 → solución completa de servicio, estados, formulario, modelo y favoritos → preview con Jardín y fallback → 1/1. Sin escribir reflexión se completó la clase, se volvió al mapa y se abrió Depura 37: starter 0/1 con el fallo de `image` reproducido → tarjeta reactiva sobre `title` e `imageUrl` → editor sin errores → 1/1, «Resuelto». La captura a 1024×768 mostró editor, resultado y reflexión opcional sin overlays ni recortes evidentes. La consola conserva únicamente los errores esperados producidos al reproducir el starter roto; no aparecieron errores nuevos tras la solución. No se abrió IA ni se atribuye aprendizaje humano a la cuenta sintética.
- PostgreSQL confirmó `componentes-lit-37-app` failure/0 a las 08:34:45 y success/100 a las 08:35:34; `componentes-lit-37-debug` failure/0 a las 08:36:15 y success/100 a las 08:36:38 del 7 de septiembre, hora de Bogotá. Se usaron servicios e identidades locales de prueba; no se verificó OAuth externo.
- Validación definitiva: TypeScript correcto; pruebas dirigidas **103/103**; suite general **1298 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit37-definitive.json`); build correcto, JS principal 2931,51 kB y 883,44 kB gzip. Persisten advertencias conocidas de Pyodide, imports y chunks grandes. El backend no cambió y no se repitió su suite completa.
- Archivos de esta tanda: `src/curriculum/web-components-lit/specs33to40.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin dependencias nuevas, commit, despliegue ni audio regenerado; se preservaron los cambios previos.

Pendiente: Lit 38 en adelante, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 37 no cierra el objetivo global.

Se cerraron las pestañas propias y los servicios temporales 3007/8087/55438; se conservó el directorio de datos local y el servidor original 3000 quedó intacto.

## Lit 38: clima concurrente, unidades y fallo parcial

- La evaluación anterior combinaba dos regex con un fixture único: aprobaba conversiones Fahrenheit y unidades incorrectas, contratos contaminados por la respuesta cruda, identidad perdida, resultados fijados, controles sin etiqueta y vistas sin temperatura o error. Depura también aprobaba filas filtradas, ciudades fijas y errores invisibles. La matriz roja reprodujo esos falsos positivos antes de cambiar el contrato.
- La clase ahora recorre un `weather-dashboard` aislado: valida el puerto `current` con cliente reemplazable, contrato exacto en C/F, selector etiquetado, tres solicitudes iniciadas antes de resolver ninguna, finalización fuera de orden, asociación por ciudad, filas ready/error, actualización con otro conjunto y array nuevo. Depura carga dos grupos diferentes, observa inicio concurrente, orden, éxito, fallo y vista real sin prescribir la sintaxis.
- Veintidós programas nuevos cubren starters, solución canónica, aliases, host render y mutaciones secuenciales, de conversión, unidad, campos crudos, identidad, filtrado, fixture fijo, etiqueta y ramas visuales. La matriz completa visible terminó en **447/447 regresiones correctas**.
- El catálogo vigente de 45 lecciones se importó únicamente en la API loopback como versión 30, release `731bf600-c378-4a3f-aa71-94b85445d2b8`, hash `6f6f780178e4cf952c361ee16e4fc4be2673acbbe8658377081df31eecc9345d`. Vite 3007 apuntó explícitamente a esa API; no hubo publicación en producción.
- Recorrido autenticado: se conservó primero el borrador previo de Depura 37, mapa → clase 38 → puerta de inicio → reproducción por gesto explícito → pausa → reto actualizado → starter 0/1 → solución completa de servicio, unidades, concurrencia y filas → editor sin errores → 1/1. Sin escribir reflexión se completó la clase, se volvió al mapa y se abrió Depura 38: starter 0/1 con el vaciado reproducido → variante con jobs en microtareas, `allSettled`, filas nuevas y rama de error → editor sin errores → 1/1, «Resuelto». La captura a 1280×720 mostró editor, resultado y reflexión opcional sin overlays ni recortes evidentes; la consola no registró errores ni advertencias. No se abrió IA ni se atribuye aprendizaje humano a la cuenta sintética.
- PostgreSQL confirmó `componentes-lit-38-app` failure/0 a las 08:56:57 y success/100 a las 08:57:46; `componentes-lit-38-debug` failure/0 a las 08:58:22 y success/100 a las 08:58:51 del 7 de septiembre, hora de Bogotá. Se usaron servicios e identidades locales de prueba; no se verificó OAuth externo.
- Validación definitiva: TypeScript correcto; pruebas dirigidas **103/103**; suite general **1298 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit38-definitive.json`); build correcto, JS principal 2938,34 kB y 885,59 kB gzip. Persisten advertencias conocidas de Pyodide, imports y chunks grandes. El backend no cambió y no se repitió su suite completa.
- Archivos de esta tanda: `src/curriculum/web-components-lit/specs33to40.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin dependencias nuevas, commit, despliegue ni audio regenerado; se preservaron los cambios previos.

Pendiente: Lit 39 en adelante, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 38 no cierra el objetivo global.

## Lit 39: contenido universal e hidratación con frontera explícita

- El evaluador anterior de clase no admitía siquiera una solución válida: su comprobación devolvía un botón DOM en vez de un resultado serializable. En depuración, programas con fallback fijo, sin `resize`, vista fija o limpieza incorrecta seguían obteniendo crédito parcial suficiente para que el contrato no distinguiera la causa real.
- La clase ahora recorre contenido semántico antes de actuar, cambios de `name` y `price`, identidad del botón, alternancia Guardar/Guardado y `aria-pressed`, entorno cliente ausente, rehidratación idempotente, limpieza y una llamada a `render` con la lectura de layout bloqueada. El laboratorio prueba `readWidth(undefined)`, dos entornos inyectados, `resize`, sustitución de fuente y silencio después de desconectar.
- Veinte programas nuevos cubren ambos starters, soluciones canónicas, aliases, host render y mutaciones de contenido, interacción, estado, layout, guard, listeners, fallback, reactividad y limpieza. La matriz completa visible terminó en **467/467 regresiones correctas**.
- El catálogo vigente de 45 lecciones se importó únicamente en la API loopback como versión 31, release `e4398e21-7223-45b3-9fc5-2c4004ca147c`, hash `7dd1fa5d188954b6e17e8b59f13d8de04b90ba110d2e7fce941080981b1c01c2`. Vite 3007 apuntó explícitamente a esa API; no hubo publicación en producción.
- En navegador autenticado como `alumna-b`, se conservó el borrador anterior, se abrió la clase 39 desde el mapa, se verificaron inicio explícito, pausa y salto al reto, el starter falló y la solución canónica aprobó 1/1. Después se completó la clase sin reflexión, el starter de depuración falló 0/1 y la corrección aprobó 1/1 con estado **Resuelto**. Un primer pegado conservó escapes del literal de pruebas, produjo el error sintáctico esperado y se corrigió antes del aprobado; no se atribuye ese intento al starter ni al producto.
- La base loopback registró para la clase fallo 0 a las 09:20:23, intento sintáctico fallido 0 a las 09:21:05 y éxito 100 a las 09:21:23; para depuración registró fallo 0 a las 09:22:12 y éxito 100 a las 09:22:48, hora de Bogotá del 7 de septiembre de 2026.
- Verificación final: `npm run lint`, las 103 pruebas focalizadas de flujo curricular/depuración nativa, las 1.298 pruebas ejecutadas de Vitest (2 pendientes), `npm run build` y `git diff --check`. El build conserva únicamente los avisos conocidos de Pyodide, importación dinámica/estática y tamaño de chunks.

Límite honesto: la comprobación de clase bloquea una lectura de layout durante `render` y ejercita la frontera con un entorno ausente, pero se ejecuta en un iframe de navegador. No sustituye una prueba de importación en Node, generación SSR ni hidratación con `@lit-labs/ssr`. Pendiente: Lit 40 en adelante, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 39 no cierra el objetivo global.

## Lit 40: capstone de soporte con persistencia atómica

- El evaluador anterior aceptaba diez defectos de clase: campos crudos, títulos sin recortar, prioridad fuera de rango, mutación de la colección confirmada, publicación tras error, cierre del ticket equivocado, filtro ignorado, formulario sin etiquetas, navegación no prevenida y estados invisibles. En depuración también aceptaba publicar tras fallo, títulos vacíos y un conteo fijo; soluciones válidas en el host producían errores de evaluación.
- La clase ahora usa una sola prueba vertical que controla el storage y recorre carga pendiente, normalización estricta, formulario y etiquetas reales, entradas inválidas, escritura pendiente con botón bloqueado, snapshots separados, filtros, cierre desde su botón, identidad de filas no elegidas, fallo atómico y recuperación tanto de escritura como de carga. Depuración verifica dos versiones exitosas, la referencia exacta entregada a save, estado pendiente, fallo sin mutación y recuperación.
- Veinticuatro programas nuevos cubren starters, soluciones canónicas, aliases, host render y mutaciones de servicio, validación, identidad, atomicidad, semántica, estados, filtros y persistencia. Al añadirlos delante, tres regresiones históricas agotaron su espera; mover el bloque nuevo al final conservó todos los contratos y la matriz visible terminó en **491/491 regresiones correctas**.
- El catálogo vigente de 45 lecciones se importó únicamente en la API loopback como versión 32, release `f7e951fb-d420-4f06-a61c-a2e5ec019eb0`, hash `3354cd0793d71b5412deb6f2f73b17fd89ca6da59caf38cc1209904e596a7e4b`. Vite 3007 apuntó explícitamente a esa API; no hubo publicación en producción.
- En navegador autenticado como `alumna-b`, se conservó el borrador anterior, se abrió la clase 40 desde el mapa, se verificaron inicio explícito, pausa y salto al reto, el starter falló 0/1 y la solución canónica aprobó 1/1. Después se completó la clase sin reflexión, se abrió la depuración desde el mapa, el starter falló 0/1 y la versión atómica aprobó 1/1 con estado **Resuelto**.
- La revisión visual detectó que el puerto inicial `save: async () => {}` hacía que el editor marcara una llamada correcta `save(next)` como argumento inesperado. La firma neutral ahora declara `_cards`; la solución se reintrodujo y terminó otra vez 1/1, **Resuelto**, “Sin errores” y sin errores ni avisos de consola.
- La base loopback registró para la clase fallo 0 a las 09:46:23 y éxito 100 a las 09:46:44; para depuración registró fallo 0 a las 09:47:12 y éxitos 100 a las 09:47:51 y 09:48:57, hora de Bogotá del 7 de septiembre de 2026. El segundo éxito confirma la repetición posterior al arreglo del diagnóstico.
- Verificación final: `npm run lint`, las 103 pruebas focalizadas de flujo curricular/depuración nativa, las 1.298 pruebas ejecutadas de Vitest (2 pendientes), `npm run build` y `git diff --check`. El build conserva únicamente los avisos conocidos de Pyodide, importación dinámica/estática y tamaño de chunks.

Límite honesto: el capstone demuestra el corte dentro del sandbox del curso y una API loopback; no publica un paquete npm, no despliega el centro ni prueba almacenamiento remoto real, lectores de pantalla o todos los fallos de concurrencia. Pendiente: Lit 41 en adelante, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 40 no cierra el objetivo global.

Se cerraron las pestañas propias y los servicios temporales 3007/8087/55438; se conservó el directorio de datos local y el servidor original 3000 quedó intacto.

## Lit 41: mixins con cadena heredada y diagnóstico semántico limpio

- El evaluador anterior dependía de dos expresiones regulares y una lectura de `shadowRoot`: aceptaba una medición 1024 fija y una vista «Disponible» codificada, rechazaba render en el host y las soluciones completas de clase fallaban la comprobación frágil. La matriz roja reprodujo esos falsos positivos y negativos antes de cambiar el contrato.
- La clase ahora cambia `window.innerWidth`, observa el render, desconecta el panel, exige silencio, lo reconecta, acredita `hostConnected`/`hostDisconnected` mediante un controlador real y rechaza listeners duplicados. Depura retira y reinserta la tarjeta y después cambia su estado para comprobar que conexión y vista continúan vivas. Las esperas están acotadas: cortar `super` produce un rechazo, no un timeout del evaluador.
- Diecinueve programas nuevos cubren starters, soluciones canónicas, nombres internos alternativos, host render y mutaciones de `super`, referencia del listener, medición inicial, reactividad, medida y vista. Tras corregir el punto cero del controlador, la matriz visible terminó en **510/510 regresiones correctas**.
- La revisión en el producto encontró un segundo defecto: TypeScript no infería que la clase devuelta por `(Base) => class extends Base` seguía siendo un `HTMLElement`, así que una solución 1/1 mostraba un error en `customElements.define`. Se añadió `@param {typeof LitElement}` a los mixins pedagógicos de esta unidad y una regresión semántica permanente; la solución quedó 1/1 y «Sin errores».
- El catálogo final de 45 lecciones se importó únicamente en la API loopback como versión 34, release `5c84cb77-faa5-451b-be38-d601d432b68c`, hash `71411b177e0dbbd692a5f79a526c723db7d08813b6ca8dfe3bb59af58ed1a294`. La versión 33 intermedia quedó superada al corregir el diagnóstico; no hubo publicación en producción.
- En navegador autenticado como `alumna-b`, se conservó el borrador previo de Depura 40, se abrió clase 41 desde el mapa, se verificaron puerta de inicio, gesto explícito, reproducción, pausa y salto al reto. El starter falló 0/1; la solución tipada aprobó 1/1 con editor limpio. Sin inventar reflexión se volvió al mapa; Depura 41 falló 0/1 y la corrección con `super.connectedCallback()` aprobó 1/1, «Resuelto» y «Sin errores». La captura final a 1280×720 no muestra overlays, solapamientos ni recortes evidentes; consola `error`/`warn` vacía. No se abrió IA ni se atribuye aprendizaje humano a la cuenta sintética.
- PostgreSQL registró para `componentes-lit-41-app` fallo 0 a las 10:18:06 y éxito 100 a las 10:23:50; para `componentes-lit-41-debug` fallo 0 a las 10:24:54 y éxito 100 a las 10:25:58, hora de Bogotá del 7 de septiembre de 2026.
- Verificación final: TypeScript correcto; **112/112** pruebas focalizadas de currículo, depuración y semántica Lit; suite general **1.299 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit41-final.json`); build correcto, JS principal 2958,00 kB y 890,42 kB gzip; `git diff --check` correcto. Persisten únicamente las advertencias conocidas de Pyodide, imports y chunks grandes.
- Archivos de esta tanda: `src/curriculum/web-components-lit/specs41to45.ts`, `src/editor/litWorkspace.integration.test.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin dependencias nuevas, commit, despliegue, audio ni cambios de backend; se preservó el resto del árbol sucio.

Límite honesto: el contrato acredita la cadena de Lit, listeners y reconexión dentro de un iframe real, pero no demuestra compatibilidad de mixins con bases ajenas a LitElement, varias capas simultáneas ni todos los órdenes posibles de herencia. Pendiente: Lit 42 en adelante, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 41 no cierra el objetivo global.

Se cerraron las pestañas propias y los servicios temporales 3007/8087/55438; se conservó el directorio de datos local y el servidor original 3000 quedó intacto.

## Lit 42: invariantes de grafo y rechazo realmente atómico

- El evaluador anterior tenía tres comprobaciones puntuales: aceptaba nodos ausentes, auto-conexiones, arrays mutados con `push`, escrituras transitorias durante un rechazo y un conteo fijo. En depuración, una condición codificada solo para `c→a`, duplicados y auto-ciclos seguían obteniendo crédito completo. También rechazaba una solución válida al renderizar en el host porque leía `shadowRoot` directamente. La matriz roja reprodujo esos falsos positivos y negativos antes del cambio productivo.
- La clase ahora construye un grafo aislado de cuatro nodos, recorre rutas de varias aristas, prueba nodos ausentes, auto-conexiones y duplicados, conserva referencias ante cada rechazo y exige un array nuevo al aceptar. Un `Proxy` demuestra que ni siquiera ocurre un `push`/`pop` transitorio en la rama rechazada. Después comprueba en el planificador real los mensajes Conectado/Rechazado y el conteo vigente. Depura reemplaza el grafo por una cadena distinta, rechaza su ciclo largo, acepta una extensión y vuelve a cerrar el ciclo sin prescribir DFS o BFS.
- Veinte programas nuevos cubren starters, soluciones canónicas, nombres internos alternativos, render en host y mutaciones de validación, recorrido, atomicidad, inmutabilidad y vista. La primera GREEN detectó dos errores en las fixtures —dependencia del nombre `hasPath` y mutaciones de auto-ciclo que seguían seguras—; se corrigieron para medir la API pública. La ejecución visible definitiva terminó en **530/530 regresiones correctas**.
- El recorrido del producto encontró además que el starter mostraba TS1345 porque `connect` vacío se infería como `void` y después se usaba como condición. Se añadió un `return false` inicial, que conserva el fallo pedagógico sin regalar la solución, y una regresión semántica permanente del workspace; el starter final quedó «Sin errores».
- El catálogo final de 45 lecciones se importó únicamente en la API loopback como versión 36, release `356140f2-fd24-4a25-a00e-86d8b9bc35bb`, hash `e5534f67ab07120071d9efc683952dfa3bc87da3bea4b39510b17febff67f47f`. La versión 35 intermedia quedó superada por la limpieza semántica; no hubo publicación en producción.
- En navegador autenticado como `alumna-b`, se conservó el borrador previo de Depura 41, se abrió clase 42 desde el mapa y se verificaron puerta de inicio, gesto explícito, reproducción, pausa y salto al reto. El starter falló 0/1; el grafo general aprobó 1/1 con editor limpio. Sin inventar reflexión se completó la clase y se abrió Depura 42: el starter mutante falló 0/1 y la corrección con recorrido general, validación previa y array nuevo aprobó 1/1, «Resuelto» y «Sin errores». Tras importar la versión 36 se reabrió la clase: el starter ya estaba limpio y la solución volvió a aprobar 1/1. La captura final a 1280×720 no mostró overlays, solapamientos ni recortes evidentes; consola `error`/`warn` vacía. No se abrió IA ni se atribuye aprendizaje humano a la cuenta sintética.
- PostgreSQL registró para `componentes-lit-42-app` fallo 0 a las 10:54:47 y éxitos 100 a las 10:55:20 y 10:59:49; para `componentes-lit-42-debug` fallo 0 a las 10:55:49 y éxito 100 a las 10:56:15, hora de Bogotá del 7 de septiembre de 2026. El segundo éxito de clase corresponde a la revalidación del catálogo final.
- Verificación final: TypeScript correcto; **113/113** pruebas focalizadas de currículo, depuración y semántica Lit; suite general **1.300 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit42-final-v36.json`); build correcto, JS principal 2960,85 kB y 891,11 kB gzip; `git diff --check` correcto. Persisten únicamente las advertencias conocidas de Pyodide, imports y chunks grandes.
- Archivos de esta tanda: `src/curriculum/web-components-lit/specs41to45.ts`, `src/editor/litWorkspace.integration.test.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin dependencias nuevas, commit, despliegue, audio ni cambios de backend; se preservó el resto del árbol sucio.

Límite honesto: el contrato prueba grafos pequeños dentro del sandbox y detecta mutaciones observables, pero no mide complejidad sobre grafos masivos, memoria, concurrencia entre escritores ni persistencia del modelo. Pendiente: Lit 43 en adelante, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 42 no cierra el objetivo global.

Se cerraron las pestañas propias y los servicios temporales 3007/8087/55438; se conservó el directorio de datos local y el servidor original 3000 quedó intacto.

## Lit 43: orden topológico y evaluadores desacoplados

- El evaluador anterior mezclaba comprobaciones de valores predeterminados, orden textual y presencia del nombre `Bridge`: aceptaba orden visual, tablas y salidas fijadas, grafos sin detección de ciclos, evaluadores o puertos ausentes, aristas desconocidas y snapshots compartidos. En depuración también aceptaba el resultado 5 codificado, un `Map` obsoleto, fallos silenciados y una vista fija. Una solución válida en el host era rechazada. La matriz roja reprodujo esos falsos positivos y negativos antes del cambio productivo.
- La clase ahora ejecuta un grafo desordenado de cuatro nodos, deriva el orden por dependencias, reúne cada entrada por `port`, inyecta el Bridge de evaluadores y repite con datos nuevos. Exige arrays y Maps nuevos, conserva el snapshot previo y comprueba rechazo atómico de ciclos, kinds sin implementación, aristas desconocidas y puertos ausentes. Depura recorre dependencias anidadas en órdenes distintos y solo publica el `Map` completo; un grafo imposible debe lanzar sin reemplazar el último resultado.
- Veintitrés programas nuevos cubren starters, soluciones canónicas, aliases, render en host, una cola diferida equivalente y mutaciones de orden, puertos, tabla, ciclos, fronteras, identidad y vista. Las primeras GREEN expusieron dos fixtures de arista desconocida que aún fallaban por otra barrera; se aislaron para medir exactamente esa frontera. Una ejecución intermedia terminó 550/553 por tres cargas CDN históricas de Lit 25; la repetición visible, sin cambios productivos, superó ese bloque y terminó en **553/553 regresiones correctas**.
- El catálogo vigente de 45 lecciones se importó únicamente en la API loopback como versión 37, release `dafd941f-e635-4456-9c54-ca5da5820c45`, hash `f2f060153eeb5b26433ecd942ee581c8e9e26e782ecb087f587c8aa49a8b0ed1`. Vite 3007 apuntó explícitamente a esa API; no hubo publicación en producción.
- En navegador autenticado como `alumna-b`, se preservó el borrador anterior, se abrió la clase 43 desde el mapa y se verificaron puerta de inicio, gesto explícito, reproducción, pausa y salto al reto. El starter limpio falló 0/1; una implementación general aprobó 1/1 con «Sin errores». Sin obligar a escribir una reflexión se completó la clase y se abrió Depura 43: el starter falló 0/1 y la corrección por dependencias aprobó 1/1, «Resuelto» y «Sin errores». La captura final a 1280×720 no muestra overlays, solapamientos ni recortes evidentes. No se abrió IA ni se atribuye aprendizaje humano a la cuenta sintética.
- PostgreSQL registró para `componentes-lit-43-app` fallo 0 a las 11:40:15 y éxito 100 a las 11:41:32; para `componentes-lit-43-debug` fallo 0 a las 11:42:00 y éxito 100 a las 11:42:44, hora de Bogotá del 7 de septiembre de 2026.
- Verificación final: TypeScript correcto; **113/113** pruebas focalizadas de currículo, depuración y semántica Lit; suite general **1.300 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit43-final.json`); build y `git diff --check` correctos. Persisten únicamente las advertencias conocidas de Pyodide, imports y chunks grandes.
- Archivos de esta tanda: `src/curriculum/web-components-lit/specs41to45.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin dependencias nuevas, commit, despliegue, audio ni cambios de backend; se preservó el resto del árbol sucio.

Límite honesto: el contrato prueba grafos pequeños, síncronos y acíclicos dentro del sandbox; no mide complejidad sobre grafos masivos, evaluación asíncrona, tipos de puertos, memoización ni ejecución incremental. Pendiente: Lit 44 en adelante, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 43 no cierra el objetivo global.

Se cerraron las pestañas propias y los servicios temporales 3007/8087/55438; se conservó el directorio de datos local y el servidor original 3000 quedó intacto.

## Lit 44: eventos de intención y propiedad del arrastre

- El evaluador anterior dependía de comprobaciones puntuales y fuente: aceptaba eventos atrapados, detalles o vistas fijados, mutación de arrays/nodos, ausencia de captura, punteros ajenos y listeners sin limpiar. La depuración también podía aprobar coordenadas constantes o una vista fija. La matriz roja reprodujo esos falsos positivos antes del cambio productivo.
- La clase ahora observa un contrato público dinámico con `nodeId`, coordenadas y `live`, exige `bubbles`/`composed`, valida nodo y finitud, y acredita reemplazo inmutable. Un destino de puntero controlado comprueba captura del id correcto, movimiento vivo, confirmación final, liberación y retiro exacto de `pointermove`, `pointerup` y `pointercancel`. Depura exige varias posiciones reales a través de Shadow DOM.
- Veinticinco programas cubren starters, soluciones generales, aliases, render en host y mutaciones de frontera, identidad, inmutabilidad, captura, limpieza y vista. Dos oráculos iniciales confundían un alias requerido y sobrescribían el resultado del puntero ajeno; se corrigieron antes de la GREEN. Una ejecución visible definitiva terminó en **578/578 regresiones correctas**.
- La revisión visible encontró primero TS2339 porque `static properties` no declara por sí sola `node` al analizador JavaScript. La regresión semántica falló con ese mensaje; el estado tipado se trasladó al mixin que lo usa. También se acreditó el cast explícito de `currentTarget` a `HTMLElement` para `setPointerCapture`/`releasePointerCapture`. Starter y soluciones quedaron «Sin errores».
- El catálogo final de 45 lecciones se importó únicamente en la API loopback como versión 40, release `5af48e97-6bde-4349-aecb-ebb8fb2f3406`, hash `52bba28306613145842d4ad2e9db3bfc3e7afc6cdd81bb45ccd03924168937eb`. Las versiones 38 y 39 intermedias quedaron superadas por la limpieza semántica; no hubo publicación en producción.
- En navegador autenticado como `alumna-b`, se preservó el borrador anterior, se abrió la clase 44 desde el mapa y se verificaron puerta de inicio, gesto explícito, reproducción, pausa y salto al reto. El starter limpio falló 0/1; una implementación general aprobó 1/1 con «Sin errores». Sin obligar a escribir una reflexión se completó la clase y se abrió Depura 44: el starter falló 0/1 y el evento dinámico con `bubbles` y `composed` aprobó 1/1, «Resuelto» y «Sin errores». La captura final no mostró overlays ni fallos visibles. No se abrió IA ni se atribuye aprendizaje humano a la cuenta sintética.
- PostgreSQL registró para `componentes-lit-44-app` fallos 0 a las 12:14:41 y 12:18:08, y éxito 100 a las 12:18:14; para `componentes-lit-44-debug` fallo 0 a las 12:18:51 y éxito 100 a las 12:18:59, hora de Bogotá del 7 de septiembre de 2026. El primer fallo de clase pertenece a la reproducción previa del diagnóstico semántico.
- Verificación final: TypeScript correcto; **115/115** pruebas focalizadas de currículo, depuración y semántica Lit; suite general **1.302 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit44-final.json`); build correcto, JS principal 2970,02 kB y 893,54 kB gzip; `git diff --check` correcto. Persisten únicamente las advertencias conocidas de Pyodide, imports y chunks grandes.
- Archivos de esta tanda: `src/curriculum/web-components-lit/specs41to45.ts`, `src/editor/litWorkspace.integration.test.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin dependencias nuevas, commit, despliegue, audio ni cambios de backend; se preservó el resto del árbol sucio.

Límite honesto: el contrato prueba un gesto aislado y eventos sintéticos sobre un destino controlado; no mide frecuencia real de `pointermove`, transformación de coordenadas, zoom, multitouch simultáneo, historial de comandos ni accesibilidad alternativa. Pendiente: Lit 45, precisión/contexto del tutor y sus fases de planes/archivos, y aceptación integral de otros cursos, móvil, backend completo y sincronización. Lit 44 no cierra el objetivo global.

## Lit 45: capstone con historial humano y reloj efímero

- El cierre anterior combinaba cinco comprobaciones puntuales con dos expresiones regulares sobre el código. La matriz roja demostró que aprobaba intervalos duplicados, nodos desconocidos, mutaciones transitorias durante rechazos, snapshots superficiales, undo sin reevaluación, contadores fijos y ticks que borraban el historial; también rechazaba renderizar en el host. Esos falsos positivos y negativos se reprodujeron antes de cambiar el evaluador.
- La clase ahora crea una instancia aislada y reemplaza su scheduler por un reloj controlado. Comprueba un solo intervalo por conexión, limpieza de cada handle y continuidad de `hostConnected`/`hostDisconnected`; dos ticks deben volver a cero sin tocar las referencias de graph o history. Después protege las aristas con `Proxy`, rechaza ciclo, nodo ausente, auto-conexión y duplicado sin ninguna escritura, acepta una conexión en un grafo nuevo con snapshot profundo, cambia el valor a 8, observa salida 22 y exige que undo restaure 36, salida 50 e historial vacío. Depura sustituye `commit` por una función que lanza para demostrar que los ticks no delegan en acciones humanas, conserva la referencia del historial y mantiene la vista dinámica antes y después de un `commit(7)` real.
- Veintitrés programas nuevos cubren starters, soluciones generales, aliases, render en host y mutaciones de ciclo de vida, atomicidad, clonación, reevaluación, frontera comando/pulso y vista. La primera GREEN encontró que una mutación etiquetada como intervalo duplicado todavía creaba uno solo en cada conexión; se corrigió la fixture para duplicarlo de verdad. Dos ejecuciones agregadas intermedias terminaron 598/601 y 596/601 por timeouts de carga en casos históricos; todos los casos de Lit 45 pasaron en ambas. Sin cambiar producción, la repetición visible en una pestaña limpia terminó en **601/601 regresiones correctas**.
- El catálogo de 45 lecciones se importó únicamente en la API loopback como versión 41, release `e64a0158-b1fb-41a2-83c4-b2482ef27512`, hash `547e8cd5e28b34c0caff21d29a2599774f492651e43acebf3c72ebfbcb88c113`. No hubo publicación en producción.
- En navegador autenticado como `alumna-b`, se preservó el borrador de Depura 44, se abrió la clase 45 desde el mapa y se verificaron puerta de inicio, gesto explícito, reproducción, pausa y salto al reto. El starter quedó «Sin errores» y falló 0/1; una implementación general aprobó 1/1 con editor limpio. Sin obligar a escribir una reflexión se completó la clase y se abrió Depura 45: el starter falló 0/1 y la corrección mínima que separa `tick` de `commit` aprobó 1/1, «Resuelto» y «Sin errores». La captura final a 1280×720 muestra la solución y su resultado sin overlays, solapamientos ni recortes evidentes. No se abrió IA ni se atribuye aprendizaje humano a la cuenta sintética.
- PostgreSQL registró para `componentes-lit-45-app` fallo 0 a las 12:43:11 y éxito 100 a las 12:44:02; para `componentes-lit-45-debug` fallo 0 a las 12:44:40 y éxito 100 a las 12:45:35, hora de Bogotá del 7 de septiembre de 2026.
- Verificación de código: TypeScript correcto; **115/115** pruebas focalizadas de currículo, depuración y semántica Lit; suite general **1.302 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/lit45-final.json`). Build correcto en 16,66 s, JS principal 2974,63 kB y 894,46 kB gzip; `git diff --check` correcto. Persisten las advertencias conocidas de Pyodide, imports, chunks grandes y abortos del entorno de prueba.
- Archivos de esta tanda: `src/curriculum/web-components-lit/specs41to45.ts`, `e2e/native/lit-contracts.ts`, `e2e/native/README.md` y este plan. Sin dependencias nuevas, commit, despliegue, audio ni cambios de backend; se preservó el resto del árbol sucio.

Límite honesto: el contrato usa grafos pequeños, evaluación síncrona y un scheduler controlado; no mide deriva temporal, throttling en segundo plano, historiales persistidos, comandos concurrentes, redo, límites de memoria ni grafos masivos. Quedan la precisión/contexto del tutor y sus fases de planes/archivos, además de aceptación integral de otros cursos, móvil, backend completo y sincronización. Completar Lit 45 no cierra el objetivo global.

## Tutor: salidas truncadas en las fases de plan y archivo

- La auditoría del agente encontró que solo la explicación final observaba `finishReason: length`. Un plan podía quedar marcado como truncado y, si su prefijo ya era JSON válido, ejecutar una lista parcial de herramientas. Más grave: una generación de archivo cortada podía superar la comprobación del identificador solicitado y reemplazar el editor con código incompleto.
- Dos regresiones reprodujeron ambos efectos antes del cambio: el plan parcial ejecutó `run_checks` en lugar de pasar por reparación, y una función `fibonacci` sin cierre fue escrita porque contenía el nombre pedido. No se infirió el fallo a partir del prompt; se observó `replaceFile` y la herramienta realmente ejecutada.
- `generatePlan` ahora considera incompleto cualquier resultado con terminación `length`, aunque el texto se pueda parsear; lo envía al único intento de reparación existente y también rechaza una reparación truncada. La fase de archivo comprueba el motivo de terminación tanto en la propuesta inicial como en la regeneración por validación, antes de extraer cercas o llamar `replaceFile`. El mensaje aclara que no se modificó el archivo y propone concretar el cambio o usar un modelo más capaz.
- Las regresiones pasaron GREEN; el conjunto del tutor y generación local terminó **84/84**. Verificación final: TypeScript correcto; suite general **1.304 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/tutor-phase-truncation-final.json`); build correcto en 19,09 s, JS principal 2975,17 kB y 894,56 kB gzip; `git diff --check` correcto. Persisten las advertencias conocidas de Pyodide, imports/chunks, localStorage y abortos del entorno de prueba.
- Archivos adicionales de esta tanda: `src/learning/tutor/tutorAgent.ts`, `src/learning/tutor/tutorAgent.integration.test.ts` y este plan. Sin cambios al protocolo del backend, dependencias, commit ni despliegue.

Límite honesto: estas pruebas acreditan que una señal explícita de truncación no produce efectos parciales; no evalúan aún la exactitud factual de una explicación completa ni la calidad de una edición completa. Siguen pendientes el uso seguro de la estrategia del plan, la precisión y el contexto real del tutor por curso, y la aceptación integral de frontend, móvil, backend y sincronización.

### Planificador local sin estrategia descartada

- La siguiente revisión confirmó que `replyStrategy` era obligatorio en el JSON, consumía parte de los 220 tokens del plan y después se descartaba en `buildTutorResponseRequest`. Incorporarlo como instrucción habría convertido una salida no verificada del mismo modelo en autoridad adicional; se retiró en lugar de confiar en ella.
- El contrato estructurado ahora solicita únicamente `calls`, reduce el máximo a 180 tokens y la reparación usa el mismo esquema. El parser, el plan de recuperación y el modo pista ya no transportan una estrategia muerta; la respuesta final sigue determinada por modo, actividad, código y observaciones verificadas. Una regresión RED acreditó que el esquema anterior todavía exigía el campo.
- Las **42/42** pruebas dirigidas de prompt y agente pasaron. Verificación final: TypeScript correcto; suite general **1.305 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/tutor-planner-slim-final.json`); build correcto en 17,94 s, JS principal 2974,75 kB y 894,43 kB gzip; `git diff --check` correcto.
- En Chrome autenticado como `alumna-a`, Depura 45 conservó el código y abrió correctamente el tutor con aislamiento por curso, selector de los tres modelos, aviso local/privado y Qwen 2.5 Coder seleccionado. Al preparar el modelo, Chrome informó que detectaba WebGPU pero no pudo obtener un adaptador utilizable. No se reintentó a ciegas ni se fabricó una respuesta: este equipo no acredita todavía calidad factual con inferencia real.

Límite honesto: retirar salida inútil reduce costo y superficie de error, pero no demuestra que el modelo produzca explicaciones correctas. La aceptación de calidad queda pendiente de un adaptador WebGPU operativo o de otro entorno compatible; mientras tanto se puede seguir reforzando contexto y validaciones deterministas sin fingir inferencia.

### Evidencia multiparchivo sin duplicar el código activo

- La respuesta no destinada a pista incluía siempre el archivo activo y después volvía a insertar en «Observaciones verificadas» todo lo leído por `read_workspace`. Eso duplicaba `app.js` y sometía un módulo relacionado, como `math.js`, al límite genérico de 1.800 caracteres, sin una etiqueta fiable de archivo.
- Una regresión reprodujo primero el defecto con `app.js` importando `math.js`: el import aparecía dos veces. La ejecución de `read_workspace` ahora conserva las rutas existentes como metadatos estructurados. La fase de respuesta retira de las observaciones el bloque de código ya leído y construye una sola sección de evidencia con el archivo activo primero, rutas únicas y como máximo tres archivos dentro de un presupuesto total de 3.000 caracteres.
- La prueba exige una sola copia del import, una sola copia de la función exportada, la etiqueta explícita `Archivo real del estudiante: math.js` y ausencia del antiguo separador `--- app.js`. El conjunto dirigido de tutor, pistas y generación local terminó **83/83** y TypeScript pasó sin errores. La suite general terminó **1.306 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos (`/tmp/tutor-multifile-final.json`); build correcto en 19,80 s, JS principal 2974,82 kB y 894,47 kB gzip; `git diff --check` correcto. Persisten solo las advertencias conocidas de Pyodide, imports y chunks grandes.

Límite honesto: esta mejora acredita selección, identidad y deduplicación del contexto enviado al modelo, no la exactitud de la explicación que el modelo produzca. Los archivos cuarto y siguientes quedan fuera deliberadamente y los archivos largos se truncan; la calidad factual real sigue bloqueada en este equipo por la ausencia de un adaptador WebGPU utilizable.

## Cierre de aceptación transversal — 7 de septiembre

- La frontera de evidencia quedó explícita en código y pruebas: saltar un reto o ver una solución conserva un estado distinto de `completed`; un `score` aislado, una reflexión, un proyecto sin comprobaciones o un error del evaluador no se convierten en dominio. Solo una lista no vacía de comprobaciones ejecutadas produce `failure`, `partial` o `success`, y el panel docente muestra los demás casos como «Sin calificar».
- La suite frontend definitiva terminó **1.306 aprobadas, 0 fallidas y 2 omitidas** en 350 archivos. TypeScript, build y `git diff --check` correctos. La suite completa del backend terminó **212/212**, incluidas 27 integraciones con identidad, cursos, intentos, sincronización, idempotencia, permisos y ayuda en vivo contra PostgreSQL desechable en loopback; TypeScript, build y `git diff --check` correctos. El clúster se detuvo al terminar y solo permaneció el Vite 3000 original.
- Se volvió a inspeccionar Depura en 390×844 con el código inicial realmente inválido, diagnósticos visibles, pestañas Reto/Resultado/Salida, Comprobar, Reiniciar, «Saltar por ahora» y Ayuda IA. Los controles quedaron alcanzables y sin solapamientos tanto en tema normal como cyber; después se restauró tema, viewport y se cerró la pestaña propia. Este cierre complementa la matriz previa de 782 actividades de cinco cursos y los recorridos de escritorio/móvil, no significa que una sola persona ejecutara manualmente todo el catálogo.

Límite final: el software acredita navegación individual por curso, comprobaciones conductuales, persistencia aislada, recuperación, permisos y degradación sin IA. No puede garantizar aprendizaje humano ni certificar calidad universal de todos los modelos. En este host la última preparación real quedó impedida por WebGPU; se conserva la evidencia anterior de Qwen 3 y no se inventa una nueva inferencia. No hubo despliegue, commit, audio, descarga automática de modelos ni acceso a cuentas o bases reales.
