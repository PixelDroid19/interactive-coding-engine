---
lesson: open-cells-12
title: "12. Estados loading, empty, error y success"
status: pendiente-de-voz
---

Estados loading, empty, error y success. Una petición no produce solo datos: produce transiciones que la interfaz debe representar sin mezclar estados incompatibles. Hoy seguiremos el recorrido src/academy-state-panel.js → src/mixins/WidgetMixin.js → demo/demo.js → test/unit/academy-state-panel.test.js. Para entender Estados loading, empty, error y success, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

Empezamos por src/academy-state-panel.js. Este archivo implementa la API, el render, las dependencias y el evento del artefacto protagonista. Antes de tocarlo en Estados loading, empty, error y success, localiza su salida pública y predice quién dependerá de ella.

Vamos a completar src/academy-state-panel.js. Conectamos propiedades, traducciones, composición scoped y una salida observable sin copiar las dependencias reutilizadas. Después lo contrastaremos con src/mixins/WidgetMixin.js para verificar el contrato desde otro archivo.

Seguimos en src/mixins/WidgetMixin.js. Este archivo concentra traducción y emisión de intenciones públicas. Revisa qué recibe de src/academy-state-panel.js y qué deja preparado para demo/demo.js. src/mixins/WidgetMixin.js actúa como consumidor del cambio y no necesita otra edición en este paso.

El siguiente enlace aparece en demo/demo.js. Este archivo consume la entrada pública y conecta propiedades, idioma y eventos. Revisa qué recibe de src/mixins/WidgetMixin.js y qué deja preparado para test/unit/academy-state-panel.test.js. Solo inspeccionamos demo/demo.js; cambiarlo también ocultaría la causa del resultado.

Terminamos en test/unit/academy-state-panel.test.js. Este archivo conecta esta responsabilidad con el resto del proyecto. Compáralo con demo/demo.js: deben colaborar sin duplicar responsabilidades. En Estados loading, empty, error y success, test/unit/academy-state-panel.test.js queda intacto para comprobar el contrato desde fuera.

Ejecutamos el proyecto y buscamos una señal concreta: En cada instante existe un estado principal y su información asociada; success tiene datos, empty confirma una respuesta válida sin elementos y error permite recuperación. Si el preview contradice tu predicción sobre Estados loading, empty, error y success, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Antes del ejercicio, intenta explicar este fallo: Usar una sola bandera loading suele dejar errores viejos junto a resultados nuevos; dibuja la tabla de transiciones antes de renderizar. En Estados loading, empty, error y success, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

La lectura siguiente organiza lo que acabamos de ver sobre Estados loading, empty, error y success. Después, el proyecto de Estados loading, empty, error y success se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
