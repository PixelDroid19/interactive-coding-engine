---
lesson: open-cells-12
title: "12. Estados loading, empty, error y success"
status: pendiente-de-voz
---

Estados loading, empty, error y success. Una petición no produce solo datos: produce transiciones que la interfaz debe representar sin mezclar estados incompatibles.

Abrimos src/academy-learning-card.js. Este archivo implementa propiedades, render, dependencias scoped y eventos del componente. Observa su entrada y busca qué otro archivo lo consume.

Ahora construimos src/academy-learning-card.js: los imports traen Lit, mixins y dependencias. La clase declara scopedElements y properties; render usa esas entradas y handleContinue devuelve una intención pública.

Abrimos src/mixins/WidgetMixin.js. Este archivo concentra traducción y emisión de intenciones públicas. Observa su entrada y busca qué otro archivo lo consume.

Abrimos demo/demo.js. Este archivo consume la entrada pública y conecta propiedades, idioma y eventos. Observa su entrada y busca qué otro archivo lo consume.

Abrimos test/unit/academy-learning-card.test.js. Este archivo prueba el componente desde su superficie pública. Observa su entrada y busca qué otro archivo lo consume.

Ejecutamos el proyecto después de conectar sus archivos. La evidencia que buscamos es esta: En cada instante existe un estado principal y su información asociada; success tiene datos, empty confirma una respuesta válida sin elementos y error permite recuperación.

Antes de practicar, contrasta el resultado con este error frecuente: Usar una sola bandera loading suele dejar errores viejos junto a resultados nuevos; dibuja la tabla de transiciones antes de renderizar.

La lectura siguiente abre un laboratorio de proyecto para Estados loading, empty, error y success. Allí corregirás archivos reales, construirás la vista previa y ejecutarás contratos sin recibir una solución copiada.
