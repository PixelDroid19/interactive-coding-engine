---
lesson: open-cells-69
title: "69. Ciclo de vida y actualizaciones seguras"
status: pendiente-de-voz
---

Ciclo de vida y actualizaciones seguras. Conecta los callbacks de Lit y Cells con cambios observables sin duplicar trabajo ni conservar efectos abandonados. Hoy seguiremos el recorrido src/academy-lifecycle-panel.js → src/mixins/WidgetMixin.js → demo/demo.js → test/unit/academy-lifecycle-panel.test.js. Para entender Ciclo de vida y actualizaciones seguras, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

Empezamos por src/academy-lifecycle-panel.js. Este archivo implementa la API, el render, las dependencias y el evento del artefacto protagonista. Antes de tocarlo en Ciclo de vida y actualizaciones seguras, localiza su salida pública y predice quién dependerá de ella.

Ahora sí editamos src/academy-lifecycle-panel.js. Conectamos propiedades, traducciones, composición scoped y una salida observable sin copiar las dependencias reutilizadas. Después lo contrastaremos con src/mixins/WidgetMixin.js para verificar el contrato desde otro archivo.

El siguiente enlace aparece en src/mixins/WidgetMixin.js. Este archivo concentra traducción y emisión de intenciones públicas. Revisa qué recibe de src/academy-lifecycle-panel.js y qué deja preparado para demo/demo.js. src/mixins/WidgetMixin.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Ahora pasamos a demo/demo.js. Este archivo consume la entrada pública y conecta propiedades, idioma y eventos. Revisa qué recibe de src/mixins/WidgetMixin.js y qué deja preparado para test/unit/academy-lifecycle-panel.test.js. Solo inspeccionamos demo/demo.js; cambiarlo también ocultaría la causa del resultado.

Terminamos en test/unit/academy-lifecycle-panel.test.js. Este archivo conecta esta responsabilidad con el resto del proyecto. Compáralo con demo/demo.js: deben colaborar sin duplicar responsabilidades. En Ciclo de vida y actualizaciones seguras, test/unit/academy-lifecycle-panel.test.js queda intacto para comprobar el contrato desde fuera.

Ejecutamos el proyecto y buscamos una señal concreta: academy-lifecycle-panel inicia una observación al conectarse, deriva su vista desde propiedades reactivas y la detiene al desconectarse. Una reconexión no debe duplicar listeners ni contadores. Si el preview contradice tu predicción sobre Ciclo de vida y actualizaciones seguras, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Antes del ejercicio, intenta explicar este fallo: Suscribirse en cada render multiplica efectos. Si un contador aumenta dos veces después de reconectar, revisa dónde se crea y dónde se libera la suscripción. En Ciclo de vida y actualizaciones seguras, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

La lectura siguiente organiza lo que acabamos de ver sobre Ciclo de vida y actualizaciones seguras. Después, el proyecto de Ciclo de vida y actualizaciones seguras se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
