---
lesson: open-cells-67
title: "67. Paridad entre browser y CLI"
status: pendiente-de-voz
---

Paridad entre browser y CLI. La plataforma no necesita ejecutar Node para conservar contratos de archivos, comandos y resultados compatibles. Hoy seguiremos el recorrido app/config/prod.js → package.json → app/scripts/app.js → README.md. Para entender Paridad entre browser y CLI, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

Nuestro punto de partida es app/config/prod.js. Este archivo declara una entrega reproducible sin opciones de depuración. Antes de tocarlo en Paridad entre browser y CLI, localiza su salida pública y predice quién dependerá de ella.

Aquí hacemos el cambio principal en app/config/prod.js. Producción cambia únicamente configuración de entrega y desactiva opciones que no deben llegar al artefacto final. Después lo contrastaremos con package.json para verificar el contrato desde otro archivo.

Después abrimos package.json. Este archivo declara identidad, entradas, dependencias y comandos que consumen las herramientas. Revisa qué recibe de app/config/prod.js y qué deja preparado para app/scripts/app.js. Solo inspeccionamos package.json; cambiarlo también ocultaría la causa del resultado.

Seguimos en app/scripts/app.js. Este archivo arranca una única instancia del runtime con outlet, rutas e inicio. Revisa qué recibe de package.json y qué deja preparado para README.md. En Paridad entre browser y CLI, app/scripts/app.js queda intacto para comprobar el contrato desde fuera.

La última parada es README.md. Este archivo explica cómo consumir, probar y continuar el proyecto fuera del curso. Compáralo con app/scripts/app.js: deben colaborar sin duplicar responsabilidades. README.md actúa como consumidor del cambio y no necesita otra edición en este paso.

Ya está conectado el recorrido. La comprobación importante es esta: Fixtures compartidas producen el mismo scaffold esperado y cada diferencia se clasifica como soportada o bloqueante. Si el preview contradice tu predicción sobre Paridad entre browser y CLI, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Ahora piensa en el caso que suele romper este contrato: Comparar solo nombres de archivo omite semántica; valida imports, scripts, API, demo, tests y metadata. En Paridad entre browser y CLI, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

A continuación podrás repasar el modelo con calma sobre Paridad entre browser y CLI. Después, el proyecto de Paridad entre browser y CLI se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
