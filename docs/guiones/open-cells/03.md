---
lesson: open-cells-03
title: "3. Anatomía de un proyecto Cells"
status: pendiente-de-voz
---

Anatomía de un proyecto Cells. Recorre un scaffold completo antes de editarlo para saber qué archivo responde a cada pregunta. Hoy seguiremos el recorrido package.json → index.js → academy-learning-card.js → src/academy-learning-card.js → custom-elements.json. Para entender Anatomía de un proyecto Cells, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

Empezamos por package.json. Este archivo declara identidad, entradas, dependencias y comandos que consumen las herramientas. Antes de tocarlo en Anatomía de un proyecto Cells, localiza su salida pública y predice quién dependerá de ella.

Aquí hacemos el cambio principal en package.json. Type module permite usar import y export. exports señala la entrada pública; cells.entry señala el bootstrap de una app. Los scripts nombran operaciones reales de desarrollo, pruebas, locales y build. Después lo contrastaremos con index.js para verificar el contrato desde otro archivo.

Después abrimos index.js. Este archivo expone la clase sin registrar efectos globales. Revisa qué recibe de package.json y qué deja preparado para academy-learning-card.js.

Vamos a completar index.js. Exportamos la clase desde src. Este archivo no registra el tag: así una prueba o un consumidor puede importar la clase sin provocar un efecto global. Después lo contrastaremos con academy-learning-card.js para verificar el contrato desde otro archivo.

Seguimos en academy-learning-card.js. Este archivo registra el tag público que una aplicación puede instanciar. Revisa qué recibe de index.js y qué deja preparado para src/academy-learning-card.js.

Ahora sí editamos academy-learning-card.js. Importamos el polyfill del registro scoped, importamos la clase y registramos únicamente el tag público si todavía no existe. Después lo contrastaremos con src/academy-learning-card.js para verificar el contrato desde otro archivo.

El siguiente enlace aparece en src/academy-learning-card.js. Este archivo implementa propiedades, render, dependencias scoped y eventos del componente. Revisa qué recibe de academy-learning-card.js y qué deja preparado para custom-elements.json. En Anatomía de un proyecto Cells, src/academy-learning-card.js queda intacto para comprobar el contrato desde fuera.

La última parada es custom-elements.json. Este archivo describe la API para herramientas sin ejecutar el módulo. Compáralo con src/academy-learning-card.js: deben colaborar sin duplicar responsabilidades. custom-elements.json actúa como consumidor del cambio y no necesita otra edición en este paso.

Ejecutamos el proyecto y buscamos una señal concreta: La demo debe importar e instanciar el componente como lo haría otra aplicación. Las pruebas también actúan desde fuera: asignan propiedades, esperan updateComplete, interactúan y escuchan eventos. Ambas superficies detectan contratos que una revisión del archivo fuente puede pasar por alto. Si el preview contradice tu predicción sobre Anatomía de un proyecto Cells, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Antes del ejercicio, intenta explicar este fallo: Los catálogos EN y ES deben contener las mismas claves y placeholders. Una traducción faltante no debe convertirse silenciosamente en una cadena vacía: el diagnóstico visible ayuda a corregir el catálogo antes de publicar. Un error frecuente consiste en cambiar varias fronteras a la vez; conserva una predicción y comprueba una sola causa antes de continuar. En Anatomía de un proyecto Cells, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

La lectura siguiente organiza lo que acabamos de ver sobre Anatomía de un proyecto Cells. Después, el proyecto de Anatomía de un proyecto Cells se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
