---
lesson: open-cells-17
title: "17. static get scopedElements()"
status: pendiente-de-voz
---

static get scopedElements(). scopedElements es una tabla nombre → clase, no una lista de strings ni una segunda importación. Hoy seguiremos el recorrido src/academy-price-tag.js → src/mixins/WidgetMixin.js → demo/demo.js → test/unit/academy-price-tag.test.js. Para entender static get scopedElements(), identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

La primera parada está en src/academy-price-tag.js. Este archivo implementa la API, el render, las dependencias y el evento del artefacto protagonista. Antes de tocarlo en static get scopedElements(), localiza su salida pública y predice quién dependerá de ella.

Ahora sí editamos src/academy-price-tag.js. Conectamos propiedades, traducciones, composición scoped y una salida observable sin copiar las dependencias reutilizadas. Después lo contrastaremos con src/mixins/WidgetMixin.js para verificar el contrato desde otro archivo.

El siguiente enlace aparece en src/mixins/WidgetMixin.js. Este archivo concentra traducción y emisión de intenciones públicas. Revisa qué recibe de src/academy-price-tag.js y qué deja preparado para demo/demo.js. En static get scopedElements(), src/mixins/WidgetMixin.js queda intacto para comprobar el contrato desde fuera.

Ahora pasamos a demo/demo.js. Este archivo consume la entrada pública y conecta propiedades, idioma y eventos. Revisa qué recibe de src/mixins/WidgetMixin.js y qué deja preparado para test/unit/academy-price-tag.test.js. demo/demo.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Cerramos el recorrido en test/unit/academy-price-tag.test.js. Este archivo conecta esta responsabilidad con el resto del proyecto. Compáralo con demo/demo.js: deben colaborar sin duplicar responsabilidades. Solo inspeccionamos test/unit/academy-price-tag.test.js; cambiarlo también ocultaría la causa del resultado.

Con los archivos enlazados, observa este contrato: Cada etiqueta usada por el template tiene una clase alcanzable y la clave coincide exactamente con el tag renderizado. Si el preview contradice tu predicción sobre static get scopedElements(), vuelve al último archivo editado y sigue su salida hasta el consumidor.

Haz una última predicción sobre este error frecuente: Una diferencia de un guion deja el elemento sin actualizar; compara tag, clave y export de clase como una cadena. En static get scopedElements(), propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

El siguiente paso separa explicación y práctica sobre static get scopedElements(). Después, el proyecto de static get scopedElements() se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
