---
lesson: open-cells-28
title: "28. this.emitEvent"
status: pendiente-de-voz
---

this.emitEvent. emitEvent convierte una acción interna en una intención pública nombrada desde el host. Hoy seguiremos el recorrido src/academy-learning-card.js → src/mixins/WidgetMixin.js → demo/demo.js → test/unit/academy-learning-card.test.js. Para entender this.emitEvent, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

Nuestro punto de partida es src/academy-learning-card.js. Este archivo implementa propiedades, render, dependencias scoped y eventos del componente. Antes de tocarlo en this.emitEvent, localiza su salida pública y predice quién dependerá de ella.

Vamos a completar src/academy-learning-card.js. Los imports traen Lit, mixins y dependencias. La clase declara scopedElements y properties; render usa esas entradas y handleContinue devuelve una intención pública. Después lo contrastaremos con src/mixins/WidgetMixin.js para verificar el contrato desde otro archivo.

Seguimos en src/mixins/WidgetMixin.js. Este archivo concentra traducción y emisión de intenciones públicas. Revisa qué recibe de src/academy-learning-card.js y qué deja preparado para demo/demo.js. Solo inspeccionamos src/mixins/WidgetMixin.js; cambiarlo también ocultaría la causa del resultado.

El siguiente enlace aparece en demo/demo.js. Este archivo consume la entrada pública y conecta propiedades, idioma y eventos. Revisa qué recibe de src/mixins/WidgetMixin.js y qué deja preparado para test/unit/academy-learning-card.test.js. En this.emitEvent, demo/demo.js queda intacto para comprobar el contrato desde fuera.

La última parada es test/unit/academy-learning-card.test.js. Este archivo prueba el componente desde su superficie pública. Compáralo con demo/demo.js: deben colaborar sin duplicar responsabilidades. test/unit/academy-learning-card.test.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Ya está conectado el recorrido. La comprobación importante es esta: El evento final incorpora el tag, viaja con bubbles y composed, y contiene un detail pequeño y estable. Si el preview contradice tu predicción sobre this.emitEvent, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Ahora piensa en el caso que suele romper este contrato: Emitir click reexpone un gesto, no una decisión; nombra qué ocurrió en el dominio del componente. En this.emitEvent, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

A continuación podrás repasar el modelo con calma sobre this.emitEvent. Después, el proyecto de this.emitEvent se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
