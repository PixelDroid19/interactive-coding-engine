---
lesson: open-cells-22
title: "22. Diagnosticar una dependencia sin registrar"
status: pendiente-de-voz
---

Diagnosticar una dependencia sin registrar. La etiqueta, el import y el registro forman tres eslabones; revisar los tres evita adivinar cambios. Hoy seguiremos el recorrido src/academy-notice-banner.js → src/mixins/WidgetMixin.js → demo/demo.js → test/unit/academy-notice-banner.test.js. Para entender Diagnosticar una dependencia sin registrar, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

Nuestro punto de partida es src/academy-notice-banner.js. Este archivo implementa la API, el render, las dependencias y el evento del artefacto protagonista. Antes de tocarlo en Diagnosticar una dependencia sin registrar, localiza su salida pública y predice quién dependerá de ella.

Este es el archivo que modificaremos: src/academy-notice-banner.js. Conectamos propiedades, traducciones, composición scoped y una salida observable sin copiar las dependencias reutilizadas. Después lo contrastaremos con src/mixins/WidgetMixin.js para verificar el contrato desde otro archivo.

Ahora pasamos a src/mixins/WidgetMixin.js. Este archivo concentra traducción y emisión de intenciones públicas. Revisa qué recibe de src/academy-notice-banner.js y qué deja preparado para demo/demo.js. Solo inspeccionamos src/mixins/WidgetMixin.js; cambiarlo también ocultaría la causa del resultado.

Después abrimos demo/demo.js. Este archivo consume la entrada pública y conecta propiedades, idioma y eventos. Revisa qué recibe de src/mixins/WidgetMixin.js y qué deja preparado para test/unit/academy-notice-banner.test.js. En Diagnosticar una dependencia sin registrar, demo/demo.js queda intacto para comprobar el contrato desde fuera.

La última parada es test/unit/academy-notice-banner.test.js. Este archivo conecta esta responsabilidad con el resto del proyecto. Compáralo con demo/demo.js: deben colaborar sin duplicar responsabilidades. test/unit/academy-notice-banner.test.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Ya está conectado el recorrido. La comprobación importante es esta: El diagnóstico indica archivo y contrato faltante sin entregar la línea final de solución. Si el preview contradice tu predicción sobre Diagnosticar una dependencia sin registrar, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Ahora piensa en el caso que suele romper este contrato: Cambiar HTML, import y registro a la vez borra evidencia; modifica un eslabón y vuelve a observar. En Diagnosticar una dependencia sin registrar, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

A continuación podrás repasar el modelo con calma sobre Diagnosticar una dependencia sin registrar. Después, el proyecto de Diagnosticar una dependencia sin registrar se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
