---
lesson: open-cells-49
title: "49. Parámetros de ruta"
status: pendiente-de-voz
---

Parámetros de ruta. Los parámetros son entrada de la visita, no estado global permanente.

Abrimos app/scripts/app-routes.js. Este archivo relaciona URL, nombre estable, tag de página y carga lazy. Observa su entrada y busca qué otro archivo lo consume.

Ahora construimos app/scripts/app-routes.js: cada objeto separa path de URL, name estable, component que se monta y action que importa la página solo cuando se visita.

Abrimos app/pages/academy-product-detail-page/academy-product-detail-page.js. Este archivo recibe parámetros de una visita y renderiza el detalle. Observa su entrada y busca qué otro archivo lo consume.

Abrimos app/pages/academy-home-page/academy-home-page.js. Este archivo coordina componentes, ciclo de página y navegación. Observa su entrada y busca qué otro archivo lo consume.

Abrimos package.json. Este archivo declara identidad, entradas, dependencias y comandos que consumen las herramientas. Observa su entrada y busca qué otro archivo lo consume.

Ejecutamos el proyecto después de conectar sus archivos. La evidencia que buscamos es esta: La ruta identifica campos, navigate los entrega y onPageEnter valida ausencia, tipo y valor.

Antes de practicar, contrasta el resultado con este error frecuente: Confiar en que id siempre existe crea pantallas vacías; ensaya navegación sin id y una ruta desconocida.

La lectura siguiente abre un laboratorio de proyecto para Parámetros de ruta. Allí corregirás archivos reales, construirás la vista previa y ejecutarás contratos sin recibir una solución copiada.
