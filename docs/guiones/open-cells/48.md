---
lesson: open-cells-48
title: "48. Navegación imperativa"
status: pendiente-de-voz
---

Navegación imperativa. navigate expresa una intención desde una página o Bridge; no debe filtrarse hacia componentes puramente visuales.

Abrimos app/components/academy-product-card/academy-product-card.js. Este archivo encapsula presentación y devuelve una intención mediante evento. Observa su entrada y busca qué otro archivo lo consume.

Abrimos app/pages/academy-home-page/academy-home-page.js. Este archivo coordina componentes, ciclo de página y navegación. Observa su entrada y busca qué otro archivo lo consume.

Abrimos app/scripts/app-routes.js. Este archivo relaciona URL, nombre estable, tag de página y carga lazy. Observa su entrada y busca qué otro archivo lo consume.

Ahora construimos app/scripts/app-routes.js: cada objeto separa path de URL, name estable, component que se monta y action que importa la página solo cuando se visita.

Abrimos package.json. Este archivo declara identidad, entradas, dependencias y comandos que consumen las herramientas. Observa su entrada y busca qué otro archivo lo consume.

Ejecutamos el proyecto después de conectar sus archivos. La evidencia que buscamos es esta: La página traduce el evento del hijo a navigate con un nombre conocido y parámetros explícitos.

Antes de practicar, contrasta el resultado con este error frecuente: Una tarjeta que importa el router deja de ser reutilizable; muévela a otra página y comprueba que aún funciona.

La lectura siguiente abre un laboratorio de proyecto para Navegación imperativa. Allí corregirás archivos reales, construirás la vista previa y ejecutarás contratos sin recibir una solución copiada.
