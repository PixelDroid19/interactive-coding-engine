---
lesson: open-cells-27
title: "27. Esperar recursos antes de comprobar texto"
status: pendiente-de-voz
---

Esperar recursos antes de comprobar texto. Una prueba asíncrona debe esperar la causa real —carga de catálogo y actualización Lit—, no un tiempo arbitrario. Hoy seguiremos el recorrido src/academy-learning-card.js → src/mixins/WidgetMixin.js → demo/demo.js → test/unit/academy-learning-card.test.js. Para entender Esperar recursos antes de comprobar texto, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

Empezamos por src/academy-learning-card.js. Este archivo implementa propiedades, render, dependencias scoped y eventos del componente. Antes de tocarlo en Esperar recursos antes de comprobar texto, localiza su salida pública y predice quién dependerá de ella.

Aquí hacemos el cambio principal en src/academy-learning-card.js. Los imports traen Lit, mixins y dependencias. La clase declara scopedElements y properties; render usa esas entradas y handleContinue devuelve una intención pública. Después lo contrastaremos con src/mixins/WidgetMixin.js para verificar el contrato desde otro archivo.

Después abrimos src/mixins/WidgetMixin.js. Este archivo concentra traducción y emisión de intenciones públicas. Revisa qué recibe de src/academy-learning-card.js y qué deja preparado para demo/demo.js. src/mixins/WidgetMixin.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Seguimos en demo/demo.js. Este archivo consume la entrada pública y conecta propiedades, idioma y eventos. Revisa qué recibe de src/mixins/WidgetMixin.js y qué deja preparado para test/unit/academy-learning-card.test.js. Solo inspeccionamos demo/demo.js; cambiarlo también ocultaría la causa del resultado.

Terminamos en test/unit/academy-learning-card.test.js. Este archivo prueba el componente desde su superficie pública. Compáralo con demo/demo.js: deben colaborar sin duplicar responsabilidades. En Esperar recursos antes de comprobar texto, test/unit/academy-learning-card.test.js queda intacto para comprobar el contrato desde fuera.

Ejecutamos el proyecto y buscamos una señal concreta: La prueba aguarda la promesa o evento de idioma y luego updateComplete antes de leer el DOM. Si el preview contradice tu predicción sobre Esperar recursos antes de comprobar texto, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Antes del ejercicio, intenta explicar este fallo: Un setTimeout largo solo esconde carreras; reemplázalo por la condición observable que necesitas. En Esperar recursos antes de comprobar texto, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

La lectura siguiente organiza lo que acabamos de ver sobre Esperar recursos antes de comprobar texto. Después, el proyecto de Esperar recursos antes de comprobar texto se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
