---
lesson: open-cells-09
title: "9. Componer mixins sin romper super"
status: pendiente-de-voz
---

Componer mixins sin romper super. Cada mixin envuelve una clase y coopera con la siguiente capa; super mantiene viva esa cadena de inicialización y ciclo. Hoy seguiremos el recorrido src/academy-state-panel.js → src/mixins/WidgetMixin.js → demo/demo.js → test/unit/academy-state-panel.test.js. Para entender Componer mixins sin romper super, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

Empezamos por src/academy-state-panel.js. Este archivo implementa la API, el render, las dependencias y el evento del artefacto protagonista. Antes de tocarlo en Componer mixins sin romper super, localiza su salida pública y predice quién dependerá de ella.

Ahora sí editamos src/academy-state-panel.js. Conectamos propiedades, traducciones, composición scoped y una salida observable sin copiar las dependencias reutilizadas. Después lo contrastaremos con src/mixins/WidgetMixin.js para verificar el contrato desde otro archivo.

El siguiente enlace aparece en src/mixins/WidgetMixin.js. Este archivo concentra traducción y emisión de intenciones públicas. Revisa qué recibe de src/academy-state-panel.js y qué deja preparado para demo/demo.js. src/mixins/WidgetMixin.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Ahora pasamos a demo/demo.js. Este archivo consume la entrada pública y conecta propiedades, idioma y eventos. Revisa qué recibe de src/mixins/WidgetMixin.js y qué deja preparado para test/unit/academy-state-panel.test.js. Solo inspeccionamos demo/demo.js; cambiarlo también ocultaría la causa del resultado.

Terminamos en test/unit/academy-state-panel.test.js. Este archivo conecta esta responsabilidad con el resto del proyecto. Compáralo con demo/demo.js: deben colaborar sin duplicar responsabilidades. En Componer mixins sin romper super, test/unit/academy-state-panel.test.js queda intacto para comprobar el contrato desde fuera.

Ejecutamos el proyecto y buscamos una señal concreta: Constructor y callbacks conservan super cuando la clase base lo requiere, y ninguna capa supone que es la única propietaria del ciclo. Si el preview contradice tu predicción sobre Componer mixins sin romper super, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Antes del ejercicio, intenta explicar este fallo: Omitir super puede dejar capacidades parcialmente activas; aísla el fallo comprobando cada método aportado por los mixins. En Componer mixins sin romper super, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

La lectura siguiente organiza lo que acabamos de ver sobre Componer mixins sin romper super. Después, el proyecto de Componer mixins sin romper super se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
