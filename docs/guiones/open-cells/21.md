---
lesson: open-cells-21
title: "21. Import permitido frente a import arbitrario"
status: pendiente-de-voz
---

Import permitido frente a import arbitrario. El runtime browser usa una allowlist porque ejecutar un import equivale a conceder código y red a ese módulo. Hoy seguiremos el recorrido src/academy-search-filter.js → src/mixins/WidgetMixin.js → demo/demo.js → test/unit/academy-search-filter.test.js. Para entender Import permitido frente a import arbitrario, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

Empezamos por src/academy-search-filter.js. Este archivo implementa la API, el render, las dependencias y el evento del artefacto protagonista. Antes de tocarlo en Import permitido frente a import arbitrario, localiza su salida pública y predice quién dependerá de ella.

Ahora sí editamos src/academy-search-filter.js. Conectamos propiedades, traducciones, composición scoped y una salida observable sin copiar las dependencias reutilizadas. Después lo contrastaremos con src/mixins/WidgetMixin.js para verificar el contrato desde otro archivo.

El siguiente enlace aparece en src/mixins/WidgetMixin.js. Este archivo concentra traducción y emisión de intenciones públicas. Revisa qué recibe de src/academy-search-filter.js y qué deja preparado para demo/demo.js. src/mixins/WidgetMixin.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Ahora pasamos a demo/demo.js. Este archivo consume la entrada pública y conecta propiedades, idioma y eventos. Revisa qué recibe de src/mixins/WidgetMixin.js y qué deja preparado para test/unit/academy-search-filter.test.js. Solo inspeccionamos demo/demo.js; cambiarlo también ocultaría la causa del resultado.

Terminamos en test/unit/academy-search-filter.test.js. Este archivo conecta esta responsabilidad con el resto del proyecto. Compáralo con demo/demo.js: deben colaborar sin duplicar responsabilidades. En Import permitido frente a import arbitrario, test/unit/academy-search-filter.test.js queda intacto para comprobar el contrato desde fuera.

Ejecutamos el proyecto y buscamos una señal concreta: Solo paquetes previstos, versiones fijadas y módulos locales normalizados entran al preview; lo demás falla con diagnóstico. Si el preview contradice tu predicción sobre Import permitido frente a import arbitrario, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Antes del ejercicio, intenta explicar este fallo: Aceptar cualquier URL hace irreproducible y riesgosa la práctica; prueba un paquete desconocido y exige un rechazo visible. En Import permitido frente a import arbitrario, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

La lectura siguiente organiza lo que acabamos de ver sobre Import permitido frente a import arbitrario. Después, el proyecto de Import permitido frente a import arbitrario se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
