---
lesson: open-cells-64
title: "64. Locales de aplicación"
status: pendiente-de-voz
---

Locales de aplicación. El shell decide idioma y las páginas consumen el mismo contrato de recursos sin duplicar catálogos por componente.

Abrimos test/unit/app.test.js. Este archivo comprueba la historia vertical desde rutas y páginas hasta datos. Observa su entrada y busca qué otro archivo lo consume.

Ahora construimos test/unit/app.test.js: las pruebas conectan rutas, páginas y data manager con valores variables; además verifican cleanup y respuestas antiguas.

Abrimos app/pages/academy-home-page/academy-home-page.js. Este archivo coordina componentes, ciclo de página y navegación. Observa su entrada y busca qué otro archivo lo consume.

Abrimos app/components/academy-product-card/academy-product-card.js. Este archivo encapsula presentación y devuelve una intención mediante evento. Observa su entrada y busca qué otro archivo lo consume.

Abrimos package.json. Este archivo declara identidad, entradas, dependencias y comandos que consumen las herramientas. Observa su entrada y busca qué otro archivo lo consume.

Ejecutamos el proyecto después de conectar sus archivos. La evidencia que buscamos es esta: Cambio de idioma actualiza la página activa y una página lazy carga su texto correcto al entrar después.

Antes de practicar, contrasta el resultado con este error frecuente: Precargar solo la página inicial deja huecos al navegar; prueba una ruta que todavía no se había importado.

La lectura siguiente abre un laboratorio de proyecto para Locales de aplicación. Allí corregirás archivos reales, construirás la vista previa y ejecutarás contratos sin recibir una solución copiada.
