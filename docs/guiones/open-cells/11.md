---
lesson: open-cells-11
title: "11. disabled como contrato observable"
status: pendiente-de-voz
---

disabled como contrato observable. disabled no es solo un color gris: bloquea interacción, comunica semántica accesible y evita emitir acciones. Hoy seguiremos el recorrido src/academy-learning-card.js → src/mixins/WidgetMixin.js → demo/demo.js → test/unit/academy-learning-card.test.js. Para entender disabled como contrato observable, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

La primera parada está en src/academy-learning-card.js. Este archivo implementa propiedades, render, dependencias scoped y eventos del componente. Antes de tocarlo en disabled como contrato observable, localiza su salida pública y predice quién dependerá de ella.

Aquí hacemos el cambio principal en src/academy-learning-card.js. Los imports traen Lit, mixins y dependencias. La clase declara scopedElements y properties; render usa esas entradas y handleContinue devuelve una intención pública. Después lo contrastaremos con src/mixins/WidgetMixin.js para verificar el contrato desde otro archivo.

Después abrimos src/mixins/WidgetMixin.js. Este archivo concentra traducción y emisión de intenciones públicas. Revisa qué recibe de src/academy-learning-card.js y qué deja preparado para demo/demo.js. En disabled como contrato observable, src/mixins/WidgetMixin.js queda intacto para comprobar el contrato desde fuera.

Seguimos en demo/demo.js. Este archivo consume la entrada pública y conecta propiedades, idioma y eventos. Revisa qué recibe de src/mixins/WidgetMixin.js y qué deja preparado para test/unit/academy-learning-card.test.js. demo/demo.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Cerramos el recorrido en test/unit/academy-learning-card.test.js. Este archivo prueba el componente desde su superficie pública. Compáralo con demo/demo.js: deben colaborar sin duplicar responsabilidades. Solo inspeccionamos test/unit/academy-learning-card.test.js; cambiarlo también ocultaría la causa del resultado.

Con los archivos enlazados, observa este contrato: Propiedad, atributo, control interno y evento mantienen el mismo estado; teclado y puntero observan el mismo bloqueo. Si el preview contradice tu predicción sobre disabled como contrato observable, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Haz una última predicción sobre este error frecuente: Cambiar únicamente CSS deja activo el comportamiento; intenta activar por teclado y escucha si todavía sale el evento. En disabled como contrato observable, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

El siguiente paso separa explicación y práctica sobre disabled como contrato observable. Después, el proyecto de disabled como contrato observable se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
