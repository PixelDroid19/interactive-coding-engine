---
lesson: open-cells-16
title: "16. ScopedElementsMixin"
status: pendiente-de-voz
---

ScopedElementsMixin. ScopedElementsMixin conecta el registro local con el Shadow Root que renderiza Lit. Hoy seguiremos el recorrido src/academy-product-list.js → src/mixins/WidgetMixin.js → demo/demo.js → test/unit/academy-product-list.test.js. Para entender ScopedElementsMixin, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

Nuestro punto de partida es src/academy-product-list.js. Este archivo implementa la API, el render, las dependencias y el evento del artefacto protagonista. Antes de tocarlo en ScopedElementsMixin, localiza su salida pública y predice quién dependerá de ella.

Vamos a completar src/academy-product-list.js. Conectamos propiedades, traducciones, composición scoped y una salida observable sin copiar las dependencias reutilizadas. Después lo contrastaremos con src/mixins/WidgetMixin.js para verificar el contrato desde otro archivo.

Seguimos en src/mixins/WidgetMixin.js. Este archivo concentra traducción y emisión de intenciones públicas. Revisa qué recibe de src/academy-product-list.js y qué deja preparado para demo/demo.js. Solo inspeccionamos src/mixins/WidgetMixin.js; cambiarlo también ocultaría la causa del resultado.

El siguiente enlace aparece en demo/demo.js. Este archivo consume la entrada pública y conecta propiedades, idioma y eventos. Revisa qué recibe de src/mixins/WidgetMixin.js y qué deja preparado para test/unit/academy-product-list.test.js. En ScopedElementsMixin, demo/demo.js queda intacto para comprobar el contrato desde fuera.

La última parada es test/unit/academy-product-list.test.js. Este archivo conecta esta responsabilidad con el resto del proyecto. Compáralo con demo/demo.js: deben colaborar sin duplicar responsabilidades. test/unit/academy-product-list.test.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Ya está conectado el recorrido. La comprobación importante es esta: La clase compone el mixin una sola vez y entrega clases importadas mediante scopedElements. Si el preview contradice tu predicción sobre ScopedElementsMixin, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Ahora piensa en el caso que suele romper este contrato: Aplicar el mixin pero olvidar el registro produce etiquetas sin comportamiento; inspecciona primero la clase asociada al nombre local. En ScopedElementsMixin, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

A continuación podrás repasar el modelo con calma sobre ScopedElementsMixin. Después, el proyecto de ScopedElementsMixin se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
