---
lesson: open-cells-05
title: "5. Workspace virtual y exportación"
status: pendiente-de-voz
---

Workspace virtual y exportación. Comprende cómo el proyecto vive en memoria, qué límites lo protegen y por qué el ZIP no es una maqueta. Hoy seguiremos el recorrido src/academy-learning-card.js → demo/demo.js → test/unit/academy-learning-card.test.js → README.md. Para entender Workspace virtual y exportación, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

La primera parada está en src/academy-learning-card.js. Este archivo implementa propiedades, render, dependencias scoped y eventos del componente. Antes de tocarlo en Workspace virtual y exportación, localiza su salida pública y predice quién dependerá de ella.

Ahora sí editamos src/academy-learning-card.js. Los imports traen Lit, mixins y dependencias. La clase declara scopedElements y properties; render usa esas entradas y handleContinue devuelve una intención pública. Después lo contrastaremos con demo/demo.js para verificar el contrato desde otro archivo.

El siguiente enlace aparece en demo/demo.js. Este archivo consume la entrada pública y conecta propiedades, idioma y eventos. Revisa qué recibe de src/academy-learning-card.js y qué deja preparado para test/unit/academy-learning-card.test.js. En Workspace virtual y exportación, demo/demo.js queda intacto para comprobar el contrato desde fuera.

Ahora pasamos a test/unit/academy-learning-card.test.js. Este archivo prueba el componente desde su superficie pública. Revisa qué recibe de demo/demo.js y qué deja preparado para README.md. test/unit/academy-learning-card.test.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Cerramos el recorrido en README.md. Este archivo explica cómo consumir, probar y continuar el proyecto fuera del curso. Compáralo con test/unit/academy-learning-card.test.js: deben colaborar sin duplicar responsabilidades. Solo inspeccionamos README.md; cambiarlo también ocultaría la causa del resultado.

Con los archivos enlazados, observa este contrato: Cada escritura produce un snapshot nuevo y aumenta la generación. Esto permite descartar respuestas atrasadas, persistir estados coherentes y comparar el trabajo del estudiante sin perder el proyecto anterior. Si el preview contradice tu predicción sobre Workspace virtual y exportación, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Haz una última predicción sobre este error frecuente: La exportación escribe entradas ZIP reales con CRC y conserva la jerarquía completa. package.json usa comandos Cells, de modo que el estudiante puede descomprimir el proyecto y continuar con el toolchain fuera de la plataforma. Un error frecuente consiste en cambiar varias fronteras a la vez; conserva una predicción y comprueba una sola causa antes de continuar. En Workspace virtual y exportación, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

El siguiente paso separa explicación y práctica sobre Workspace virtual y exportación. Después, el proyecto de Workspace virtual y exportación se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
