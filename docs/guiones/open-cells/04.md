---
lesson: open-cells-04
title: "4. Comandos reales en un navegador"
status: pendiente-de-voz
---

Comandos reales en un navegador. Distingue intención de comando, capacidad disponible y resultado verificable sin fingir una terminal de sistema. Hoy seguiremos el recorrido package.json → demo/index.html → demo/demo.js → demo/demo-build.js. Para entender Comandos reales en un navegador, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

Nuestro punto de partida es package.json. Este archivo declara identidad, entradas, dependencias y comandos que consumen las herramientas. Antes de tocarlo en Comandos reales en un navegador, localiza su salida pública y predice quién dependerá de ella.

Vamos a completar package.json. Type module permite usar import y export. exports señala la entrada pública; cells.entry señala el bootstrap de una app. Los scripts nombran operaciones reales de desarrollo, pruebas, locales y build. Después lo contrastaremos con demo/index.html para verificar el contrato desde otro archivo.

Seguimos en demo/index.html. Este archivo presenta controles y una instancia real del paquete. Revisa qué recibe de package.json y qué deja preparado para demo/demo.js. Solo inspeccionamos demo/index.html; cambiarlo también ocultaría la causa del resultado.

El siguiente enlace aparece en demo/demo.js. Este archivo consume la entrada pública y conecta propiedades, idioma y eventos. Revisa qué recibe de demo/index.html y qué deja preparado para demo/demo-build.js. En Comandos reales en un navegador, demo/demo.js queda intacto para comprobar el contrato desde fuera.

La última parada es demo/demo-build.js. Este archivo actúa como raíz del grafo que construye la demo. Compáralo con demo/demo.js: deben colaborar sin duplicar responsabilidades. demo/demo-build.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Ya está conectado el recorrido. La comprobación importante es esta: Cada operación lleva requestId, sessionId y generation. El identificador permite asociar respuesta y petición; la sesión evita mezclar laboratorios; la generación impide que un resultado viejo reemplace el proyecto más reciente. Si el preview contradice tu predicción sobre Comandos reales en un navegador, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Ahora piensa en el caso que suele romper este contrato: Cancelar no equivale a marcar una tarea como exitosa. El cliente rechaza la operación pendiente y el Worker recibe la orden. Los errores conservan código, archivo, línea, columna y una pista cuando esa información existe. En Comandos reales en un navegador, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

A continuación podrás repasar el modelo con calma sobre Comandos reales en un navegador. Después, el proyecto de Comandos reales en un navegador se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
