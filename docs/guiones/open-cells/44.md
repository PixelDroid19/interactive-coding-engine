---
lesson: open-cells-44
title: "44. Páginas declarativas y lazy"
status: pendiente-de-voz
---

Páginas declarativas y lazy. La tabla de rutas describe qué página corresponde a cada nombre y cómo cargarla cuando se necesita.

Abrimos app/scripts/app-routes.js. Este archivo relaciona URL, nombre estable, tag de página y carga lazy. Observa su entrada y busca qué otro archivo lo consume.

Abrimos app/pages/academy-home-page/academy-home-page.js. Este archivo coordina componentes, ciclo de página y navegación. Observa su entrada y busca qué otro archivo lo consume.

Ahora construimos app/pages/academy-home-page/academy-home-page.js: PageMixin aporta navegación y canales; ScopedElementsMixin resuelve la tarjeta. onPageEnter adquiere recursos, onPageLeave los libera y render conserva UI declarativa.

Abrimos app/pages/academy-product-detail-page/academy-product-detail-page.js. Este archivo recibe parámetros de una visita y renderiza el detalle. Observa su entrada y busca qué otro archivo lo consume.

Abrimos package.json. Este archivo declara identidad, entradas, dependencias y comandos que consumen las herramientas. Observa su entrada y busca qué otro archivo lo consume.

Ejecutamos el proyecto después de conectar sus archivos. La evidencia que buscamos es esta: Cada action devuelve un import dinámico y el componente declarado coincide con el tag definido por el módulo.

Antes de practicar, contrasta el resultado con este error frecuente: Importar todas las páginas al inicio contradice lazy loading; observa la red antes y después de navegar.

La lectura siguiente abre un laboratorio de proyecto para Páginas declarativas y lazy. Allí corregirás archivos reales, construirás la vista previa y ejecutarás contratos sin recibir una solución copiada.
