---
lesson: open-cells-08
title: "8. WidgetMixin y su responsabilidad"
status: pendiente-de-voz
---

WidgetMixin y su responsabilidad. WidgetMixin añade capacidades transversales de componente, especialmente traducción y emisión de eventos, sin decidir el aspecto ni el estado de negocio. Hoy seguiremos el recorrido src/mixins/WidgetMixin.js → src/academy-action-button.js → demo/demo.js → package.json. Para entender WidgetMixin y su responsabilidad, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

La primera parada está en src/mixins/WidgetMixin.js. Este archivo concentra traducción y emisión de intenciones públicas. Antes de tocarlo en WidgetMixin y su responsabilidad, localiza su salida pública y predice quién dependerá de ella. Solo inspeccionamos src/mixins/WidgetMixin.js; cambiarlo también ocultaría la causa del resultado.

Seguimos en src/academy-action-button.js. Este archivo implementa la API, el render, las dependencias y el evento del artefacto protagonista. Revisa qué recibe de src/mixins/WidgetMixin.js y qué deja preparado para demo/demo.js.

Ahora sí editamos src/academy-action-button.js. Conectamos propiedades, traducciones, composición scoped y una salida observable sin copiar las dependencias reutilizadas. Después lo contrastaremos con demo/demo.js para verificar el contrato desde otro archivo.

El siguiente enlace aparece en demo/demo.js. Este archivo consume la entrada pública y conecta propiedades, idioma y eventos. Revisa qué recibe de src/academy-action-button.js y qué deja preparado para package.json. demo/demo.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Cerramos el recorrido en package.json. Este archivo declara identidad, entradas, dependencias y comandos que consumen las herramientas. Compáralo con demo/demo.js: deben colaborar sin duplicar responsabilidades. Solo inspeccionamos package.json; cambiarlo también ocultaría la causa del resultado.

Con los archivos enlazados, observa este contrato: La clase usa this.t para texto visible y this.emitEvent para intenciones públicas; el render continúa siendo responsabilidad del componente. Si el preview contradice tu predicción sobre WidgetMixin y su responsabilidad, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Haz una última predicción sobre este error frecuente: Convertir el mixin en un almacén de estado global acopla instancias; revisa qué dato pertenece realmente al host. En WidgetMixin y su responsabilidad, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

El siguiente paso separa explicación y práctica sobre WidgetMixin y su responsabilidad. Después, el proyecto de WidgetMixin y su responsabilidad se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
