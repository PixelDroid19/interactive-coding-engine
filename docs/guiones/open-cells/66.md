---
lesson: open-cells-66
title: "66. Seguridad de iframe, Worker e imports"
status: pendiente-de-voz
---

Seguridad de iframe, Worker e imports. Código editable vive detrás de fronteras: Worker para transformación y tests; iframe sandbox para UI; allowlist para paquetes.

Abrimos package.json. Este archivo declara identidad, entradas, dependencias y comandos que consumen las herramientas. Observa su entrada y busca qué otro archivo lo consume.

Ahora construimos package.json: type module permite usar import y export. exports señala la entrada pública; cells.entry señala el bootstrap de una app. Los scripts nombran operaciones reales de desarrollo, pruebas, locales y build.

Abrimos app/config/prod.js. Este archivo declara una entrega reproducible sin opciones de depuración. Observa su entrada y busca qué otro archivo lo consume.

Ahora construimos app/config/prod.js: producción cambia únicamente configuración de entrega y desactiva opciones que no deben llegar al artefacto final.

Abrimos test/unit/app.test.js. Este archivo comprueba la historia vertical desde rutas y páginas hasta datos. Observa su entrada y busca qué otro archivo lo consume.

Ahora construimos test/unit/app.test.js: las pruebas conectan rutas, páginas y data manager con valores variables; además verifican cleanup y respuestas antiguas.

Abrimos README.md. Este archivo explica cómo consumir, probar y continuar el proyecto fuera del curso. Observa su entrada y busca qué otro archivo lo consume.

Ejecutamos el proyecto después de conectar sus archivos. La evidencia que buscamos es esta: No hay acceso a fs, procesos, sockets ni origen del host, y los errores cruzan por mensajes validados.

Antes de practicar, contrasta el resultado con este error frecuente: Añadir allow-same-origin junto con allow-scripts debilita el sandbox; revisa permisos como una lista mínima.

La lectura siguiente abre un laboratorio de proyecto para Seguridad de iframe, Worker e imports. Allí corregirás archivos reales, construirás la vista previa y ejecutarás contratos sin recibir una solución copiada.
