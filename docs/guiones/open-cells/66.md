---
lesson: open-cells-66
title: "66. Seguridad de iframe, Worker e imports"
status: pendiente-de-voz
---

Seguridad de iframe, Worker e imports. Código editable vive detrás de fronteras: Worker para transformación y tests; iframe sandbox para UI; allowlist para paquetes. Hoy seguiremos el recorrido package.json → app/config/prod.js → test/unit/app.test.js → README.md. Para entender Seguridad de iframe, Worker e imports, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

Empezamos por package.json. Este archivo declara identidad, entradas, dependencias y comandos que consumen las herramientas. Antes de tocarlo en Seguridad de iframe, Worker e imports, localiza su salida pública y predice quién dependerá de ella.

Este es el archivo que modificaremos: package.json. Type module permite usar import y export. exports señala la entrada pública; cells.entry señala el bootstrap de una app. Los scripts nombran operaciones reales de desarrollo, pruebas, locales y build. Después lo contrastaremos con app/config/prod.js para verificar el contrato desde otro archivo.

Ahora pasamos a app/config/prod.js. Este archivo declara una entrega reproducible sin opciones de depuración. Revisa qué recibe de package.json y qué deja preparado para test/unit/app.test.js.

Aquí hacemos el cambio principal en app/config/prod.js. Producción cambia únicamente configuración de entrega y desactiva opciones que no deben llegar al artefacto final. Después lo contrastaremos con test/unit/app.test.js para verificar el contrato desde otro archivo.

Después abrimos test/unit/app.test.js. Este archivo comprueba la historia vertical desde rutas y páginas hasta datos. Revisa qué recibe de app/config/prod.js y qué deja preparado para README.md.

Vamos a completar test/unit/app.test.js. Las pruebas conectan rutas, páginas y data manager con valores variables; además verifican cleanup y respuestas antiguas. Después lo contrastaremos con README.md para verificar el contrato desde otro archivo.

Terminamos en README.md. Este archivo explica cómo consumir, probar y continuar el proyecto fuera del curso. Compáralo con test/unit/app.test.js: deben colaborar sin duplicar responsabilidades. En Seguridad de iframe, Worker e imports, README.md queda intacto para comprobar el contrato desde fuera.

Ejecutamos el proyecto y buscamos una señal concreta: No hay acceso a fs, procesos, sockets ni origen del host, y los errores cruzan por mensajes validados. Si el preview contradice tu predicción sobre Seguridad de iframe, Worker e imports, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Antes del ejercicio, intenta explicar este fallo: Añadir allow-same-origin junto con allow-scripts debilita el sandbox; revisa permisos como una lista mínima. En Seguridad de iframe, Worker e imports, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

La lectura siguiente organiza lo que acabamos de ver sobre Seguridad de iframe, Worker e imports. Después, el proyecto de Seguridad de iframe, Worker e imports se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
