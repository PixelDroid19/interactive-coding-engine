---
lesson: open-cells-26
title: "26. Inicialización y cambio de idioma"
status: pendiente-de-voz
---

Inicialización y cambio de idioma. El idioma es estado de aplicación que debe estar listo antes de afirmar qué texto ve la persona. Hoy seguiremos el recorrido src/academy-learning-card.js → src/mixins/WidgetMixin.js → demo/demo.js → test/unit/academy-learning-card.test.js. Para entender Inicialización y cambio de idioma, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

La primera parada está en src/academy-learning-card.js. Este archivo implementa propiedades, render, dependencias scoped y eventos del componente. Antes de tocarlo en Inicialización y cambio de idioma, localiza su salida pública y predice quién dependerá de ella.

Este es el archivo que modificaremos: src/academy-learning-card.js. Los imports traen Lit, mixins y dependencias. La clase declara scopedElements y properties; render usa esas entradas y handleContinue devuelve una intención pública. Después lo contrastaremos con src/mixins/WidgetMixin.js para verificar el contrato desde otro archivo.

Ahora pasamos a src/mixins/WidgetMixin.js. Este archivo concentra traducción y emisión de intenciones públicas. Revisa qué recibe de src/academy-learning-card.js y qué deja preparado para demo/demo.js. En Inicialización y cambio de idioma, src/mixins/WidgetMixin.js queda intacto para comprobar el contrato desde fuera.

Después abrimos demo/demo.js. Este archivo consume la entrada pública y conecta propiedades, idioma y eventos. Revisa qué recibe de src/mixins/WidgetMixin.js y qué deja preparado para test/unit/academy-learning-card.test.js. demo/demo.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Cerramos el recorrido en test/unit/academy-learning-card.test.js. Este archivo prueba el componente desde su superficie pública. Compáralo con demo/demo.js: deben colaborar sin duplicar responsabilidades. Solo inspeccionamos test/unit/academy-learning-card.test.js; cambiarlo también ocultaría la causa del resultado.

Con los archivos enlazados, observa este contrato: El shell publica el cambio, los recursos terminan de cargar y los componentes actualizan sin recrear toda la app. Si el preview contradice tu predicción sobre Inicialización y cambio de idioma, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Haz una última predicción sobre este error frecuente: Afirmar inmediatamente después de cambiar idioma produce tests intermitentes; espera la señal de recursos listos. En Inicialización y cambio de idioma, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

El siguiente paso separa explicación y práctica sobre Inicialización y cambio de idioma. Después, el proyecto de Inicialización y cambio de idioma se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
