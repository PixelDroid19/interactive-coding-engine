---
lesson: open-cells-47
title: "47. Definir rutas y nombres"
status: pendiente-de-voz
---

Definir rutas y nombres. path pertenece a la URL; name es el identificador estable que usa navigate; component es el tag que se renderiza. Hoy seguiremos el recorrido app/scripts/app-routes.js → app/scripts/app.js → app/pages/academy-home-page/academy-home-page.js → package.json. Para entender Definir rutas y nombres, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

La primera parada está en app/scripts/app-routes.js. Este archivo relaciona URL, nombre estable, tag de página y carga lazy. Antes de tocarlo en Definir rutas y nombres, localiza su salida pública y predice quién dependerá de ella.

Aquí hacemos el cambio principal en app/scripts/app-routes.js. Cada objeto separa path de URL, name estable, component que se monta y action que importa la página solo cuando se visita. Después lo contrastaremos con app/scripts/app.js para verificar el contrato desde otro archivo.

Después abrimos app/scripts/app.js. Este archivo arranca una única instancia del runtime con outlet, rutas e inicio. Revisa qué recibe de app/scripts/app-routes.js y qué deja preparado para app/pages/academy-home-page/academy-home-page.js. En Definir rutas y nombres, app/scripts/app.js queda intacto para comprobar el contrato desde fuera.

Seguimos en app/pages/academy-home-page/academy-home-page.js. Este archivo coordina componentes, ciclo de página y navegación. Revisa qué recibe de app/scripts/app.js y qué deja preparado para package.json. app/pages/academy-home-page/academy-home-page.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Cerramos el recorrido en package.json. Este archivo declara identidad, entradas, dependencias y comandos que consumen las herramientas. Compáralo con app/pages/academy-home-page/academy-home-page.js: deben colaborar sin duplicar responsabilidades. Solo inspeccionamos package.json; cambiarlo también ocultaría la causa del resultado.

Con los archivos enlazados, observa este contrato: Cada ruta tiene las tres piezas coherentes y solo una ruta notFound. Si el preview contradice tu predicción sobre Definir rutas y nombres, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Haz una última predicción sobre este error frecuente: Navegar usando el path donde la API espera name rompe alias y cambios futuros; prueba un path alternativo para la misma página. En Definir rutas y nombres, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

El siguiente paso separa explicación y práctica sobre Definir rutas y nombres. Después, el proyecto de Definir rutas y nombres se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
