---
lesson: open-cells-33
title: "33. Aislamiento y timeout de pruebas"
status: pendiente-de-voz
---

Aislamiento y timeout de pruebas. Cada prueba necesita un DOM limpio y un límite de tiempo que convierta bloqueos en resultados diagnosticables. Hoy seguiremos el recorrido test/unit/academy-learning-card.test.js → src/academy-learning-card.js → demo/index.html → academy-learning-card.js. Para entender Aislamiento y timeout de pruebas, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

Empezamos por test/unit/academy-learning-card.test.js. Este archivo prueba el componente desde su superficie pública. Antes de tocarlo en Aislamiento y timeout de pruebas, localiza su salida pública y predice quién dependerá de ella.

Ahora sí editamos test/unit/academy-learning-card.test.js. Cada caso crea o usa la API pública, cambia valores, espera la actualización y comprueba DOM, scopedElements o eventos; no busca una línea exacta. Después lo contrastaremos con src/academy-learning-card.js para verificar el contrato desde otro archivo.

El siguiente enlace aparece en src/academy-learning-card.js. Este archivo implementa propiedades, render, dependencias scoped y eventos del componente. Revisa qué recibe de test/unit/academy-learning-card.test.js y qué deja preparado para demo/index.html. src/academy-learning-card.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Ahora pasamos a demo/index.html. Este archivo presenta controles y una instancia real del paquete. Revisa qué recibe de src/academy-learning-card.js y qué deja preparado para academy-learning-card.js. Solo inspeccionamos demo/index.html; cambiarlo también ocultaría la causa del resultado.

Terminamos en academy-learning-card.js. Este archivo registra el tag público que una aplicación puede instanciar. Compáralo con demo/index.html: deben colaborar sin duplicar responsabilidades. En Aislamiento y timeout de pruebas, academy-learning-card.js queda intacto para comprobar el contrato desde fuera.

Ejecutamos el proyecto y buscamos una señal concreta: El runner separa casos, restaura recursos y reporta qué promesa o evento no terminó. Si el preview contradice tu predicción sobre Aislamiento y timeout de pruebas, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Antes del ejercicio, intenta explicar este fallo: Compartir instancias hace que el orden cambie el resultado; ejecuta el mismo caso solo y después dentro de la suite. En Aislamiento y timeout de pruebas, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

La lectura siguiente organiza lo que acabamos de ver sobre Aislamiento y timeout de pruebas. Después, el proyecto de Aislamiento y timeout de pruebas se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
