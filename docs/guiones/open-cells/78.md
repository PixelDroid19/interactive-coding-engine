---
lesson: open-cells-78
title: "78. Feature flags con estado explícito"
status: pendiente-de-voz
---

Feature flags con estado explícito. Introduce capacidades de manera gradual sin repartir condicionales opacos por toda la aplicación. Hoy seguiremos el recorrido app/config/feature-flags.js → app/scripts/app-routes.js → app/components/academy-product-card/academy-product-card.js → app/scripts/channels.js. Para entender Feature flags con estado explícito, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

Empezamos por app/config/feature-flags.js. Este archivo conecta esta responsabilidad con el resto del proyecto. Antes de tocarlo en Feature flags con estado explícito, localiza su salida pública y predice quién dependerá de ella.

Este es el archivo que modificaremos: app/config/feature-flags.js. Leemos sus imports, su salida pública y la relación que mantiene con el siguiente archivo antes de ejecutarlo. Después lo contrastaremos con app/scripts/app-routes.js para verificar el contrato desde otro archivo.

Ahora pasamos a app/scripts/app-routes.js. Este archivo relaciona URL, nombre estable, tag de página y carga lazy. Revisa qué recibe de app/config/feature-flags.js y qué deja preparado para app/components/academy-product-card/academy-product-card.js. app/scripts/app-routes.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Después abrimos app/components/academy-product-card/academy-product-card.js. Este archivo encapsula presentación y devuelve una intención mediante evento. Revisa qué recibe de app/scripts/app-routes.js y qué deja preparado para app/scripts/channels.js. Solo inspeccionamos app/components/academy-product-card/academy-product-card.js; cambiarlo también ocultaría la causa del resultado.

Terminamos en app/scripts/channels.js. Este archivo nombra contratos de comunicación de larga distancia. Compáralo con app/components/academy-product-card/academy-product-card.js: deben colaborar sin duplicar responsabilidades. En Feature flags con estado explícito, app/scripts/channels.js queda intacto para comprobar el contrato desde fuera.

Ejecutamos el proyecto y buscamos una señal concreta: academy-feature-toggle resuelve una experiencia nueva o estable a partir de configuración validada. Las pruebas cubren flag activo, inactivo y ausente. Si el preview contradice tu predicción sobre Feature flags con estado explícito, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Antes del ejercicio, intenta explicar este fallo: Leer strings directamente en cada componente produce interpretaciones distintas. Si dos pantallas discrepan, localiza las lecturas y centraliza la resolución. En Feature flags con estado explícito, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

La lectura siguiente organiza lo que acabamos de ver sobre Feature flags con estado explícito. Después, el proyecto de Feature flags con estado explícito se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
