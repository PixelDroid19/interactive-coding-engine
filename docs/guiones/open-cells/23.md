---
lesson: open-cells-23
title: "23. this.t y claves visibles"
status: pendiente-de-voz
---

this.t y claves visibles. La interfaz pide significado mediante una clave; el catálogo decide la frase para el idioma activo. Hoy seguiremos el recorrido locales/locales.json → src/academy-learning-card.js → demo/demo.js → package.json. Para entender this.t y claves visibles, identificaremos quién posee cada dato, quién consume su salida y qué resultado observable confirma el contrato.

La primera parada está en locales/locales.json. Este archivo es la fuente de las traducciones del componente. Antes de tocarlo en this.t y claves visibles, localiza su salida pública y predice quién dependerá de ella.

Aquí hacemos el cambio principal en locales/locales.json. En y es repiten exactamente las mismas claves. El placeholder de nombre también debe coincidir para que this.t pueda sustituirlo sin casos especiales. Después lo contrastaremos con src/academy-learning-card.js para verificar el contrato desde otro archivo.

Después abrimos src/academy-learning-card.js. Este archivo implementa propiedades, render, dependencias scoped y eventos del componente. Revisa qué recibe de locales/locales.json y qué deja preparado para demo/demo.js. En this.t y claves visibles, src/academy-learning-card.js queda intacto para comprobar el contrato desde fuera.

Seguimos en demo/demo.js. Este archivo consume la entrada pública y conecta propiedades, idioma y eventos. Revisa qué recibe de src/academy-learning-card.js y qué deja preparado para package.json. demo/demo.js actúa como consumidor del cambio y no necesita otra edición en este paso.

Cerramos el recorrido en package.json. Este archivo declara identidad, entradas, dependencias y comandos que consumen las herramientas. Compáralo con demo/demo.js: deben colaborar sin duplicar responsabilidades. Solo inspeccionamos package.json; cambiarlo también ocultaría la causa del resultado.

Con los archivos enlazados, observa este contrato: Todo texto de producto usa claves estables, y una clave ausente se ve como error en vez de desaparecer. Si el preview contradice tu predicción sobre this.t y claves visibles, vuelve al último archivo editado y sigue su salida hasta el consumidor.

Haz una última predicción sobre este error frecuente: Mezclar texto literal y traducido crea pantallas parcialmente localizadas; busca nodos visibles fuera de this.t. En this.t y claves visibles, propón una causa, cambia una sola frontera y usa el preview o las pruebas para refutarla.

El siguiente paso separa explicación y práctica sobre this.t y claves visibles. Después, el proyecto de this.t y claves visibles se abrirá completo para que experimentes y compares soluciones equivalentes sin recibir una línea para copiar.
