---
lesson: componentes-lit-21
title: "Eventos y formularios en Lit"
status: listo_para_grabar
---

Conecta eventos declarativamente, lee valores en el momento correcto y emite resultados de dominio. En esta clase construiremos un editor de perfil con envío y validación; la meta no es memorizar una API, sino poder predecir el contrato antes de escribir código.

El template conecta el cable; el manejador traduce un evento DOM en una decisión de dominio y la publica.

Fíjate en la frontera, no solo en la sintaxis. Mientras construimos un editor de perfil con envío y validación, separa responsabilidades de plataforma, componente y consumidor.

Usa eventos declarativos para nodos del template y submit para acciones completas de formulario. Si el problema no necesita esa frontera, una función o HTML normal puede ser una decisión más sencilla.

Usa event.currentTarget, valida en la frontera y emite datos serializables y estables. El error que más veremos es este: llamar el manejador durante render, leer target equivocado, olvidar preventDefault o emitir el evento nativo como API.

Lleva el modelo a un editor de perfil con envío y validación. Si te atascas, vuelve al flujo de cuatro pasos; las pistas orientan la investigación, pero no entregan el programa.
