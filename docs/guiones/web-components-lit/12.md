---
lesson: componentes-lit-12
title: "Componentes asociados a formularios"
status: listo_para_grabar
---

Integra un custom element con formularios mediante ElementInternals, validación y estados disabled. En esta clase construiremos un campo de cantidad que participa en FormData; la meta no es memorizar una API, sino poder predecir el contrato antes de escribir código.

ElementInternals es el adaptador entre tu implementación interna y el protocolo de formularios del navegador.

Ahora pondremos el modelo bajo presión con un campo de cantidad que participa en FormData. Primero identifica los datos; luego el ciclo que actúa sobre ellos; al final comprueba la interfaz.

Úsalo para controles reutilizables que deban enviar, validar, resetearse o respetar disabled como un input nativo. Si el problema no necesita esa frontera, una función o HTML normal puede ser una decisión más sencilla.

No reemplaces controles nativos simples; implementa nombre accesible, validación, reset y disabled. El error que más veremos es este: ocultar un input sin sincronizar, olvidar name, no implementar reset o construir un control menos accesible que el nativo.

El siguiente paso ya no es copiar. Construye un campo de cantidad que participa en FormData, explica por qué funciona y busca un caso límite antes de enviar tu respuesta.
