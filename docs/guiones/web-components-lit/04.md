---
lesson: componentes-lit-04
title: "Shadow DOM como frontera"
status: listo_para_grabar
---

Encapsula estructura y CSS con Shadow DOM y decide conscientemente qué permanece público. En esta clase construiremos un aviso con estructura y estilos encapsulados; la meta no es memorizar una API, sino poder predecir el contrato antes de escribir código.

Shadow DOM es una pared con puertas diseñadas: protege detalles, no secretos, y obliga a decidir atributos, eventos, slots y parts públicos.

Ahora pondremos el modelo bajo presión con un aviso con estructura y estilos encapsulados. Primero identifica los datos; luego el ciclo que actúa sobre ellos; al final comprueba la interfaz.

Úsalo cuando estructura y estilos necesiten una frontera reusable; evítalo si ocultar el DOM rompe una integración que debía ser pública. Si el problema no necesita esa frontera, una función o HTML normal puede ser una decisión más sencilla.

Prefiere mode open, evita innerHTML con datos externos y ofrece puntos de extensión deliberados. El error que más veremos es este: pensar que closed protege secretos, bloquear toda personalización o renderizar datos externos como HTML.

El siguiente paso ya no es copiar. Construye un aviso con estructura y estilos encapsulados, explica por qué funciona y busca un caso límite antes de enviar tu respuesta.
